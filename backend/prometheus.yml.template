# This is a TEMPLATE, not the file Prometheus actually reads -- the prometheus-config-render
# service (docker-compose.yml) substitutes the environment placeholder below with the real
# ENVIRONMENT value (.env, defaults to "dev") and writes the rendered result to a shared
# volume before the prometheus container starts. Native Prometheus env-var substitution for
# external_labels exists (--enable-feature=expand-external-labels) but only from v2.55+; this
# repo pins v2.52.0, and bumping to the current 3.x LTS line to get it was judged out of scope
# for this fix (real breaking-change risk to alerts.yml/behavior, not verified here) -- see #324.
global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'buildnest-ecommerce'
    environment: '__ENVIRONMENT__'

# Alertmanager configuration
alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - alertmanager:9093

# Alert rule files (PERFORMANCE_OPTIMIZATION_GUIDE - Section 7)
rule_files:
  - 'alerts.yml'

scrape_configs:
  # BuildNest Application Metrics (PERFORMANCE_OPTIMIZATION_GUIDE - Monitoring & Metrics)
  - job_name: 'buildnest'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['buildnest-ecommerce:8080']
        labels:
          service: 'buildnest-ecommerce'
          tier: 'application'
    scrape_interval: 15s
    scrape_timeout: 10s
    relabel_configs:
      - source_labels: [__address__]
        target_label: instance

  # Prometheus Self-Monitoring
  - job_name: 'prometheus'
    static_configs:
      - targets: ['prometheus:9090']
        labels:
          service: 'prometheus'
          tier: 'monitoring'

  # mysql/redis/node scrape jobs deliberately removed (#325, decided with the user: Option B).
  # None of mysqld_exporter, redis_exporter, or node_exporter are deployed anywhere in
  # docker-compose.yml, so these targets were permanently "down" in Prometheus's own /targets
  # page -- noise that could mask a real outage among the expected-down noise, and this repo's
  # observability maturity doesn't yet have anything (e.g. Grafana dashboards, #122) consuming
  # this data. Re-add a job here, correctly wired to a real exporter service, if/when that
  # metrics tier is actually planned -- don't restore these as placeholders again.
