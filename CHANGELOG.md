# Changelog

All notable changes to this project are documented in this file.

Format follows [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).
Versioning follows [Semantic Versioning](https://semver.org/spec/v2.0.0.html).
Issue numbers reference the [GitHub issue tracker](https://github.com/pradip9096/buildnest-ecommerce-platform/issues).

Pre-1.0 convention: MINOR increments represent completed milestones; PATCH increments represent hotfixes within a milestone. `1.0.0` will mark the first production-ready release (end of M5).

---

## [Unreleased] — M4: Feature Development

### Added
- `epic-auto-close.yml` GitHub Action (#600) — auto-closes a parent epic with a summary comment
  once every native sub-issue under it is closed. GitHub's own Sub-issues feature only updates the
  parent's progress indicator on child-close, never closes it — 4 epics (#424/#430/#434/#437) and
  most recently #552 previously sat open with 100% completion until manually checked and closed.
- Seller data-isolation hardening (FR-SEL-08, #559) — audited the existing FR-SEL-04/05/06
  seller-scoped endpoints (`SellerProductController`/`SellerInventoryController`/
  `SellerOrderController`) and confirmed ownership isolation was already correctly enforced via
  class-level `@PreAuthorize("hasRole('SELLER')")` plus service-layer ownership-scoped repository
  queries (`findByIdAndSeller_Id`/`findByIdAndSellerId`/`findByProduct_IdAndProduct_Seller_Id`),
  already fully unit-tested for the rejection path. The real gap: every existing controller test
  constructed the controller directly with a mocked service, so `@PreAuthorize` had never been
  exercised through the real Spring Security proxy/filter chain by any test. Added
  `SellerDataIsolationIntegrationTest` (`@SpringBootTest`+MockMvc, real filter chain) proving
  non-SELLER rejection, owner access, and cross-seller rejection end-to-end for
  products/inventory/orders — the last sub-issue under Epic #552.
- Buyer-to-seller ratings/reviews (FR-SEL-07, #558) — `SellerReview` entity/table (`seller_review`,
  Liquibase changeset `20260728-026-create-seller-review.xml`), `SellerReviewController`
  (`/api/sellers/{sellerId}/reviews`, GET public), `SellerReviewServiceImpl` (create/update/delete
  with an average-rating and rating-distribution summary), mirroring the existing `ProductReview`
  pattern but scoped by the seller's `User.id` (per `Product.seller`/`SellerOrderController`'s
  established convention, not the separate `Seller` business-profile entity). Unlike its precedent,
  `updateReview` enforces ownership — `ProductReview.updateReview` lacks that check, a latent IDOR
  filed separately as a follow-up rather than fixed inline here. Frontend `SellerReviewPanel`
  surfaces from a delivered order's detail view; required adding a new `sellerId` field to
  `OrderResponseDTO` (a live instance of the backend-only-sub-issue-chain DTO-exposure gap
  documented from #581) so the frontend could link a buyer's order to the seller being rated.
  Verified via 20 backend unit tests, 4 frontend RTL tests, and a real H2-backed Spring context
  load (`CivilEcommerceApplicationTests`) confirming the new entity/changeset's schema mapping.

### Fixed
- (#558 follow-up) Null-checked `Product.seller` when deriving `Order.sellerId` in
  `OrderServiceImpl`/`CheckoutServiceImpl` — `Product.seller` is nullable (admin-created products
  have no owning seller), and the initial derivation dereferenced it unconditionally, caught by
  CI's `CheckoutFlowIntegrationTest` (500 instead of 200) rather than any mocked unit test.
- (#558 follow-up) Added the missing `SellerReviewControllerTest`, closing a JaCoCo
  `controller.user` package-coverage gap (0.84 vs 0.85 threshold) the new controller introduced.
- (#558 follow-up) Extracted `ReviewRatingUtils.buildDistribution` from the identical
  rating-distribution logic duplicated between `ProductReviewServiceImpl` and
  `SellerReviewServiceImpl`, resolving a SonarCloud new-code duplication gate failure (6.1% vs
  3% max).
- (#558 follow-up) Extracted `AbstractReview` (`@MappedSuperclass`) for the remaining duplicated
  entity fields/`@PrePersist`/`@PreUpdate` callbacks between `ProductReview` and `SellerReview`,
  resolving the residual SonarCloud duplication (5.3% after the first fix, still above the 3% max).

- Order-group schema for seller-scoped order splitting (FR-SEL-06, #578, first of three
  sub-issues under parent #557) — a cart spanning multiple sellers currently produces one
  shared `Order`; a real design decision (no repo precedent, confirmed via `gh search`) resolved
  to splitting a multi-seller cart into one `Order` per seller at checkout, rejecting a
  filter-only alternative that would have left order-status/fulfillment ownership ambiguous
  once sellers ship independently. This issue covers only the additive schema/entity layer: new
  `order_groups` table + nullable `orders.order_group_id` FK (Liquibase changeset
  `20260725-025-create-order-groups.xml`, no backfill needed — existing orders keep
  `order_group_id = NULL`), new `OrderGroup` JPA entity, `Order.orderGroup` mapping. Verified via
  a real H2-backed `@DataJpaTest` addition to `OrderRepositoryTest` (framework/mapping-level
  risk, not a mocked unit test). Fixed a pre-existing `OrderTest` break along the way: adding a
  field to `Order`'s Lombok `@AllArgsConstructor` broke its one positional `new Order(...)` test
  call site (a known recurring pattern — see the allargs-constructor-positional-test-fragility
  wiki lesson) — converted to setter-based construction. The checkout-split rewrite (#579) and
  seller-scoped order API (#580) remain unimplemented; a linked frontend follow-up (#581) covers
  the buyer/seller UI once those land.
- Multi-seller checkout order splitting (FR-SEL-06, #579, second of three
  sub-issues under parent #557) — rewrites `CheckoutServiceImpl`'s 3
  order-creation call sites (`initiatePayment`, `checkoutCart`,
  `checkoutWithPayment`) to group cart items by `product.seller` and
  create one `Order` per seller, linked via the `OrderGroup` schema from
  #578. A single-seller cart (the common case) still produces exactly one
  order and never creates an `OrderGroup`. Shipping and the session-level
  discount are apportioned across sibling orders proportionally to each
  seller's subtotal share — a deliberate, documented trade-off; precise
  seller-negotiated splitting is out of scope here (see #580). Payment
  stays a single combined charge against the group's total, initiated
  against the "primary" (first) seller-group order — per-seller payment
  splitting is not part of this issue. Adds `OrderGroupRepository` and
  `OrderRepository.findByOrderGroupId`, used by `confirmCheckout` to
  confirm every sibling order in a split as one unit. Verified via unit
  tests (service-layer splitting/apportionment logic — no new framework
  wiring beyond #578's already-tested JPA mapping). Seller-scoped order
  API (#580) remains unimplemented.
- Seller-scoped order API (FR-SEL-06, #580, third of three sub-issues under parent #557) —
  mirrors #555/#556's `SellerProductController`/`SellerInventoryController` ownership-scoping
  pattern for orders. `Order` carries no direct seller reference; ownership is derived
  transitively via `OrderItem.product.seller`, since #579's checkout split guarantees every item
  in one `Order` belongs to a single seller. New `OrderRepository.findBySellerId`/
  `findByIdAndSellerId` use an `EXISTS` subquery rather than an explicit join so pagination stays
  correct. A verified seller can list their own orders (`GET /api/user/seller/orders`,
  paginated), view detail (`GET /api/user/seller/orders/{id}`), and update status
  (`PATCH /api/user/seller/orders/{id}/status`) — reusing `OrderServiceImpl`'s existing
  `VALID_TRANSITIONS` state machine, which already excludes `PAID`/`PAYMENT_FAILED` as reachable
  targets (payment-webhook-only), so a seller can never set those through this path without any
  extra restriction logic. Defense-in-depth secured via `@PreAuthorize("hasRole('SELLER')")` plus
  the repository-level ownership filter (`AccessDeniedException` → 404/403 if the order does not
  belong to the seller). Verified via unit tests (service ownership/transition logic) and a real
  H2-backed `@DataJpaTest` addition to `OrderRepositoryTest` proving the `EXISTS`-subquery
  scoping actually filters correctly — a mocked service-layer test only proves parameter
  pass-through, never the query's own correctness.
- Frontend: buyer order-group view + seller order management UI (FR-SEL-06, #581) — completes
  the FR-SEL-06 sub-issue chain (#578/#579/#580) with a real frontend. Buyer-facing
  `account/OrdersTab.tsx` now groups sibling orders sharing the same `orderGroupId` under a
  "1 purchase, N shipments" label, mirroring `admin/OrdersTab.tsx`'s existing table pattern for
  the new seller side. New seller-facing surface (previously nonexistent): `/seller` route
  (`SellerDashboardPage`, `RequireAuth role="SELLER"`), `components/seller/OrdersTab.tsx`
  (list/detail/status-update against `/api/user/seller/orders`), `api/sellerOrders.ts`, and a
  Navbar entry gated on the `SELLER` role. **Scope expansion discovered mid-implementation**
  (Mid-Implementation Scope Discovery, judged SAME concern): `OrderResponseDTO` had no
  `orderGroupId` field despite `Order.orderGroup` existing since #578 — the buyer UI this issue
  asks for is impossible to build without it, so a small additive backend change
  (`orderGroupId` on `OrderResponseDTO`, populated in both `OrderServiceImpl.mapToResponseDTO`
  and `CheckoutServiceImpl.toOrderDTO`) was folded into this same issue rather than filed
  separately. Verified via backend unit tests (null/non-null `orderGroupId` mapping in both
  services) and frontend RTL tests (11 new/updated tests: seller list/empty/error/status-update/
  failed-update, buyer ungrouped-vs-grouped rendering). Full frontend suite (271 tests) and
  build pass.
- Seller-scoped inventory management (FR-SEL-05, #556) — mirrors #555's
  `SellerProductController`/`ProductServiceImpl` ownership-scoping pattern for inventory. A
  verified seller can list their own inventory (`GET /api/user/seller/inventory`, paginated)
  and adjust stock only for products they own (`PATCH /api/user/seller/inventory/{productId}`),
  defense-in-depth secured via `@PreAuthorize("hasRole('SELLER')")` plus a repository-level
  `findByProduct_IdAndProduct_Seller_Id` ownership check (404 if the product belongs to another
  seller). No new Liquibase changeset — reuses the existing `inventory`/`product` schema and
  `Product.seller` mapping from #555. Verified via Mockito-mocked unit tests (simple derived
  Spring Data queries, no custom JPQL — no framework-level risk requiring a real-context test).
  CI caught a genuine defect during this issue's own work: an `@throws` Javadoc reference
  wrapped across lines (to satisfy CheckStyle's line-length ratchet) is invalid Javadoc syntax,
  breaking the `javadoc:jar` goal only in CI's real `mvn verify`, not local `compile`/`test` —
  fixed by importing the exception classes and using simple names instead of FQNs.
- Seller-owned product catalogue (FR-SEL-03, FR-SEL-04, #555) — reactivates the dormant
  `supplier_id`/`fk_product_supplier` FK (`product` → `users`) already present in the original
  bootstrap schema, mapped onto `Product.seller`, per SDD v4.0's documented finding and design
  recommendation. No new Liquibase changeset — the column/FK already exist physically. A
  verified seller (`Seller.verificationStatus == VERIFIED`) can now create, update, and delete
  their own product listings, scoped so a seller can never touch another seller's products
  (`POST`/`PUT`/`DELETE`/`GET` under `/api/user/seller/products`, defense-in-depth secured via
  `@PreAuthorize("hasRole('SELLER')")` plus a service-layer `findByIdAndSeller_Id` ownership
  check). An unverified seller creating a product is rejected with 403. Verified via a real
  H2-backed `@DataJpaTest` that the FK mapping round-trips correctly (framework-mapping risk,
  not just service-layer logic).
- Admin seller verification/approval workflow (FR-SEL-02, #554) — mirrors
  `AdminOrderController.updateOrderStatus`'s admin-gated status-transition shape. Admins can
  list sellers by verification status (`GET /api/v1/admin/sellers?status=PENDING|VERIFIED|
  REJECTED`, defaulting to `PENDING`) and decide on a pending seller
  (`PATCH /api/v1/admin/sellers/{id}/verification-status`, `ROLE_ADMIN`-only), transitioning
  `PENDING`→`VERIFIED` or `PENDING`→`REJECTED` only (any other transition rejected with 400).
  The decided seller receives an email notification (approval or rejection with an optional
  reason). Live-verified against a running instance: 403 for a non-admin caller, correct state
  change for an admin decision, 400 for an invalid re-transition, 404 for an unknown seller.
- Seller registration (FR-SEL-01, #553) — the first requirement implemented from the Ph-3
  marketplace-pivot addendum. An authenticated buyer can register as a seller
  (`POST /api/user/seller/register`), creating a `Seller` entity (1:1 extension of `User`,
  mirroring the existing `Address` pattern) and granting `ROLE_SELLER`. `district_id` is
  implemented as a plain nullable column with no FK constraint — the SDD's planned
  `Seller ──N:1──► District` FK depends on an unresolved ADR (#561, OQ-01/OQ-02: strict
  same-district vs. seller-declared-radius matching); registration itself does not require it,
  so district assignment is deferred to a follow-up once #561 resolves. Added
  `GET /api/user/seller/profile` and a `DuplicateResourceException` → 409 handler to
  `GlobalExceptionHandler` (the exception class existed but had no handler before this issue,
  so a duplicate seller registration would have fallen through to a 500).

### Fixed
- Legacy inventory add-stock/update-stock endpoints had no server-side floor validation on
  quantity (#487, discovered during #440): `InventoryServiceImpl.addStock()`/`updateStock()`
  applied the caller-supplied value with no floor check, and `AdminInventoryController`'s
  `@RequestParam Integer quantity` had no `@Min(0)` — a negative value (e.g. via a raw API
  client, bypassing the frontend's client-side guard added in #440) could push
  `Inventory.quantityInStock` negative with no validation error. Not a #440 regression — the new
  frontend guard (`InventoryDetailModal`) rejects negative input client-side; this closed the
  pre-existing backend gap itself. Fixed with defense-in-depth, matching this repo's existing
  `@PreAuthorize`-at-both-layers pattern: `@Min(0)` + `@Validated` on the controller (added a
  `ConstraintViolationException` handler to `GlobalExceptionHandler`, which didn't exist before —
  needed since `@RequestParam` validation failures aren't caught by the endpoints' own
  `try/catch(Exception)` blocks, and would otherwise 500 instead of 400), plus a defensive check
  in both service methods (`addStock` also guards the *resulting* quantity, not just the
  argument, to protect against rows already corrupted by this exact bug before the fix shipped).
  5 new tests: 3 unit tests (`InventoryServiceImplTest`, including a pre-existing-negative-row
  scenario) and 2 `@WebMvcTest` tests (`AdminInventoryControllerTest`) proving the real Spring
  bean-validation binding rejects the request before the service is ever called
  (`verifyNoInteractions`).

### Documentation
- README Roadmap table was badly stale across multiple milestones (#489): M3 was marked
  **Complete — 13/13** even though 26 issues from a later CheckStyle debt-reduction initiative
  (epics #371/#376/#380/#387/#391 and their sub-issues, filed 2026-07-13) had been tagged into the
  M3 milestone afterward without ever updating M3's Roadmap status — root cause confirmed via
  `gh api .../milestones` plus a listing of M3's 26 open issues, all dated after M3 was originally
  marked complete. Corrected via the milestone API (not `gh issue list --milestone`, which is known
  to silently undercount — see
  `docs/wiki/learned-lessons/gh-issue-list-milestone-undercounts-use-gh-search-issues-instead.md`):
  M2 16/16 → 17/17, M3 **Complete** → **In progress**, 13/13 → 15/41, M5 29/72 → 52/90. M1 was
  already accurate and left unchanged. M4's own 190/212 was accurate at the time of the fix but
  drifted to 191/212 within minutes, since #489's own closure carries the M4 milestone label —
  corrected in the same closure pass via a small follow-up PR rather than left stale.

### Changed
- Eliminate `Product.stockQuantity`/`Inventory` dual source of truth (#485, INV-01), follow-up from
  #309: `Product.stockQuantity` is no longer a persisted column — it's now a derived getter reading
  `Inventory.quantityInStock` (`Product.getInventory()`), so there is exactly one writable
  representation of stock. Confirmed the drift the issue warned about was already live, not
  hypothetical: `ProductServiceImpl.updateProduct()` wrote `product.stockQuantity` from the request
  DTO but never touched `Inventory`, desyncing the two on every ordinary product edit immediately
  after creation. `createProduct()` now seeds `Inventory` and links it in-memory onto the returned
  entity (needed since `Product.inventory` is `mappedBy="product"`, never populated from the
  `Inventory` side); `updateProduct()` no longer writes stock at all — `CreateProductRequest.stockQuantity`
  is now create-only and explicitly ignored on update (stock changes go through
  `AdminInventoryController`'s adjust-inventory endpoint). `PublicProductDTO`/`ProductDocument`
  (search index)/`MapperUtil`/`ProductSearchServiceImpl` needed no changes — they already call
  `product.getStockQuantity()`, now transparently backed by `Inventory`. Liquibase changeset
  `20260721-023-drop-product-stock-quantity` drops the now-unused `products.stock_quantity` column
  (guarded the same way as `20260704-012`/`20260718-021`, since `products` is Hibernate-created in
  the test profile, not Liquibase-created). Frontend: `ProductFormModal.tsx`'s stock field is now
  create-only (disabled + not submitted on edit, with a hint pointing at the Inventory tab), since
  the backend silently ignoring it on update would otherwise be an invisible dead control.
  `ProductServiceImpl.detachCollections()` extended to also initialize the lazy `inventory`
  association (mirroring its existing tags/variants handling), needed both for the derived getter
  to be safe post-transaction and because a raw `Product` entity is still returned directly from
  `AdminProductController`'s create/update endpoints. New `ProductInventorySingleSourceOfTruthIT`
  (`@SpringBootTest`, real H2 persistence) is the regression guard for the actual bug: create seeds
  `Inventory` correctly, and an update carrying a different `stockQuantity` in the request DTO does
  not change the persisted `Inventory` row. One implementation pitfall worth recording: an earlier
  version of this fix called `inventoryRepository.save(inventory)` explicitly *and* linked it via
  `saved.setInventory(inventory)` — since `Product.inventory` is `cascade = ALL`, Hibernate's own
  cascade-persist at transaction flush re-inserted the identical row, causing a real (not
  hypothetical) unique-constraint violation caught by the new IT test itself; fixed by relying on
  cascade alone (dropped the explicit `inventoryRepository.save()` call and the now-unused
  `InventoryRepository` dependency from `ProductServiceImpl`).

### Added
- `ProductVariant` serialization regression test (#482): the issue's premise (`ProductVariant.product`
  lacking `@JsonIgnore`, same latent `LazyInitializationException` risk as #426's `ProductImage.product`
  fix) was already stale — `@JsonIgnore` had already been added to `ProductVariant.product` as a side
  effect of #427/PR #497 (2026-07-19), confirmed via `git blame`. Only the regression test the issue
  explicitly requested was missing. Added `ProductVariantTest.testProductFieldIsNotSerialized`,
  mirroring `ProductImageTest`'s established pattern, asserting the back-reference is never
  serialized. Also confirmed clean against the two other risk areas the governing wiki lesson
  (`raw-entity-with-lazy-collection-...md`) calls out: `ProductVariantRepository`'s queries already
  eager-fetch `product`/`inventory` via `@EntityGraph`, and no `@Cacheable` sits on the variant service.

- Dead backend code audit — decision record (#448): audited four endpoint groups with zero frontend callers (`ProductControllerV1` `/api/v1/products`, `ProductControllerV2` `/api/v2/products`, legacy single-step `CheckoutController` `/api/checkout`, `/auth/validate-token`) to determine whether each is genuine dead code or serves a not-yet-built external/mobile consumer, per direct verification against `frontend/src/api/{products,checkout}.ts` and `ApiSunsetInterceptor`. This audit's own factual groundwork had already been laid one day after #448 was filed, by #456/#471's endpoint-catalogue re-derivation — this issue adds the actual disposition decision on top of those already-verified facts. No code removed: V1 is a deliberate, already-documented deprecation-lifecycle demonstration (sunset 2026-12-31); V2/`CheckoutController`/`validate-token` are kept absent proof no external consumer exists, since removing API surface on absence-of-evidence is a product-scope call, not a code-cleanup call. Added a Dead-Code Audit Decision Record to SDD §4.7.3. Surfaced and filed as follow-ups: **#535 (priority: low)** — SDD mislabels unused `ProductControllerV2` "Current" and the actually-traffic-serving `HomeController` "Legacy"; **#536 (priority: low)** — reminder to revisit `ProductControllerV1` removal once its 2026-12-31 sunset passes (also references the pre-existing #504 raw-entity bug in the same controller).

- Inventory threshold & breach reporting UI (#433, part of Epic: Admin Analytics & Reporting UI): `AdminInventoryReportController`'s below-threshold/breaches/frequent-problems/product-detail/summary endpoints (base `/api/admin/inventory-reports`) were already implemented and tested but had no admin UI consumer — direct 1:1 mirror of the just-added Inventory Analytics tab pattern (#432/PR #528), a clean unambiguous precedent (Amendment #69, no `gh search` needed). Added a new "Inventory Thresholds" tab (`InventoryThresholdsTab.tsx`) showing summary stat cards, a below-threshold list with inline minimum-level editing (`AdminInventoryThresholdController.setProductThreshold`), a date-range-scoped threshold-breach list, and a frequently-low-stock-products list. Added `fetchInventoryBelowThreshold`/`fetchInventoryThresholdBreaches`/`fetchInventoryFrequentProblems`/`fetchInventoryReportSummary`/`setProductThreshold` (`api/admin.ts`) and wired the tab into `AdminDashboardPage.tsx`. Scope deliberately excludes category-level threshold configuration and the category-inheritance toggle (`setCategoryThreshold`/`getEffectiveThreshold`/`useProductCategoryThreshold`) — filed as a follow-up, **#532 (priority: low)**. No backend change — `InventoryReportService` returns plain `Map<String,Object>`/`List<Map<String,Object>>` built inside the service method (no raw JPA entities, no lazy-collection Jackson risk), confirmed by direct source read; neither controller is `@Cacheable`, so live browser verification was skipped per `testing.md`'s frontend-wiring skip criterion (both (a) no backend change and (b) no lazy/cache exposure hold). `InventoryThresholdsTab.test.tsx` (4 tests) covers stat/list rendering, empty state, fetch-error display, and the set-threshold mutation + reload. The issue's own body cited `AdminThresholdController` as an alternate controller name — verified against source and found incorrect (that controller is unrelated system-monitoring config); traced to a genuine, pre-existing SDD requirement-ID mistagging defect, filed separately as **#531 (priority: low)**, not fixed here. RTM `FR-INV-07` corrected from 🔵 Pending Ph-2 to ✅ Implemented; `FR-ADM-06` citation extended, stays 🟡 Partial (category-level remainder tracked in #532).

- Inventory analytics dashboard UI (#432, part of Epic: Admin Analytics & Reporting UI): `AdminInventoryAnalyticsController`'s high-demand-low-inventory/seasonal-patterns/stock-turnover/restocking-plan endpoints (base `/api/admin/inventory-analytics`) were already implemented and tested but had no admin UI consumer — direct 1:1 mirror of the just-added Sales Analytics tab pattern (#431/PR #526), a clean unambiguous precedent (Amendment #69, no `gh search` needed). Added a new "Inventory Analytics" tab (`InventoryAnalyticsTab.tsx`, distinct from the existing stock-CRUD "Inventory" tab) showing a date-range-scoped high-demand/low-stock list with risk-level badges, seasonal demand patterns, a stock-turnover list with health-status badges, and an on-demand predictive restocking plan (separate days-period control, since the backend endpoint takes a period rather than a date range). Added `fetchInventoryHighDemandLowStock`/`fetchInventorySeasonalPatterns`/`fetchInventoryStockTurnover`/`fetchInventoryRestockingPlan` (`api/admin.ts`) and wired the tab into `AdminDashboardPage.tsx`. No backend change — all four endpoints return `List<Map<String,Object>>`/`Map<String,Object>` of plain scalars (no JPA entities), confirmed by direct source read of `InventoryAnalyticsService`, and none is `@Cacheable`, so live browser verification was skipped per `testing.md`'s frontend-wiring skip criterion (both (a) no backend change and (b) no lazy/cache exposure hold). `InventoryAnalyticsTab.test.tsx` (3 tests) covers section rendering, fetch-error display, and the on-demand restocking-plan generation. RTM `FR-ADM-02` corrected from 🔵 Pending Ph-2 to ✅ Implemented; `FR-INV-07` (which additionally covers `InventoryReportService`, out of scope here) stays Pending.

- Sales analytics dashboard UI (#431, part of Epic: Admin Analytics & Reporting UI): `SalesAnalyticsController`'s dashboard/revenue/conversion-rate/cart-abandonment-rate/average-order-value/customer-lifetime-value endpoints (base `/api/v1/admin/analytics/sales`) were already implemented and tested but had no admin UI consumer. Added a new "Analytics" tab (`SalesAnalyticsTab.tsx`) — direct 1:1 mirror of `OverviewTab.tsx`'s existing stat-card + custom-CSS-bar pattern (no chart library added, since `OverviewTab.tsx` itself already anticipates this with its "live time-series charts available in the Analytics section" placeholder text), a clean unambiguous precedent (Amendment #69, no `gh search` needed). Shows stat cards (30-day revenue, average order value, conversion rate, cart abandonment rate) from the single `/dashboard` call (date-range selectable, defaults to last 30 days), a revenue-trend bar chart, top-selling products, revenue-by-category bars, and a customer-lifetime-value lookup by user ID (the one endpoint the dashboard call doesn't cover, since it's scoped to a single customer rather than the date range). Added `fetchSalesDashboard`/`fetchCustomerLifetimeValue` (`api/admin.ts`) and wired the tab into `AdminDashboardPage.tsx`. No backend change — `SalesDashboardDTO` is a flat DTO (`BigDecimal`/`Long`/`Double`/`List`/`Map` of plain nested DTOs, no lazy JPA associations) and the endpoint isn't `@Cacheable`, confirmed by direct source read, so live browser verification was skipped per `testing.md`'s frontend-wiring skip criterion (both (a) no backend change and (b) no lazy/cache exposure hold). `SalesAnalyticsTab.test.tsx` (4 tests) covers stat rendering, fetch-error display, CLV lookup success, and CLV input validation. RTM `FR-ADM-01` corrected from 🔵 Pending Ph-2 to ✅ Implemented.

- Admin search reindex trigger UI (#447): `AdminSearchController`'s `POST /api/v1/admin/search/reindex` (SRCH-02, #75) was already implemented and secured (`ROLE_ADMIN`) but had no admin UI consumer — confirmed via `find` that zero frontend caller existed anywhere. Rather than a new dashboard tab (no prior precedent for a single-action trigger button existed in this repo — confirmed via `gh search issues`/`gh search prs` for "trigger"/"reindex"/"action button", no hits — so this is ordinary UI-placement judgment, not a `solution-options-adr`-level architectural decision), added a "⟳ Reindex Search" button directly into the existing `ProductsTab.tsx` header (alongside "+ New Product"), since reindexing is a product-search-specific action. Added `triggerSearchReindex()` (`api/admin.ts`), returning the backend's own `ApiResponse.message` for display (success or the 503 "Elasticsearch is not enabled" case) rather than the more common `requestData` data-unwrapping helper, since this endpoint's payload is the message itself, not a data object. No backend change — the endpoint's response is a flat `ApiResponse<null>` with no lazy JPA associations and no `@Cacheable` exposure, so live browser verification was skipped per `testing.md`'s frontend-wiring skip criterion. 2 new tests added to `ProductsTab.test.tsx` (success message shown, backend error message surfaced on failure).

- Admin webhook subscription management UI (#446): `WebhookAdminController`'s CRUD endpoints (`GET`/`POST`/`PUT {id}/deactivate`/`DELETE`, base `/api/admin/webhooks`) were already implemented but had no admin UI consumer — direct 1:1 mirror of the just-added Shipping tab pattern (#445/PR #519), itself mirroring Categories, since a clean, unambiguous precedent existed (Amendment #69, no `gh search` needed). Unlike Shipping/Categories, there is no full-update (`PUT` for arbitrary field edits) endpoint on the backend — only create, list, deactivate, and hard-delete — so the new `WebhookSubscriptionFormModal.tsx` is create-only (no edit mode), and `WebhookSubscriptionsTab.tsx` offers both Deactivate (soft) and Delete (hard) actions per row, matching the controller's actual capability surface rather than the Shipping tab's edit+deactivate shape. Added `fetchAdminWebhookSubscriptions`/`createAdminWebhookSubscription`/`deactivateAdminWebhookSubscription`/`deleteAdminWebhookSubscription` (`api/admin.ts`) and wired a new "Webhooks" tab into `AdminDashboardPage.tsx`. No backend change — `WebhookSubscriptionResponse` is a flat DTO with no lazy JPA associations and no `@Cacheable` exposure, confirmed by direct source read, so live browser verification was skipped per `testing.md`'s frontend-wiring skip criterion (both (a) no backend change and (b) no lazy/cache exposure hold). `WebhookSubscriptionsTab.test.tsx` (9 tests) covers list/search/empty-state/create/validation (blank event type, invalid target URL)/deactivate success+failure/delete.

- Admin shipping-methods management UI (#445): `AdminShippingController`'s full CRUD (`GET`/`GET {id}`/`POST`/`PUT`/`DELETE`, base `/api/v1/admin/shipping-methods`) was already implemented and audited (SRS Appendix A.21, SDD's endpoint catalogue) but had no admin UI consumer at all — direct 1:1 mirror of the existing Categories tab pattern (`CategoriesTab.tsx`/`CategoryFormModal.tsx`, create+edit modal) since a clean, unambiguous precedent existed (Amendment #69, no `gh search` needed). Added `fetchAdminShippingMethods`/`createAdminShippingMethod`/`updateAdminShippingMethod`/`deactivateAdminShippingMethod` (`api/admin.ts`), `ShippingMethodsTab.tsx` + `ShippingMethodFormModal.tsx` (name/description/baseCost/costPerKg/estimatedDaysMin/Max fields, client-side validation mirroring the backend's own `@Valid` constraints plus a min/max-days ordering check the backend doesn't enforce), and wired a new "Shipping" tab into `AdminDashboardPage.tsx`. `DELETE` deactivates rather than hard-deletes (per `ShippingServiceImpl.deactivateShippingMethod`), so the tab follows Coupons' deactivate-not-delete UI convention rather than Categories' hard-delete one. No backend change — the endpoint's own contract, auth (`ROLE_ADMIN`), and tests were unaffected. `ShippingMethodsTab.test.tsx` (9 tests) covers list/search/empty-state/create/edit-prefill/validation (name length, max<min days)/deactivate success and failure paths, mirroring `CategoriesTab.test.tsx`'s structure.
- Real-time notifications UI — SSE consumer (#444): `NotificationController`'s `GET /api/user/notifications/stream` (`SseNotificationServiceImpl`, NOTIF-02, #63) was already fully implemented and tested server-side but had no frontend `EventSource`/consumer anywhere in the codebase — confirmed via grep before implementing. Added `useNotifications.ts` (opens an `EventSource` with `withCredentials: true` while authenticated, mirroring this app's `fetch(..., { credentials: 'include' })` cookie-auth convention rather than a header the native API can't set; listens for the `order-status` named event) and `NotificationBell.tsx` (bell icon + unread badge + dropdown, mirroring `Navbar`'s existing account/wishlist dropdown patterns), wired into `Navbar.tsx` next to the cart/wishlist icons, rendered only when authenticated. No real architectural decision — a standard native-`EventSource` consumer, so `solution-options-adr` was N/A. jsdom has no `EventSource`; added a minimal stub to `test/setup.ts` so components mounting one don't crash in unrelated tests, with dedicated tests using a finer-grained mock. Live end-to-end verification was partially blocked by an unrelated, pre-existing bug discovered mid-task (`apiRefresh()`'s 401 handling recurses into itself when the refresh endpoint itself returns 401, producing an unbounded client-side request loop — filed separately as **#516**, not fixed here); in its place, verified via the browser network panel that the real `EventSource` connects (`200`) and via a raw `curl -N` SSE stream against the real admin status-update endpoint that the backend emits exactly the `order-status` payload shape (`{orderId, previousStatus, newStatus}`) the hook parses, combined with 21 passing unit/component tests.
- Public live inventory-status endpoint wired into the product detail page (#443): `InventoryStatusController`'s `/status`, `/details`, `/available` endpoints (base `/api/inventory`) were never called by the frontend — `ProductDetailPage.tsx`/`ProductCard.tsx` showed stock derived only from the product payload's `stockQuantity`. Premise check found a second, deeper gap beyond the issue's own framing: despite the controller's own "Public controller" javadoc and both SRS Appendix A.15 and SDD's endpoint catalogue already documenting these 3 GET endpoints as `Public`, `SecurityConfig`'s `permitAll()` block never actually listed `/api/inventory/**` — it fell through to `anyRequest().authenticated()`, so every unauthenticated visitor to a guest-visible product page (`/products/:id` is not behind `ProtectedRoute`) would have received a 401 had the endpoint been wired in as originally titled. Confirmed via `AskUserQuestion` (security-relevant scope decision): widened `SecurityConfig`/`TestSecurityConfig` to `permitAll()` `GET /api/inventory/{id}/status|details|available`, correcting an implementation-vs-already-documented-design drift rather than making a new architectural choice. Added `api/inventory.ts` (`fetchInventoryStatus`) + `useInventoryStatus.ts` (mirrors `useProduct`'s `useAsync` pattern) and wired `/status` into `ProductDetailPage.tsx`'s stock display (falls back to the existing payload-derived display while loading/on error; cart-quantity gating stays payload-derived, unchanged). Listing-page (`ProductCard`) wiring deliberately deferred via a second `AskUserQuestion`: the 3 endpoints only take a single `productId` with no bulk variant, so calling `/status` per visible card would be N+1 — flagged as a real design trade-off rather than silently wired in or silently skipped. New `InventoryStatusPublicAccessIntegrationTest` (`@SpringBootTest`+`MockMvc`, asserts no 401/403 on all 3 endpoints without auth — the concrete regression test for the security-config gap itself) and `inventory.test.ts` (API function, mirrors `wishlist.test.ts`'s pattern). RTM `FR-INV-01` citation extended; SRS/SDD checked, already accurate (both already documented `Public` — this fix closes an implementation gap against their own stated design, not a new documented fact).
- Wishlist completeness — frontend (#442): `WishlistController`'s `contains/{productId}`, `count`, and `clear-all` (`DELETE`) endpoints were fully implemented server-side but had no frontend caller at all — `api/wishlist.ts` only wrapped `getWishlist`/`addToWishlist` (itself never called anywhere)/`removeFromWishlist`. Tracing why `contains` was unused surfaced the real gap: no wishlist toggle UI existed anywhere in the frontend. Added a shared `WishlistButton.tsx` (heart toggle, checks `contains` on mount, optimistic `add`/`remove` with revert-on-failure, hidden when unauthenticated) wired into `ProductCard.tsx` and `ProductDetailPage.tsx` — a direct mirror of `ProductCard`'s existing `handleAddToCart` optimistic-state pattern, no `AskUserQuestion` needed. Added `clearWishlist`/`getWishlistCount`/`isInWishlist` (`api/wishlist.ts`), a "Clear All" button in `WishlistTab.tsx` (confirm-gated, mirrors this repo's existing `confirm(...)` convention), and a wishlist count badge in `Navbar.tsx` (direct mirror of the existing `cartCount` badge pattern, linking to `/account`). Live browser verification (scratch user, real backend) surfaced and fixed a genuine, pre-existing backend defect blocking the wishlist page entirely (not introduced by this UI): `WishlistController.getWishlist()` (via `WishlistServiceImpl.getWishlistProducts`) returned raw `Product` entities with several uninitialized lazy fields (`category`, `inventory`, `variants`, `tags`) — under `open-in-view=false`, Jackson serializing the response after the transaction closed threw `LazyInitializationException`, surfacing as a bare `500 "Failed to write request"` on `GET /api/user/wishlist` for any wishlist containing a persisted product with populated associations. This is the 6th occurrence of the `raw-entity-with-lazy-collection-returned-from-controller-...` bug family (#425/#426/#440/#427/#441); fixed via `Hibernate.initialize()` on each lazy field in `WishlistServiceImpl.getWishlistProducts`, mirroring the established pattern. New `WishlistServiceImplLazyLoadingTest` (`@DataJpaTest`, asserts `Hibernate.isInitialized()` directly rather than relying on post-transaction serialization) added as a regression test, RED-GREEN confirmed against the fix — the existing Mockito-mocked `WishlistServiceImplTest` cannot observe this class of bug, since a mocked repository returns whatever plain (non-proxy) `Product` object the test constructs. `wishlist.test.ts`/`WishlistButton.test.tsx`/`WishlistTab.test.tsx`/`Navbar.test.tsx` cover the new API functions and UI behavior. RTM `FR-FE-20` citation extended to include `WishlistButton.tsx`/`Navbar.tsx`.
- Product reviews write path — frontend (#441): backend (`ProductReviewController`, `ProductReviewServiceImpl`) already supported full CRUD, but `ReviewsSection`/`useReviews` were read-only with no "write a review" form anywhere. Added `WriteReviewForm.tsx` (interactive star input + comment, gated behind `isAuthenticated`, sign-in prompt otherwise), `submitReview` (`api/reviews.ts`), and a `refetch` capability on `useReviews` so a successful submit updates the visible list/summary without a page reload. Mirrors `AddressStep.tsx`'s controlled-form pattern — no real architectural decision, no sibling-precedent search needed. Live browser verification (two distinct authenticated users, real backend) surfaced and fixed four genuine, pre-existing backend defects blocking the feature entirely, none introduced by this UI: (1) `Product.tags`/`variants`, once `Hibernate.initialize()`'d, remain Hibernate `PersistentSet`/`PersistentBag` at runtime — `@Cacheable`'s Redis JSON serializer (default typing) embeds that concrete class name on cache write, then fails to *instantiate* it on a cache-hit read (those classes require a live Hibernate session, no no-arg constructor), surfacing as a false "product not found" on the very next request for any product once cached; fixed by copying both to plain `HashSet`/`ArrayList` before returning (`ProductServiceImpl.detachCollections`). (2) `Inventory.getAvailableQuantity()` — a derived getter with no backing column — hit the identical cache round-trip asymmetry (serializes fine on write, "Unrecognized field" on deserialize-read); fixed via class-level `@JsonIgnoreProperties(ignoreUnknown = true)` rather than `@JsonIgnore` on the getter, since legacy admin inventory endpoints (`AdminInventoryController`, consumed by `InventoryDetailModal.tsx`) still return this raw entity and need the field serialized on writes. (3) `ProductReview.user`, a lazy `@ManyToOne`, was never initialized by any of `updateReview`/`markAsHelpful`/`getProductReviews`/etc.'s plain `findById`-style repository calls, throwing `LazyInitializationException` ("no session") once Jackson serialized the response post-transaction — fixed by initializing it in the service layer before every return, mirroring `ProductServiceImpl`'s own established pattern; `ProductReview.product` was `@JsonIgnore`'d instead of fixed, since no caller needs it nested (the product is already known from the URL) and it would have dragged in `Product`'s own lazy tags/variants graph. (4) Most notably: **`User.password` had no `@JsonIgnore` anywhere in the codebase** — no endpoint currently returns a raw `User` (`UserController`/`AdminUserController` both map to DTOs), so this had never actually leaked, but `ProductReview.user` would have been the first to do so had it gone unfixed; fixed globally on the entity, independent of this feature. Also found and fixed, via the same live-verification pass: `User.addresses` (unguarded lazy collection, the proximate trigger once `password` was hidden) and `Role.users`/`Permission.roles` (unguarded lazy back-references reachable through `User.roles`, which is `@ManyToMany(fetch = EAGER)`) — all four entity fixes follow this repo's established `@JsonIgnore`/plain-collection pattern already used for `ProductImage.product`/`ProductVariant.product`/`Inventory.thresholdBreaches` (#425/#426/#440). `WriteReviewForm.test.tsx` covers unauthenticated-prompt, missing-rating validation, successful submit + refetch, and the backend's duplicate-review error message surfacing verbatim. RTM `FR-REV-01` citation extended (was backend-only despite being ✅ Implemented).
- Checkout coupon application wiring (#436, part of Epic: Coupon Management End-to-End): the backend's `POST /api/v1/checkout/coupon` endpoint (`MultiStepCheckoutController.applyCoupon`, shipped under #77) had no frontend caller at all — `CartPage.tsx`'s own coupon input was a placeholder ("Coupon codes are not yet supported"), and the real checkout flow's `ShippingStep`/`PaymentStep` never surfaced discounts. Added `applyCheckoutCoupon` (`api/checkout.ts`) and a coupon-code input on `ShippingStep` (the only step where the session is `PENDING_SHIPPING`/`PENDING_PAYMENT`, the two states `CheckoutServiceImpl.applyCoupon` accepts) — direct 1:1 mirror of the existing `ShippingStep`/`CheckoutPage` step pattern, no `AskUserQuestion` needed (Amendment #69). `CheckoutSession`'s `couponCode`/`discountAmount` now flow into both `CheckoutPage`'s order-summary sidebar and `PaymentStep`'s totals, so the displayed grand total matches what `CheckoutServiceImpl.buildOrderFromCart` actually charges. Live browser verification (Chrome DevTools MCP, scratch user + scratch coupon, cleaned up after) caught a real bug the mocked RTL unit tests couldn't see: the coupon UI was originally a `<form>` nested inside `ShippingStep`'s own outer `<form>` — invalid HTML that made the "Apply" button submit the outer form (a real page navigation) instead of calling the coupon handler; fixed by converting it to a `<div>` with a `type="button"` click handler. Added `FR-CHK-09` to SRS/RTM — no requirement row ever existed for this customer-facing coupon-application requirement despite its sibling admin-side requirement (`FR-ADM-11`) existing since #435, and SDD's own endpoint catalogue was found citing the wrong FR (`FR-CHK-02`) for this same endpoint, fixed in the same pass. `ShippingStep.test.tsx`/`PaymentStep.test.tsx` cover the coupon form/error/applied-state UI and the discount-aware total math.
- Admin coupon CRUD UI (#435, part of Epic #434): new `CouponsTab.tsx` + `CouponFormModal.tsx` consuming `AdminCouponController`'s CRUD endpoints. The issue as filed only named `AdminCouponController`'s existing `POST`/`DELETE` endpoints, but the controller had no `GET`/list endpoint at all — unlike every sibling admin CRUD UI (`AdminProductTagController`, `AdminCategoryController`), which each expose full `GET`/`POST`/`DELETE`. Building the UI as literally scoped would have left "deactivate" with no way to discover coupon IDs to act on. Surfaced via `AskUserQuestion`; user chose to add the missing `GET /api/v1/admin/coupons` endpoint (`CouponService.getAllCoupons()` → `couponRepository.findAll()`) in this same issue rather than scope it down or split it out, matching the Mid-Implementation Scope Discovery "same concern, bigger than estimated" branch. `Coupon` has no lazy associations, so the entity is returned directly (matching `AdminProductTagController`'s pattern for `ProductTag`) with no lazy-serialization risk. Follows the established `useAsync` + `api/admin.ts` + Tailwind table/modal pattern set by `TagsTab.tsx`/`TagFormModal.tsx` (#429) — sibling-precedent check satisfied via direct source comparison (Amendment #69), no `AskUserQuestion` needed for the UI shape itself. Added `FR-ADM-11` to RTM — no requirement row existed for coupon admin management despite `CHK-02`'s checkout-side coupon application already being tracked separately. Live end-to-end browser verification surfaced a genuine, pre-existing backend defect blocking the issue's own "create a coupon" acceptance criterion: two Liquibase changesets (`20260624-005-create-coupons.xml`, `20260711-019-create-coupons.xml`) both define a `coupons` table for the same entity under different column names, and the newer, entity-matching one guards itself with "skip if table exists" — meaning on any environment where the older changeset runs first (its normal chronological order), the resulting schema never matches the `Coupon` entity, and every insert fails on a legacy `valid_from NOT NULL` column the entity never sets. Fixed via a new reconciling changeset (`20260719-022-reconcile-coupons-schema.xml`) that adds the entity's real columns (guarded, idempotent against drifted dev volumes) and drops the dead legacy ones — including a same-fix H2 gotcha where `#005`'s own `idx_coupons_active_validity` index had to be dropped before the columns it referenced, or `DROP COLUMN` fails outright. This is a schema/data migration, bumping the effective severity tier one level (Medium → High) for this specific fix per `development-workflow.md`'s risk-modifier table; the dropped columns were never referenced by any application code at any point, so the usual two-release drop safety window doesn't apply here.
- Admin product tag management UI (#429, part of Epic #424): new `TagsTab.tsx` + `TagFormModal.tsx` consuming `AdminProductTagController`'s CRUD endpoints (`GET/POST/PUT/DELETE /api/v1/admin/tags`), following the same `useAsync` + `api/admin.ts` + Tailwind table/modal pattern established by `CategoriesTab.tsx`/`CategoryFormModal.tsx` (#428) — a direct 1:1 mirror since tags are an equally flat name-based entity (simpler still: a single `name` field, no description/image/hierarchy, slug auto-derived server-side). No `AskUserQuestion` needed; the precedent was read directly rather than searched for, since it was an unambiguous single match with no competing design (per Amendment #69's direct-source-comparison allowance). Unlike #425/#426/#427/#440, `ProductTag`'s `products` back-reference already carried the correct `@JsonIgnore`/`@JsonIgnoreProperties` guards before this change, so no pre-existing lazy-serialization defect was found this time — live browser verification (create, edit, delete, duplicate-name error path, all confirmed via real HTTP request/response) surfaced no backend defects. Added FR-ADM-10 to SRS/RTM — no requirement row existed for tag management at all despite the backend and its Appendix A.19 endpoint catalogue already being complete.
- Admin product variant management UI (#427): new `ProductVariantsModal.tsx` and `fetchProductVariants`/`createProductVariant`/`updateProductVariant`/`deleteProductVariant` (`api/admin.ts`), consuming `AdminProductController`'s previously UI-less nested variant CRUD endpoints (`GET/POST/PUT/DELETE /api/v1/admin/products/{id}/variants`), opened via a new "Variants" button on each `ProductsTab` row. Follows the established admin-tab modal pattern (`useAsync` + `api/admin.ts` + Tailwind table/inline form) set by `ProductImagesModal`/#425/#426 — no `AskUserQuestion` needed, no competing design to choose between. Live end-to-end browser verification against a real running backend surfaced three genuine, pre-existing backend defects blocking the issue's own acceptance criteria (none introduced by this UI, all found via the `raw-entity-with-lazy-collection-returned-from-controller-throws-post-transaction-with-open-in-view-false.md` wiki lesson's pattern, recurring for a 4th time): (1) `ProductVariantRepository.findByProductId`'s `@EntityGraph` listed only `"inventory"`, leaving `product` an uninitialized lazy proxy — masked in `AdminProductVariantControllerIntegrationTest` by its class-level `@Transactional` keeping the Hibernate session open through serialization, so CI never caught it; fixed by adding `"product"` to the entity graph, matching the sibling `findById` query which already had it correctly. (2) `ProductVariantServiceImpl.createVariant()` set `createdAt` but never `updatedAt`, and the column is `NOT NULL` with no MySQL-side fallback once Hibernate binds an explicit `NULL` — every variant creation failed outright with a raw SQL error leaked to the client (`Column 'updated_at' cannot be null`, itself a separate, broader error-message-leakage pattern already present across this whole controller's catch blocks, out of this issue's scope and not fixed here). Fixed by setting `updatedAt` alongside `createdAt`, matching the pattern already correct in `updateVariant`/`deleteVariant`. (3) Once `product` was correctly initialized, its own lazy `tags` collection was still walked and thrown on during serialization (`ProductVariant.product` had no `@JsonIgnore`, unlike the established pattern already applied to `inventory`'s back-reference) — fixed by `@JsonIgnore`-ing `product` entirely, since the API consumer never needs it (the parent product is already in view) and `getEffectivePrice()`'s computed getter still uses it server-side without issue. New `ProductVariantRepositoryTest` (`@DataJpaTest`, asserts `Hibernate.isInitialized()` directly) added as a regression test for defect (1), independent of transaction-boundary masking; `ProductVariantsModal.test.tsx` covers list/create/edit/deactivate/error-surfacing. RTM `FR-PROD-08`/`FR-FE-23` citations updated.
- Admin user detail view + edit in `UsersTab` (#439): new `UserDetailModal.tsx` and `fetchAdminUser`/`updateAdminUser` (`api/admin.ts`), wiring `AdminUserController`'s previously-unused `GET/PUT /api/admin/users/{id}` — until now only the list and `DELETE` (disable) were called from the frontend, confirmed via source read of `UsersTab.tsx` before implementing; the backend endpoints themselves were already fully implemented, tested, and correct (return `AdminUserDto`, never the raw entity; `UpdateUserDTO` is already its own dedicated update DTO, not reused from create). Follows the established admin-tab modal pattern (`useState<T|null>` + row action button + `onSuccess` → in-place list update) set by `RefundModal`/`CategoryFormModal`/`ProductFormModal` — no `AskUserQuestion` needed, no competing design to choose between. Client-side email/phone validation mirrors the backend's `ValidEmail`/`ValidPhoneNumber` regexes; the `address` field on `UpdateUserDTO` was found unused by `AdminServiceImpl.updateUserByAdmin` (a pre-existing, out-of-scope dead field) and deliberately left out of the edit form rather than exposing a field that can't be populated or persisted correctly. Live end-to-end verification against a real running backend (view, edit, save, DB persistence check) confirmed no CORS/CSRF gap. RTM `FR-ADM-03` updated from 🔵 Pending Ph-2 to ✅ Implemented.
- Order refund action in Admin `OrdersTab` (#438): new `RefundModal.tsx` and `refundOrder` (`api/admin.ts`), wiring `AdminOrderController`'s previously-unused `POST /api/v1/admin/orders/{id}/refund` — until now only the order list and `PATCH .../status` were called from the frontend, confirmed via source read of `OrdersTab.tsx` before implementing. Follows the established admin-tab modal pattern (`useAsync` + `api/admin.ts` function + Tailwind modal) set by `InventoryTab`/`InventoryAdjustModal` (#440) exactly — no `AskUserQuestion` needed, no competing design to choose between. Client-side rejects a non-positive amount or blank reason before any request; the backend's own validation/error messages (payment not in `SUCCESS` status, amount exceeding refundable balance) surface verbatim via `client.ts`'s existing `ApiResponse.message` preference. RTM `FR-FE-25` updated to reflect the new refund action and its test coverage.
- Admin product CRUD UI (#425, part of Epic #424): new `ProductsTab.tsx` + `ProductFormModal.tsx` consuming `AdminProductController`'s CRUD endpoints (`GET/POST/PUT/DELETE /api/v1/admin/products`), following the same `useAsync` + `api/admin.ts` + Tailwind table/modal pattern established by `CategoriesTab.tsx` (#428). The form covers `CreateProductRequest`'s full field set (name, description, price, discountPrice, stockQuantity, SKU, category, imageUrl, isFeatured), mirroring the backend's own `ValidPrice`/`ValidQuantity`/`ValidSKU` constraints client-side. Category selection reuses the existing `fetchAdminCategories` call rather than duplicating it. Delete is a plain soft-delete (`Product.isActive = false`, no reference-blocking like categories have) — the list marks the row Inactive in place rather than removing it. Image and variant management are explicitly out of scope (separate sibling issues #426/#427). Live end-to-end verification against a real running backend surfaced a genuine, pre-existing backend defect (not introduced by this UI, but blocking its core edit flow): `AdminProductController`'s update/list endpoints return the raw `Product` JPA entity, whose lazy `@ManyToMany tags` collection is an uninitialized Hibernate proxy — with `spring.jpa.open-in-view=false`, Jackson serializing the response after the transaction closes threw `LazyInitializationException`, surfacing as an HTTP 500 on every real product edit (the underlying `UPDATE` had already succeeded; only the response serialization failed). Root-caused and fixed in `ProductServiceImpl.getAllProducts()`/`getProductById()` via `Hibernate.initialize(product.getTags())` while the session is still open, rather than suppressing the field or reworking the broader entity-vs-DTO architecture. `getProductsByCategory`/`searchProducts`/`updateProductImage` share the same latent risk but aren't exercised by this issue's own scope — tracked as a follow-up.
- Admin product image management UI (#426, part of Epic #424): new `ProductImagesModal.tsx` consuming `AdminProductController`'s image gallery endpoints (`GET/POST/DELETE /api/v1/admin/products/{id}/images`, `PATCH .../images/reorder`), opened via a new "Images" button on each `ProductsTab` row. Client-side file validation (type: jpeg/png/webp/gif, size: ≤10MB) mirrors `LocalStorageService`'s exact server-side contract, so bad uploads fail fast without a round trip. Reorder uses simple up/down buttons rather than a drag-and-drop library, matching #425's "no new dependency" precedent (no dnd library exists in `package.json`, and no prior sibling issue introduced one). Required extending `api/client.ts`'s `request()` with a `FormData` branch (skip `JSON.stringify`/`Content-Type` so the browser can attach the multipart boundary itself, while keeping the existing CSRF-header and 401-retry logic unchanged) — the first file-upload capability this client has ever needed; chosen (via `AskUserQuestion`, no prior sibling precedent existed for this specific sub-decision — confirmed via `grep`/`gh search`, both empty) over an isolated duplicate function, per this repo's established "route new capabilities through the existing shared boundary" convention (`rate-limiting.md`/`elasticsearch.md`). Live end-to-end browser verification surfaced two genuine, pre-existing backend defects blocking the issue's own acceptance criteria (neither introduced by this UI): (1) `ProductImage.product` had no `@JsonIgnore` on its back-reference to `Product` (unlike the established pattern on `Inventory.product`/`Category`'s parent-child fields), so any image-endpoint response walked into `Product`'s own lazy fields (e.g. `tags`) post-transaction and threw `LazyInitializationException`, the same defect class #425 found in `ProductServiceImpl` but a fresh instance here, not covered by #481's scope — fixed by adding `@JsonIgnore`, with a new `ProductImageTest` regression test asserting `product` is never serialized (a real, deterministic unit test, since `@JsonIgnore` doesn't depend on Hibernate proxy timing the way #425's fix did); (2) `SecurityConfig`'s CORS `allowedMethods` never included `PATCH`, so the browser (real CORS preflight enforcement, unlike curl or `TestSecurityConfig`'s test-only `*` wildcard) rejected the reorder endpoint with `403 Invalid CORS request` — fixed by adding `PATCH` to the allowed-methods list, which also latently fixes `updateOrderStatus`/`adjustInventory` (both already `@PatchMapping`, both previously broken from a real browser with no prior report). No automated regression test exists for the CORS fix — `SecurityConfig` is `@Profile("!test")` and excluded from every test context, and CORS preflight is browser-only enforced; verification is the live browser re-run itself, per `testing.md`'s tier-4/E2E allowance.
- Admin category management UI (#428, part of Epic #424): new `CategoriesTab.tsx` + `CategoryFormModal.tsx` consuming `AdminCategoryController`'s CRUD endpoints (`GET/POST/PUT/DELETE /api/v1/admin/categories`, implemented via #68), following the existing `useAsync` + `api/admin.ts` + Tailwind table/modal pattern already established by `UsersTab`/`InventoryTab`/`InventoryAdjustModal` — no new state library or fetch pattern introduced. Hierarchy is rendered as an indented tree (client-side, derived from each category's `parentCategory.id`), and the create/edit modal's parent selector excludes the category itself plus all of its descendants (a client-side UX mirror of the backend's own `resolveParent` cycle-prevention, which remains the actual source of truth). Delete surfaces the backend's exact block message when products or subcategories still reference the category, rather than a generic error, since `client.ts`'s `request()` already prefers the backend's `ApiResponse.message`. Live-verified end-to-end against a real running backend/MySQL/Redis: create (top-level and nested), hierarchy indentation, edit with self/descendant exclusion in the parent dropdown, delete blocked on a category with a subcategory (exact backend message surfaced), and delete succeeding once the subcategory was removed first.
- Production `frontend/Dockerfile` for the React/Vite frontend (OPS-07, #125): multi-stage build (`node:22-alpine` builder running `npm run build`, `nginxinc/nginx-unprivileged:1.27-alpine` runtime serving the static `dist/` output), non-root on port 8080 matching the issue's stated port — confirmed empirically (`docker exec ... id` → `uid=101(nginx)`), not a hand-rolled `nginx:alpine` + manual `USER`/`chown` setup, since the unprivileged image already encodes that exact guarantee and `backend/Dockerfile` (the only prior Dockerfile in the repo) was found to have silently shipped *without* a non-root user despite its own issue (#220, closed as a duplicate) claiming it — tracked separately by the still-open #124, not duplicated here. `nginx.conf` adds gzip, immutable long-lived caching for hashed static assets vs. `no-cache` on `index.html`, and SPA fallback (`try_files ... /index.html`) for client-side routing. Security headers (CSP, HSTS, X-Frame-Options, etc.) initially silently vanished from every response — root cause: nginx does not inherit a parent context's `add_header` directives into a `location` block that defines its own `add_header` (each location here already sets its own `Cache-Control`) — fixed by extracting the security headers into `security-headers.conf` and `include`-ing it explicitly in every `location` block; verified via `curl -D -` against a real running container, not just config inspection. `npm ci` also failed in the build stage on a pre-existing peer-dependency conflict (`eslint-plugin-react@7.37.5`'s peer range caps at eslint `^9.7`, repo pins `eslint ^10.3.0`) unrelated to containerization — reproduced identically outside Docker (`npm ci --dry-run` on the host); worked around with `--legacy-peer-deps` in the build stage only, tracked as a separate follow-up (#421) rather than folded into this issue's scope
- Real CodeQL semantic-analysis scanning, previously never run (#358). The only prior CodeQL reference in `.github/workflows/` was `security.yml`'s `upload-sarif` step, which only renders OWASP Dependency-Check's SARIF report through CodeQL's display plumbing — it never invoked CodeQL's own `init`/`analyze` engine, confirmed via `grep -rn codeql .github/workflows/*.yml` before implementing. Added a dedicated `codeql.yml` workflow (kept separate from `security.yml` rather than added as a job there, to avoid inheriting that file's `defaults.run.working-directory: backend`, which only applies to `run:` steps and not `uses:` steps — a gotcha this repo has hit once before) scanning both `java-kotlin` (backend, `build-mode: autobuild`, JDK 21) and `javascript-typescript` (frontend, `build-mode: none` — the frontend is real production code, ~7,000 lines across 86 files, not the stub `CLAUDE.md` currently describes, decided in scope via `AskUserQuestion`). Uses `codeql-action@v4` (verified via web search: v3 is being deprecated December 2026). Verified empirically on the PR's own CI run and via `gh api .../code-scanning/analyses`: two new genuine analysis records (`/language:java-kotlin`, `/language:javascript-typescript`), distinct from the pre-existing `dependency-check` SARIF category — both completed a real scan (zero findings), not a silent no-op
- `GET /api/v2/products/{id}/related` — returns up to 8 related products, same-category matches ranked ahead of shared-tag-only matches, excluding the source product and any inactive/out-of-stock product; result cached 5 minutes (`relatedProducts` Redis region) (PROD-04, #84). The issue's stated path (`GET /api/v1/products/{id}/related`) targets `ProductControllerV1`, which is `@Deprecated(forRemoval = true)`; added to `ProductControllerV2` (the actively maintained controller) instead, per explicit confirmation — same adaptation pattern as #83/#65/#63. New `ProductRepository.findRelatedProducts` JPQL query ranks via `CASE WHEN p.category.id = :categoryId THEN 0 ELSE 1 END`; a source product with no tags is queried against a sentinel tag id (`-1L`) rather than an empty JPQL `IN` list, which is invalid. PR #370's CI CheckStyle check initially failed (8338 violations vs. the 8305 baseline ceiling from #354); brought the new code under the ceiling with proper javadoc/final-parameter/line-length compliance and a small `CacheConfig.regionConfig()` helper, landing 2 violations under ceiling — same category of fix as #83's `package-info.java` backfill (line above). SonarCloud's PR analysis then caught a genuine `CRITICAL` self-invocation bug (`java:S6809`): `getRelatedProducts` called `getProductById` via `this`, so Spring's proxy never intercepted the call and `getProductById`'s own `@Cacheable` was silently bypassed — same class of bug `jpa.md` already documents for `@Transactional` self-calls. Fixed by calling `productRepository.findById` directly instead, matching the pattern already used in `deleteProduct`/`updateProductImage`. Added `ProductServiceCachingIntegrationTest` (`@SpringBootTest`, `spring.cache.type=simple` override since the `test` profile normally disables caching entirely) proving `getProductById`'s `@Cacheable` actually functions through the real Spring proxy — the existing Mockito-mocked `ProductServiceImplTest` suite structurally cannot observe proxy/caching behavior at all, so it gave zero regression protection against this class of bug recurring. Also backfilled `ProductRepositoryTest` with 6 new repository-level scenarios for `findRelatedProducts` (ranking order, source exclusion, inactive exclusion, out-of-stock exclusion including zero/negative-available, no-inventory-row exclusion, pageable limit) — the acceptance criteria for #84 were previously only verified against a *mocked* repository at the service layer, never against real H2-backed data at the layer that actually implements them. Sonar's PR quality gate then failed on `new_coverage` (61.5% vs. 80% required, matching `codecov/patch`): `CacheConfig`'s Redis-conditional `cacheManager` bean (including the new `relatedProducts` region/`regionConfig()` helper) was never exercised by any test, since `ProductServiceCachingIntegrationTest` overrides `spring.cache.type=simple` and never activates the `redis`-conditional bean; `ProductControllerV2.getRelatedProducts` also had no controller-level test at all. Added `CacheConfigTest` (plain unit test, `RedisConnectionFactory` mocked, no live Redis needed) covering the `cacheManager` bean directly, and a `ProductControllerV2Test` case for the new endpoint. Coverage still failed both gates after those tests (73.1% vs. 80%) — root cause turned out to be one level deeper: `pom.xml`'s `jacoco-maven-plugin` `report` execution has a long-standing `<exclude>**/config/**</exclude>` (among others), so `CacheConfig.java` never gets coverage data in the report at all regardless of test effort — `CacheConfigTest` genuinely runs and passes in the exact CI job that generates the report, but the report generator structurally never includes those lines. Fixed by aligning SonarCloud's `sonar.coverage.exclusions` (`.github/workflows/security.yml`) and a new `codecov.yml`'s `ignore:` list with `pom.xml`'s existing jacoco excludes, closing the gap between the three configs rather than testing against an unmeasurable target — tracked separately as #396 (a pre-existing, repo-wide CI-config inconsistency, not scoped to #84, per this repo's Mid-Implementation Scope Discovery process) even though the fix landed in this same PR since it was directly blocking merge
- Inventory sub-actions in Admin `InventoryTab` (#440, part of Epic #437): new `InventoryDetailModal.tsx`, opened via a "Details" button alongside the existing "Adjust" action, wires `AdminInventoryController`'s previously-unused legacy sub-path endpoints (`GET /product/{id}`, `POST /add-stock/{id}`, `POST /update-stock/{id}`, `GET /check-availability/{id}`) — until now only the versioned `GET /` list and `PATCH /{id}` (delta+reason) adjust endpoint were called from the frontend, confirmed via `grep -rn inventory frontend/src` before implementing. Follows the established admin-tab pattern (`useAsync` + `api/admin.ts` functions + Tailwind modal) set by #425/#426/#428 — no `AskUserQuestion` needed, no competing design to choose between. Client-side rejects negative/non-numeric quantities before any request for all three mutating/query actions. Live browser verification against a real running backend surfaced a genuine, pre-existing backend defect (not introduced by this UI, but blocking `GET /product/{id}` outright with a 500 `"Failed to write request"`): `Inventory.thresholdBreaches` (a lazy `@OneToMany`) was not `@JsonIgnore`'d, unlike its sibling `product`/`variant` fields on the same entity — the same defect class #425's `ProductServiceImpl`/#426's `ProductImage.product` lesson already documents, a fresh instance here. Fixed by adding `@JsonIgnore`, matching the existing pattern on `product`/`variant`. This also caught a mismatch in the new TypeScript `InventoryDetail` interface, which had assumed the list endpoint's DTO field names (`quantity`/`reservedQuantity`) rather than the legacy endpoint's actual raw-entity field names (`quantityInStock`/`quantityReserved`) — corrected before the fix was verified. Discovered, not fixed here (filed as a follow-up, #487): the backend's legacy `addStock`/`updateStock` service methods have no server-side floor validation on quantity — only the new client-side guard prevents a negative value reaching the API, a pre-existing gap in code this issue only wires to, not introduces.
- Elasticsearch ILM (Index Lifecycle Management) retention policy for `app-logs-*` indices (#326). New `elasticsearch-ilm-setup` one-shot sidecar (mirroring `prometheus-config-render`/`alertmanager-config-render`) registers a delete-only `app-logs-retention-policy` (`min_age: 30d`, no rollover action — Logstash already creates one new index per day) and an `app-logs-template` index template (`index_patterns: ["app-logs-*"]`, `index.lifecycle.name`) before `logstash` starts, so every new daily index picks up the policy automatically with no manual step. 30-day retention window chosen as a reasonable default absent a specific compliance/audit requirement (confirmed with the user; see the docker-compose.yml comment above the new service for the full rationale). Live-verified end-to-end: registered the policy/template via the real sidecar, confirmed a template-matching index auto-inherits `index.lifecycle.name`, and forced a scratch policy through the delete phase to confirm ILM's delete action actually removes the index (not just theoretical config) — then confirmed the real pipeline (`nc` → Logstash TCP input → filter chain → Elasticsearch output) produces an index that inherits the real 30-day policy the same way

### Fixed
- `ProductServiceImpl.getProductsByCategory`, `searchProducts`, and `updateProductImage` shared the same `LazyInitializationException` risk #425 fixed in `getAllProducts`/`getProductById` — each returned a raw, persisted `Product` entity with an uninitialized lazy `tags`/`variants` proxy, which throws once Jackson serializes the response after the transaction closes under `open-in-view=false` (#481). Deliberately left unfixed by #425 (out of its own scope, per Mid-Implementation Scope Discovery) since none of these three methods were exercised by admin CRUD. Fixed by calling the existing private `detachCollections()` helper (already applied to `getAllProducts`/`getProductById`) in all three — a single, unambiguous precedent, mirrored directly with no `AskUserQuestion`/`gh search` needed. Added `ProductServiceImplLazyLoadingImplTest` (`@DataJpaTest` + `TestEntityManager`, asserting `Hibernate.isInitialized()` directly against a real persisted entity after `entityManager.clear()`), mirroring `WishlistServiceImplLazyLoadingImplTest`'s pattern (#442) — a `@Transactional`-wrapped test method would keep the Hibernate session open through assertions and mask the exact failure a real HTTP request hits; the existing Mockito-mocked `ProductServiceImplTest` suite cannot observe this at all, since a mocked repository never returns a real Hibernate proxy. Full backend suite (1751 tests) green, no regressions.
- `products.stock_quantity` and the `inventory` table were two independent representations of stock with no seed data or sync mechanism (#309) — filed as a low-severity "Admin Inventory tab shows no data" cosmetic bug, but tracing every reader of `Inventory` found the real impact was worse: `CheckoutServiceImpl.validateCartForCheckout()` calls `InventoryServiceImpl.hasStock()`, which returns `false` (not an error) whenever no `Inventory` row exists for a product — meaning **every real product with no backing `Inventory` row silently failed checkout**, not just the admin tab. Confirmed live against the running dev MySQL (0 rows in `inventory` despite real rows in `products`) before scoping the fix; relabeled the issue `priority: high` to reflect the corrected severity, confirmed with the user via `AskUserQuestion` alongside the fix-direction choice (backfill + prevent recurrence, vs. a larger single-source-of-truth refactor eliminating `Product.stockQuantity` entirely — deferred as a follow-up, not required to close this issue). Fixed two ends: (1) `ProductServiceImpl.createProduct()` now creates a matching `Inventory` row for every new product (mirroring the existing `ProductVariantServiceImpl.createVariant()` pattern), so the defect cannot recur for future products; (2) a new Liquibase changeset (`20260718-021-backfill-inventory-for-products.xml`) backfills one `Inventory` row per existing product lacking one, using `stock_quantity` as the initial `quantityInStock`. Guarded with a `tableExists` `preConditions` (`onFail="MARK_RAN"`) on both `products` and `inventory`, since `products` is Hibernate-drift (not Liquibase-managed, per the existing `known-table-drift-list` lesson) and doesn't exist yet when Liquibase runs in the `test` profile (Hibernate `create-drop` runs afterward) — without the guard this would fail every test-profile startup. Verified live end-to-end against the real running backend/MySQL: applied the backfill (confirmed via `Inventory` row appearing for a pre-existing product with `stock_quantity IS NULL`, correctly defaulted to 0/`OUT_OF_STOCK`), created a new product via the real admin API (confirmed a matching `IN_STOCK` `Inventory` row was created automatically), and confirmed `GET /api/v1/admin/inventory` (the Admin Inventory tab's own endpoint) now returns real data and `check-availability` now returns `true` for a product with real stock — the original symptom and the deeper checkout-blocking defect are both resolved
- A user-requested "verify RTM/SRS/SDD/Test Plan against source code" pass found two distinct classes of drift, both surfacing despite prior fixes: (1) Test Plan §17 was correctly updated in #461 to the real gate values (JaCoCo 85% INSTRUCTION/PACKAGE, PIT mutation threshold 77%), but §8.3/§8.4/§9.1/§9.2 elsewhere in the *same document* were never touched and still stated the pre-fix 0.40/0.70/75% values (plus a wrong Counter type, LINE instead of the real INSTRUCTION) — a direct self-contradiction within one file; corrected all four to match §17 and verified against `pom.xml` directly. (2) Every cross-document `Related SRS`/`Related SDD`/`Related TP`/`Related RTM` pointer across RTM, SDD, Test Plan, and SDP had drifted out of sync with its target's real current version — some by one bump (RTM's `Related SRS` behind after today's #474 SRS bump), some never updated even once since the 2026-06-19 baseline (Test Plan's own `Related SRS`/`Related SDD` fields stuck at v4.0/v3.0; SDP's header fields and its Appendix D "Document Suite Summary" table both stuck at every sibling's original baseline version). Updated all cross-references to current (SRS v4.5, SDD v3.5, Test Plan v4.2, RTM v1.7, SDP v1.2) and bumped each touched document's own Document Control Version/Revision History per `[defect-class: sdlc-doc-version-never-bumped]`. Confirmed still accurate and untouched: RTM's Coverage Summary Totals (recomputed from all 24 category rows: 180/113/16/51/0/0, matches), SDD's §4.2.3 Component Statistics (recomputed live via the table's own cited `find`/`grep` commands: 352/195/38/46, matches — no backend code changed since #458), and SDP's tech-stack/test-count/risk-register content (already correct from #461)
- SDD §4.7.3's API Endpoint Catalogue was stale in the same way SRS Appendix A was before #456's fix — only 7 endpoint groups documented (vs. 36 real controllers), wrong path prefixes throughout (`/api/auth/refresh-token` instead of the real `/api/auth/refresh`, `/api/auth/forgot-password`/`reset-password` instead of the real `PasswordResetController`'s `/api/password/forgot`/`/api/password/reset`), and only the legacy `/api/checkout/*` flow catalogued — the current `/api/v1/checkout/*` multi-step flow (`MultiStepCheckoutController`) was missing entirely (#471, follow-up from #456). Performed a full per-endpoint re-derivation directly from controller source, mirroring #456's methodology (sibling-precedent match, no `AskUserQuestion` needed since the precedent left no real alternative) but producing a different table shape: this catalogue also carries a per-row Rate Limit column, which #456's SRS Appendix A does not. Verifying that column against `RateLimitHeaderInterceptor`/`AdminRateLimitFilter`/`RateLimitUtil` source (not assumed from `rate-limiting.md`'s example defaults) surfaced a previously-undocumented gap: `/api/v1/admin/**` (base path for most admin resource controllers — products, categories, tags, coupons, shipping-methods, search, orders, inventory, sales analytics) matches neither mechanism's literal `/api/admin/` prefix check, so those endpoints get only the 100/min default header and no dedicated admin-tier blocking, unlike the literal `/api/admin/**` groups (users, analytics, audit, webhooks, monitoring, thresholds, inventory-threshold/analytics/reports) which get a real 30/min header + 50/min block. SDD Document Control Version/Revision History bumped (3.3→3.4) per `[defect-class: sdlc-doc-version-never-bumped]`; RTM's `Related SDD` cross-reference updated to match (v3.3→v3.4, RTM's own Version bumped 1.5→1.6)
- RTM §6.10's header falsely claimed "the frontend is a stub; no implementation exists" and all 30 `FR-FE-*` rows were marked `🔵 Pending Ph-2`, citing fictional `.jsx` paths under directories that never existed — despite `frontend/src/` containing 71 real `.ts`/`.tsx` source files (#453, follow-up from #452's broader RTM verification sweep). Performed a genuine per-requirement audit rather than a path-only correction: for each of the 25 remaining rows (FR-FE-22/23/24/25/31 were already fixed by #450), read the requirement's actual text and checked whether the real frontend code satisfies it — not just whether a plausibly-named file exists. Result: 20 rows ✅ Implemented (paths corrected to their real `.tsx` files/components, e.g. `App.tsx` for both routing and JWT-refresh-adjacent client setup, `api/client.ts` for the fetch wrapper); 7 rows 🟡 Partial — genuine functional gaps found by reading the actual implementation, not the file's mere presence: form validation exists but not via the specified React Hook Form + Yup (neither is a dependency); the checkout flow creates a real backend Razorpay order and displays its ID but never invokes the client-side Razorpay `checkout.js` modal; registration has minimum-length validation but no visual password-strength indicator; and profile/order-history/wishlist/search are all real, working features delivered as `AccountPage`/`ProductListingPage` tabs and query-params rather than the standalone routes the RTM described; 4 rows 🔵 Pending — Toast notifications, Footer, and Breadcrumb components genuinely do not exist anywhere in `frontend/src/`. Recomputed the RTM's Frontend Coverage Summary row (4→20 Implemented, 0→7 Partial, 27→4 Pending) and the overall Coverage Summary Totals row from the corrected data. Recomputing §12's Phase 2 Frontend Started/Not-Started counts also surfaced and fixed a pre-existing, unrelated arithmetic error in that table's own row-total sum (the categories had always summed to 81, not the 80 the table previously stated) — a separate, mechanical off-by-one predating and independent of the Frontend staleness this issue targeted. Document Control Version/Revision History bumped (1.2→1.3) per `[defect-class: sdlc-doc-version-never-bumped]`
- SRS's `FR-FE-*` series had no requirement row for admin category management UI at all, despite #428 implementing it; RTM rows for `FR-FE-22`/`24`/`25` (admin dashboard/inventory/orders) still cited fictional `.jsx` paths (`AdminDashboard.jsx`, `AdminInventory.jsx`, `AdminOrderMgmt.jsx`) that never matched the real implementation, and were all marked "🔵 Pending Ph-2" despite 3 of the 4 already being live (#450). Added `FR-FE-31` (admin category management, tracing to #428) to both SRS §3.2.10.2 and the RTM; corrected the 3 stale RTM paths to the real components (`AdminDashboardPage.tsx`, `InventoryTab.tsx`, `OrdersTab.tsx`) and their status to ✅ Implemented — `FR-FE-23` (admin product management) confirmed still genuinely 🔵 Pending, since its real path doesn't exist yet (#425 open). Recomputed the RTM's Frontend category row and Coverage Summary Totals row from the corrected data rather than assuming (30→31 total, 0→4 implemented). Both docs' Document Control Version/Revision History bumped per `[defect-class: sdlc-doc-version-never-bumped]`. Checking the SDD's own §4.7.4 Frontend Route Design table for the same class of staleness surfaced a deeper, structural mismatch (the table models one route per admin feature; the real app uses a single `/admin` route with client-side tabs) — filed as a separate follow-up (#467) rather than fixed here, since it needs a full re-derivation, not a path substitution, matching the precedent set by #455/#456's SRS Appendix A treatment
- SRS Appendix A's API Endpoint Catalogue was extensively stale against the real controller mappings — wrong base-path prefixes throughout (`/api/v2/public/products` vs. actual `/api/v2/products`, `/api/admin/products` vs. actual `/api/v1/admin/products`, `/api/auth/refresh-token` vs. actual `/api/auth/refresh`, etc.) and entire endpoint groups never catalogued at all (categories, tags, coupons, shipping-methods, search reindex, inventory-threshold/analytics/reports, the public webhook receiver, product reviews, notifications SSE, auth validate-token/csrf) (#456, follow-up from #455's SRS verification sweep). Performed a full per-endpoint re-derivation directly from each controller's `@RequestMapping`/`@GetMapping`/etc. source rather than a prefix substitution, per the issue's own stated scope — 18 sections expanded to 36, each now citing its real controller class so a future drift check can grep the exact source file. Investigated, rather than assumed, which of `/api/checkout` (`CheckoutController`) vs. `/api/v1/checkout` (`MultiStepCheckoutController`) is current: neither is `@Deprecated`-annotated in code, but `frontend/src/api/checkout.ts` calls only `/api/v1/checkout` and `CheckoutController` predates `MultiStepCheckoutController` by one day (#76/CHK-01) — documented as legacy single-step (kept, not removed) vs. current multi-step, not silently dropped from the catalogue. SRS Document Control Version/Revision History bumped (4.2→4.3) per `[defect-class: sdlc-doc-version-never-bumped]`; RTM's own `Related SRS` cross-reference updated to match (v4.2→v4.3, its own Version bumped 1.3→1.4)
- SRS §4.2's Traceability Summary "Frontend" row and SDD's "React SPA design" traceability row both still cited `FR-FE-01–30`/count 30, never updated when #450 added `FR-FE-31` (#470, follow-up from #453's doc-enumeration pass). Corrected both to `FR-FE-01–31`/31. Recomputing the priority breakdown from all 31 rows' actual stated priorities (not incrementing the stale figure) found the real split is 16 High/12 Medium/3 Low — the previously-stated "15 High, 10 Medium, 5 Low" was already wrong even for the original 30, independent of FR-FE-31. Recomputed SRS's **Total Functional** row from the table's own row counts: 99, not the previously-stated 95, which also didn't match the sum of its own listed rows before this fix. Both docs' Document Control Version/Revision History bumped (SRS 4.3→4.4, SDD 3.2→3.3) per `[defect-class: sdlc-doc-version-never-bumped]`; RTM's `Related SRS`/`Related SDD` cross-references updated to match (v4.3→v4.4, and a long-stale v3.0→v3.3 that had drifted through 3 intervening SDD bumps), RTM's own Version bumped 1.4→1.5. Found but deliberately not fixed inline: 3 other rows in the same SRS §4.2 table (Authentication, Shopping Cart, Payment) have a duplicated priority label where a third distinct priority was clearly intended (e.g. "4 High, 1 Medium, 1 High") — filed as a separate follow-up (#474) since it's an unrelated pre-existing defect in the same table, not part of this issue's scope
- SRS §4.2's Authentication, Shopping Cart, and Payment rows each had a duplicated priority label from a copy-paste-shaped defect — e.g. Payment's "3 High, 1 Medium, 1 High" — found incidentally during #470 and filed separately as #474. Rather than guessing the intended third label (the issue itself suspected "Low"), recomputed each row directly from its §3.2 requirement priorities: Authentication is 9 High/2 Medium (not "8 High, 2 Medium, 1 Medium"), Shopping Cart is 5 High/1 Medium (not "4 High, 1 Medium, 1 High"), Payment is 4 High/1 Medium (not "3 High, 1 Medium, 1 High") — none of the three actually contains a Low-priority requirement, falsifying that initial guess. Checked every other row in the same §4.2 table for the same duplicate-label pattern; none found. SRS Document Control Version/Revision History bumped (4.4→4.5) per `[defect-class: sdlc-doc-version-never-bumped]`
- Required status checks (`Build & Test`, `Quality Gates`, `Unit Tests`, `Integration Tests & Coverage`) could never report a status on a PR touching only `docs/**`/`**/*.md`/`.github/ISSUE_TEMPLATE/**`, permanently blocking merge for those PRs since GitHub's required-checks mechanism does not treat "never reported" as satisfied (#412, surfaced while implementing #408's branch protection — see PR #411, resolved there only via `--admin` override). Root cause: those 4 checks live in `ci.yml`/`ci-cd-pipeline.yml`, both gated by a workflow-level `paths-ignore` (added in #336 to skip the ~30-50 min full pipeline on doc-only PRs) — when every changed file matches the ignore pattern, the whole workflow never triggers, so the required-check names never get a status at all, not even "skipped." Verified via GitHub's own troubleshooting docs and community discussions before implementing (not assumed): the two documented fixes are a "dummy/shim workflow" (a second workflow with matching job names, triggered by the inverse path filter) or moving path filtering inside an always-triggering workflow via job-level conditionals — presented both to the user via `AskUserQuestion` since a genuine tradeoff separates them (shim preserves #336's near-zero doc-only cost exactly but risks job-name drift between two files; inline conditional avoids duplication but costs a few seconds of CI on every doc-only PR). Chose the inline-conditional approach: added a `dorny/paths-filter`-based `detect-changes` job to both workflows and removed `paths-ignore` from their `pull_request` triggers (kept on `push`, which doesn't gate merge); every step doing real work is now gated on `needs.detect-changes.outputs.code-changed == 'true'` rather than the job itself being skipped, so the 4 required-check jobs always complete and report success/skipped quickly on doc-only PRs while a mixed docs+code PR still runs the full real pipeline (no regression on #336's guarantee). `OWASP Dependency-Check` (not a required check) is skipped at the job level instead, preserving its CI-minutes savings. Verified empirically with a real throwaway docs-only PR (#416, based on this fix) and this issue's own PR (#414, a real workflow-file change) run side by side
- `Check Dependencies` CI job's Node Audit Analyzer (OWASP Dependency-Check) failed identically on the last several runs — both PR #404 and the immediate post-merge push to `master` — with "Failed to read results from the NPM Audit API" (#405). Root-caused via web research (not fixable from this repo's CI config): npm retired its legacy audit endpoints (`registry.npmjs.org/-/npm/v1/security/audits` and `/quick`) in ~April 2026, now returning HTTP 410 "This endpoint is being retired. Use the bulk advisory endpoint instead." — an upstream DependencyCheck limitation (tracked e.g. `dependency-check/DependencyCheck#8421`/`#8423`, migration to the new bulk API), not a transient network issue. Added `--disableNodeAudit` to the `dependency-check` job's args in `security.yml`, mirroring how the Sonatype OSS Index analyzer is already disabled non-fatally in the same job for the same "unreachable dependency" reason, rather than letting every run fail-then-self-disable at runtime. RetireJS and the NVD CPE analyzer remain active, still covering `frontend/package-lock.json`
- Configured branch protection on `master` for the first time (#408, follow-up from #359/#316/#318). Previously `gh api repos/.../branches/master/protection` returned 404 — nothing technically prevented merging a PR with red CI checks, force-pushing, or deleting `master` outright, and two prior sessions (#359, #316/#318) each had to judge merging past known-red checks safe via ad hoc investigation rather than a standing policy. Configured `required_status_checks` on 6 genuinely-blocking, always-running checks verified directly against the GitHub Checks API on a real recent commit rather than assumed from workflow file names: `Build & Test`/`Quality Gates` (JaCoCo coverage, `ci.yml`/"Quality Gate Pipeline"), `Unit Tests`/`Integration Tests & Coverage` (the latter is where PIT mutation testing actually runs — confirmed via `pom.xml`'s `mutationCoverage` goal binding to the `verify` phase, not assumed from the job name — `ci-cd-pipeline.yml`/"Full Test Matrix & Docker Publish"), and `Code Quality Analysis`/`Security Vulnerabilities` (CheckStyle/SpotBugs/PMD/SonarCloud, and a Java-only OWASP CVSS≥7 gate unaffected by the npm-specific flake, `security.yml`). Deliberately excluded `Check Dependencies` (the pre-existing, still-open #405 NPM Audit API flake — would have permanently blocked every future merge) and `Load Tests`/`Stress Tests`/`Build & Push Docker Image` (confirmed these report `skipping`, not `success`, on every normal push — GitHub's required-checks mechanism does not treat "skipped" as satisfying the requirement, so including them would have blocked all future merges too). `enforce_admins=false` and no required PR review count (`required_pull_request_reviews: null`) — both deliberate choices, put to the user via `AskUserQuestion` rather than decided unilaterally, to preserve this repo's already-established self-merge-with-judgment convention (`git-workflow.md`) that #359 and #316/#318 both actually relied on; `enforce_admins=true` would have made both of those real, already-completed merges impossible without first disabling protection. `strict` (require-branches-up-to-date) left off, matching this repo's actual one-issue-at-a-time merge pattern. **Verified live, satisfying the issue's own AC #4**: opened a throwaway PR (#410) with a deliberate CheckStyle violation, confirmed `Code Quality Analysis` and (as a real, explained side effect — an untested new class tripping JaCoCo's per-package 85% instruction-coverage rule) `Integration Tests & Coverage` both genuinely failed, and `gh pr merge --squash` was refused by GitHub itself (`the base branch policy prohibits the merge`) — not just a red check in the UI. Also confirmed `enforce_admins=false` was applied correctly: the same refusal message offered a `--admin` override flag. PR closed without merging, throwaway branch deleted
- Deleted `.github/workflows/ci-cd.yml` and renamed the two remaining CI/CD-shaped workflow files for clarity (#407, follow-up from #318). This repo had three near-identically-named workflow files (`ci.yml` "CI/CD Pipeline", `ci-cd.yml` "BuildNest CI/CD Pipeline", `ci-cd-pipeline.yml` "BuildNest E-Commerce CI/CD Pipeline") with overlapping-but-not-identical job sets — exactly the confusion that let #318's PMD gate land in `ci-cd.yml`, a file whose trigger (`branches: [main, develop]`) never matched this repo's real branch (`master`) and so never ran. Investigation confirmed `ci-cd.yml` was entirely dead weight, not just mis-triggered: its `docker-build-push` job never actually published (`push: false`, already superseded by `ci-cd-pipeline.yml`'s real `push: true` build), and its `deploy-dev`/`security-scan`/`coverage` jobs were all non-blocking stubs (`continue-on-error: true`, `\|\| true`) fully superseded by the real, blocking gates already live in `ci.yml`/`security.yml`; PMD itself had already been migrated out in #406. Deleted rather than fixed, confirmed via `deploy.yml`'s trigger scoping that nothing depends on it. `ci.yml` and `ci-cd-pipeline.yml` turned out to **not** be true duplicates despite both superficially "building/testing" — they run different Maven profiles for genuinely different purposes (fast JaCoCo-gated quality path vs. the full unit/integration/PIT-mutation/reliability/load/stress/e2e matrix + real Docker publish) — so both were kept, renamed to "Quality Gate Pipeline" and "Full Test Matrix & Docker Publish" respectively to remove the naming confusion itself rather than merging genuinely distinct pipelines. Critically, `deploy.yml`'s `workflow_run` trigger matches by workflow *display name* (not filename) — updated its `workflows: [...]` reference to `"Quality Gate Pipeline"` in the same commit as `ci.yml`'s rename, since an unsynced rename here would have silently broken deployment the exact same way `ci-cd.yml`'s branch mismatch silently broke PMD. Corrected a pre-existing factual error found in passing in `push-vs-pr-vs-ci-and-batch-size.md`, which had `deploy.yml` gated on `ci-cd-pipeline.yml` — it was always actually gated on `ci.yml`
- PMD's blocking CI gate (#318, sub-issue of #316) had never actually executed since it was added — the check lived only in `ci-cd.yml`, whose trigger is `branches: [main, develop]`, and this repo's real branch is `master`; `gh run list --workflow="BuildNest CI/CD Pipeline"` confirmed zero run history. Discovered while verifying #316's own closure premise against fresh CI execution evidence rather than trusting the closed-issue label — CheckStyle (#319), SpotBugs (#317), and SonarCloud (#320) were all independently confirmed live via the same check, isolating this to PMD/`ci-cd.yml` specifically. Relocated `pmd:check -Dpmd.maxAllowedViolations=48` into `security.yml`'s "Code Quality Analysis" job, proven to actually trigger on `master` alongside the other three tools; removed the dead step from `ci-cd.yml` rather than leaving stale duplicate logic behind (that file's broader 3-way overlap with `ci.yml`/`ci-cd-pipeline.yml` is tracked as a separate follow-up). Running the gate for the first time on real code surfaced a genuine net-new violation introduced by #359 (`SecurityConfig`'s fully-qualified `@Value` annotation, `UnnecessaryFullyQualifiedName`) that had silently slipped through while the gate was dead — fixed at root cause rather than raising the baseline, confirming `-Dpmd.maxAllowedViolations=48` passes cleanly again. #318 reopened and will be re-closed once verified via a fresh post-merge run on `master`, not just this PR's own green check
- Prometheus's `buildnest` scrape job had no credentials for `/actuator/prometheus`, which is `ROLE_ADMIN`-protected per `spring-security.md` — confirmed live (`curl` against a running instance returned `401 Unauthorized`) before implementing, so the target could never have scraped successfully even with the full observability stack running (#359). Researched current Spring Boot Actuator security guidance before choosing an approach (both a dedicated Basic Auth monitoring user and an isolated `management.server.port` are legitimate 2026 patterns); picked the Basic Auth credential for its smaller blast radius — no change to the existing actuator port/topology or Docker healthchecks. Added a new, narrowly-scoped `@Order(0)` `SecurityFilterChain` (`SecurityConfig#actuatorMonitoringSecurityFilterChain`) matched only to `/actuator/prometheus`, authenticated via an explicit `ProviderManager` wrapping an `InMemoryUserDetailsManager` for a single dedicated `monitoring` identity — deliberately never the ADMIN login account, and never JWT (Prometheus is a machine scraper, not a browser session). Every other `/actuator/**` path is untouched, still governed by the main chain's existing `hasRole(ADMIN)` rule. Caught and fixed a real bug before it shipped: an initial `.userDetailsService()`/`.authenticationProvider()` call on the new chain's `HttpSecurity` silently leaked into the app's shared, DB-backed `AuthenticationManagerBuilder` — the monitoring Basic Auth request resolved via `CustomUserDetailsService` instead of the intended in-memory identity, confirmed via a live log line (`Loading user by username: monitoring` from the real user-account service) before being fixed with an explicit, chain-local `ProviderManager`. Credential sourced via `MONITORING_USERNAME`/`MONITORING_PASSWORD` (`.env.example`, `application.properties`), wired into `prometheus.yml.template`'s `basic_auth` block via the existing `prometheus-config-render` sidecar (#324's substitution mechanism, extended with two more placeholders). `SecurityConfig` fails fast at startup in the `production` profile if `MONITORING_PASSWORD` is left at its local-dev default, mirroring `JwtKeyValidator`'s existing pattern for `JWT_SECRET`. New `ActuatorMonitoringSecurityTest` (4 tests: unauthenticated rejected, correct credential clears the security layer, wrong credential rejected, other `/actuator/**` paths still require ADMIN not the monitoring credential) — a Mockito-mocked unit test cannot observe this, since it's filter-chain/proxy-level behavior. `TestSecurityConfig` (the hand-maintained stand-in for `SecurityConfig`, `@Profile("!test")`) updated in lockstep to avoid the drift #312 already found once between the two. PR #404's own CI caught two real gaps: CheckStyle's ratchet (`maxAllowedViolations=8305`, whole-file not diff) tripped from 8304 to 8306 on the new code — fixed with proper javadoc/line-length/final-parameter compliance, landing exactly at the ceiling; CodeQL and SonarCloud (`java:S4502`) both flagged the new chain's `.csrf(csrf -> csrf.disable())` as CRITICAL/high — confirmed with the user this is a false positive (the chain is `STATELESS` and Basic-Auth-only, no cookie is ever set or read, and its only client is a machine scraper, not a browser session), documented inline rather than silently dismissed
- SonarCloud analysis, confirmed already running on `master` (not skipped — `SONAR_TOKEN` correctly picked up since #350), now genuinely gates CI on its quality gate result instead of only ever exiting 0 (#320, sub-issue of #316). Added `sonar.qualitygate.wait=true` to the `sonar:sonar` goal invocation in `security.yml` — without it, the previous `ANALYSIS SUCCESSFUL` log line only meant the report was submitted to SonarCloud's background compute engine, never that the gate was evaluated or waited on, so the step could never have failed on a real quality-gate violation regardless of the `|| echo` fallback. Verified the `buildnest-ecommerce` project uses SonarCloud's org-default "Sonar way" gate (no new bugs/vulnerabilities, Maintainability rating A, 100% new hotspots reviewed, ≥80% new coverage, ≤3% duplication on New Code) — functionally equivalent to "fail on new Critical/Blocker," since any such finding drops the rating below A. The `|| echo` fallback is now scoped only to a genuine "not configured" case (empty `SONAR_TOKEN`, e.g. a fork PR without secret access); when the token is present, a real scanner error or a failed quality gate now fails the step for real. Added `if: always()` to the following `Run CheckStyle` step — per #354's already-documented step-skip gotcha, a step with no `if:` condition is silently skipped once a preceding step in the same job fails, which would otherwise have applied one step earlier than before now that Sonar can genuinely fail
- PMD graduated from fully non-blocking (`./mvnw pmd:check -q || true`) to blocking in CI, baseline-ratcheted (#318, sub-issue of #316) — mirrors CheckStyle's #354 pattern rather than SpotBugs's #317 severity-threshold pattern, since PMD's 48 current violations are entirely priority 3/4 (10/38) with zero priority-1/2 findings to gate a threshold on. `maven-pmd-plugin`'s native `maxAllowedViolations`/`failurePriority` parameters used directly on the CLI (`-Dpmd.maxAllowedViolations=48`) — no `pom.xml` plugin declaration needed, since `pmd` resolves via Maven's default plugin-prefix group the same way `checkstyle` does (unlike SpotBugs's `com.github.spotbugs`, which required an explicit `pom.xml` declaration in #353). Verified the ratchet genuinely blocks: `-Dpmd.maxAllowedViolations=47` fails the build (exit 1) against the same 48-violation codebase, `=48` passes (exit 0). PMD is the last step in its CI job, so no `if: always()` next-step-skip risk applies (the gotcha #354 caught for CheckStyle→SpotBugs sequencing in `security.yml` doesn't recur here)
- SpotBugs graduated from fully non-blocking to blocking on Priority-1/High findings in CI (#317, sub-issue of #316). Fixed the 4 pre-existing High-priority findings so the blocking threshold starts at zero: `AuditAspect.auditMethod` called `toString()` on `oldValue`, an `Object` field that only ever holds `joinPoint.getArgs()` (an `Object[]`) — produced array-identity strings (`[Ljava.lang.Object;@...`) instead of actual argument values in audit/Elasticsearch logs, now uses `Arrays.toString()`; `PerformanceMonitoringService.slowRequestCount`/`totalRequestCount` were `volatile long` fields incremented with `++` under concurrent request load — a non-atomic read-modify-write race that silently undercounts, switched to `AtomicLong`; `ShippingServiceImpl.resolveZoneMultiplier` computed `Math.abs(prefix.hashCode()) % multipliers.size()` — `Math.abs(Integer.MIN_VALUE)` stays negative (two's-complement overflow), risking an `IndexOutOfBoundsException` on `List.get()`, switched to `Math.floorMod()`. `pom.xml`'s spotbugs-maven-plugin now sets `threshold=High`/`effort=Max`/`failOnError=true`; `security.yml`'s SpotBugs step lost its `|| echo` fallback. Priority-2/Medium findings (97, mostly `EI_EXPOSE_REP`/`EI_EXPOSE_REP2` and `VA_FORMAT_STRING_USES_NEWLINE`) remain advisory, tracked separately per #316's other sub-issues (#318 PMD, #319 CheckStyle, #320 SonarCloud). Verified the gate actually blocks by temporarily reintroducing the `ShippingServiceImpl` bug, confirming `spotbugs:check` failed the build, then reverting. PR #399's own explanatory comment initially pushed CheckStyle from 8305 to 8306 violations (2 new `LineLength` hits) — shortened it to fit, landing at 8304, one below baseline
- Three config bugs in #122's Grafana/Alertmanager/exporter stack, found only by actually running `docker compose up` (the earlier PR had validated YAML/JSON syntax and simulated the render scripts standalone, since Docker wasn't available in that session, but never started the real containers): (1) `node-exporter`'s `/:/host:ro,rslave` bind mount failed outright under Docker Desktop's WSL2 backend ("path / is mounted on / but it is not a shared mount") — `rslave` propagation isn't needed for read-only disk-usage stats, switched to plain `:ro`; (2) `alertmanager-config-render`'s multi-line `command: >` folded-scalar script silently mis-tokenized into 8+ separate shell arguments (YAML's more-indented-continuation-line folding rules interacting badly with escaped inner quotes) — rewrote as an unambiguous YAML list + literal block script; (3) `mysqld_exporter:v0.15.1` has fully removed `DATA_SOURCE_NAME` env var support in favor of a mounted `--config.my-cnf` file — added a `mysqld-exporter-config-render` sidecar (mirroring `prometheus-config-render`, #324) to render `mysqld-exporter/my.cnf.template` from `MYSQL_ROOT_PASSWORD`. Live-verified end-to-end after all three fixes: Prometheus shows `mysql`/`redis`/`node` targets `up`, Grafana's MySQL/Redis dashboards render real live values (`mysql_up=1`, `redis_connected_clients=1`) through its provisioned datasource, and a real test alert fired through Alertmanager was delivered to Mailpit as `[FIRING:1]` with correct labels/from/to within the configured `group_wait`
- `ProductRepository.advancedSearch`'s JPQL correctly joins on `p.tags` now, but discovered a separate, pre-existing latent bug while adding a repository-level test: the `inStock` clause's implicit `p.inventory.quantityInStock` path navigation silently excludes any product with no `Inventory` row from every `advancedSearch` result, regardless of the `inStock` parameter's value (including `null`, which should not filter on stock at all). Not fixed here — filed as a separate follow-up (#365) since it's unrelated to PROD-03's scope; worked around locally in the new test by giving every test product an `Inventory` row, matching production reality where `Product`→`Inventory` is always created together
- SDD's 6 frontend-design sections (§1.2 Scope, §4.1.1 context diagram, §4.2.2 package structure, §4.3.6 component model, §4.5.5 state model, §4.7.4 route design, §4.10.5 deployment) still framed the entire frontend as unbuilt "Design Intent — Phase 2," despite `frontend/src/` containing 71 real source files (#459, third document carrying this false claim after RTM's #453 and SRS Appendix A's #456). Performed a full re-derivation against real source rather than a relabel: §4.2.2 now describes the actual `api/`/`components/`/`contexts/`/`hooks/`/`pages/` layout (no `core/`/`auth/`/`router/`/`services/`); §4.3.6 describes the real `useAsync` generic-fetch hook, the single `RequireAuth` guard (optional `role` prop, not two separate components), and Tailwind-only styling with no component library; §4.5.5 corrects the state model to `AuthContext` (the only Context) plus hook-local state for cart/server data — no `CartContext`, no Redux/React Query/SWR; §4.7.4 rewrites the route table to match `App.tsx`'s real `react-router-dom` v7 routes and the RTM's FR-FE mapping, including the tabs-not-routes structure of `AccountPage`/`AdminDashboardPage` already documented in #453/#467; §4.10.5 verified against the real `frontend/Dockerfile`/`nginx.conf` (`nginx-unprivileged`, healthcheck, real cache-control directives) rather than a generic "Nginx Alpine" description. Document Control Version/Revision History bumped (3.1→3.2) per `[defect-class: sdlc-doc-version-never-bumped]`

### Added
- `ProductTag` entity (many-to-many with `Product` via `product_tag_map`, slug auto-derived from name) and admin CRUD at `/api/v1/admin/tags` (`AdminProductTagController`, `ProductTagService`/`ProductTagServiceImpl`, ROLE_ADMIN) (PROD-03, #83). `GET /api/v2/products/search?tag=` filters by tag name (JPA path via a correlated `EXISTS` subquery — a plain `LEFT JOIN p.tags` combined with the existing `@EntityGraph` fetch of `variants` produced an incorrect Cartesian/DISTINCT interaction that silently dropped matching rows; ES path via `ProductDocument.tags` + `ProductElasticsearchRepository.findByTagsAndIsActiveTrue`, with in-document filtering when combined with a text query, mirroring the existing price/stock filter pattern). The issue's acceptance criterion named the filter endpoint `GET /api/v1/products?tag=`, but `ProductControllerV1` is `@Deprecated(forRemoval = true)`; the filter was added to `ProductControllerV2` (the actively maintained endpoint) instead, per explicit confirmation. Also fixes a previously-latent broken changeset (`20260624-004-create-product-tags.xml`): same class of bug as #81/#82 — `fk_tag_map_product` referenced the legacy singular `product` table instead of `products`, silently resolving against a dead legacy table rather than failing outright; corrected via a new changeset (`20260712-020`) rather than editing the already-applied one, with the same `tableExists` guard this repo's other `products`-touching changesets use
- `AdminProductTagControllerIntegrationTest` (10 tests), `AdminProductTagControllerTest` (unit tests for its 500/400/404 branches), `ProductTagServiceImplTest`, `ProductTagRepositoryTest`-equivalent coverage via `ProductRepositoryTest.testAdvancedSearchFiltersByTag`, `ProductTagTest`, `ProductSearchServiceTest` +3 tag-filter tests (#83). Removed a genuinely unreachable `catch (Exception e)` in `AdminProductTagController.updateTag`/`deleteTag` — `ProductTagService`'s interface declares no checked exceptions, so that branch could never execute after the preceding `catch (RuntimeException e)`
- `ProductTagServiceImpl.toSlug`'s trailing-hyphen-stripping regex (`^-|-$`) had ambiguous operator precedence between alternation and anchors (SonarCloud `java:S5850`); made explicit as `(^-)|(-$)`
- `package-info.java` added to every package touched by #83 that lacked one (`controller/admin`, `controller/user`, `model/dto`, `model/entity`, `model/elasticsearch`, `repository`, `repository/elasticsearch`, `service/product`, `service/producttag`) — a cheap, repo-wide reduction in CheckStyle's `JavadocPackage` violation count, done to make room under the CI CheckStyle ceiling for #83's new files
- Grafana dashboards (application, JVM, MySQL, Redis) and Alertmanager-routed alerting (OPS-04, #122). `grafana` service added to `docker-compose.yml` with datasource + dashboard auto-provisioning (`grafana/provisioning/`) reading from JSON committed to `docs/monitoring/dashboards/`. `alertmanager` service added, wired to the `alertmanager:9093` target `prometheus.yml.template` already referenced but that pointed nowhere until now; notification channel (Slack webhook and/or SMTP email) is configurable via `ALERTMANAGER_SLACK_ENABLED`/`ALERTMANAGER_EMAIL_ENABLED` and rendered by a `sed`-based `alertmanager-config-render` sidecar mirroring the existing `prometheus-config-render` pattern (#324). `alerts.yml` thresholds aligned to the AC's exact figures: P95 latency > 500ms (was > 1s), heap memory > 85% (was > 80%); error rate > 1% and disk > 80% already matched. Also re-added `mysqld-exporter`, `redis-exporter`, and `node-exporter` plus their Prometheus scrape jobs — deliberately removed in #325 for having no consumer, but #122's MySQL/Redis dashboards and the pre-existing `HighDiskUsage` rule are exactly the "real metrics tier" that decision said to wait for
- `POST /api/v1/checkout/coupon` — apply a coupon/discount code to the active multi-step checkout session (CHK-02, #77). New `Coupon` entity (`PERCENTAGE`/`FIXED` discount types, optional `usageLimit`/`expiresAt`, `minOrderValue`) + `CouponRepository`, `CouponService`/`CouponServiceImpl` (validation order: exists → active → not expired → under usage limit → subtotal meets minimum; discount capped at the cart subtotal for both discount types). Coupon can only be applied while the checkout session is at `PENDING_SHIPPING` or `PENDING_PAYMENT` — rejected with 409 once payment has been initiated (a deliberate scope decision, not an oversight). `CheckoutSession`/`CheckoutSessionDTO` extended with `couponId`/`couponCode`/`discountAmount`; `CheckoutServiceImpl.buildOrderFromCart` applies the discount before tax (`Order.discountAmount`, previously an unused field). Usage-limit consumption happens at `confirmCheckout` (final order commit), not at apply-time — a deliberate trade-off accepting a small race-window rather than adding pessimistic locking the acceptance criteria didn't call for. `POST /api/v1/admin/coupons` (create) and `DELETE /api/v1/admin/coupons/{id}` (deactivate) on a new `AdminCouponController`, `@PreAuthorize("hasRole('ADMIN')")`. Also fixes a previously-latent gap: `GlobalExceptionHandler` had no handler for `ValidationException` — it had never actually been thrown anywhere in the codebase before `CouponServiceImpl`, so failures would have surfaced as 500 instead of 400
- `CouponServiceImplTest` (20 tests) — full validation-rule matrix, discount calculation for both types (including subtotal-capping), usage increment, create/deactivate. `CheckoutServiceImplTest` +15 tests — `applyCoupon` at each checkout step, discount applied to order totals, usage increment wired into `confirmCheckout`. `AdminCouponControllerTest` (4 tests), `MultiStepCheckoutControllerTest` (1 test, first test file for this controller) (#77)
- `GET /api/admin/analytics/behaviour` — user behaviour analytics: page views per product, cart abandonment rate, and conversion-funnel stage counts (ANL-02, #65). New `UserBehaviorEvent` Elasticsearch document + `UserBehaviorEventRepository`, and `UserEventService`/`UserEventServiceImpl` (domain-owned direct repository access wrapped in the shared `elasticsearchCircuitBreaker`, mirroring `ProductSearchServiceImpl`'s pattern rather than routing through `ElasticsearchIngestionService` — a distinct domain with its own aggregation queries). Async, fire-and-forget event recording wired into `ProductControllerV2.getProduct` (`PRODUCT_VIEW`, tolerates anonymous/unauthenticated views), `CartServiceImpl.addToCart` (`ADD_TO_CART`), and `CheckoutServiceImpl.setAddress` — checkout step 1/4 of the multi-step flow (`CHECKOUT_STARTED`). The issue's stated endpoint path (`/api/v1/admin/analytics/behaviour`) was adapted to `/api/admin/analytics/behaviour` to match every other route already on the same `AdminAnalyticsController`. Cart-abandonment/conversion-funnel metrics are a simplified, session-independent approximation (aggregate stage counts and inter-stage rates within a time window, not per-cart/per-session tracing) — a deliberate scope boundary, not an oversight. `UserEventService` is injected as `Optional<UserEventService>` everywhere it's consumed outside `AdminAnalyticsController` (itself already ES-gated), since the service is genuinely Elasticsearch-only and those callers are not — the same dependency-graph care applied in #345
- `GET /api/user/notifications/stream` — in-app real-time notifications via Server-Sent Events (NOTIF-02, #63). `SseNotificationService`/`SseNotificationServiceImpl` maintains a per-user registry of live `SseEmitter` connections (a user may have more than one open, e.g. multiple tabs); connections are secured by the existing JWT-cookie auth (`hasRole('USER')`, inherited automatically from the `/api/user/**` security rule — the issue's originally-stated path `/api/v1/users/notifications/stream` didn't match this repo's controller convention and would have fallen through to the looser `anyRequest().authenticated()` catch-all, so it was adapted to `/api/user/notifications/stream` instead). `OrderStatusChangedEvent` extended with a `userId` field (previously only carried `orderId`/`previousStatus`/`newStatus`) so a new, deliberately separate `OrderStatusSseListener` can route the event to the right user's connection(s) — kept independent of `DomainEventListener`'s existing `elasticsearch.enabled` gate, since in-app notifications are a user-facing feature, not observability infrastructure, and must work regardless of that flag (#63)
- `SseNotificationServiceImplTest` (14 tests), `NotificationControllerTest` (1 test), `OrderStatusSseListenerTest` (1 test), `OrderServiceImplTest` +1 test verifying the published event carries the order owner's `userId`. Tests assert on the actual SSE content written to the connection (via `MockMvc` + `result.getResponse().getContentAsString()`), not just registry structure — including a real onCompletion cleanup path (a bare `new SseEmitter().complete()` is a no-op outside a real servlet async request; Spring only wires the completion callback through the async context once `initialize()`/`asyncDispatch` have run) and the onTimeout/onError callback bodies plus the initial-handshake-failure branch, exercised via a package-private `register(userId, emitter)` test seam on `SseNotificationServiceImpl` since none of those three are reachable by driving a real request alone. `SseNotificationServiceImpl` is 100% line-covered (#63)
- `ProductVariant` entity (size/colour/price adjustment), linked to `Product` and to a per-variant `Inventory` row (`Inventory.variant_id`, nullable alongside the existing `product_id`, so variants reuse the existing reservation/threshold/low-stock logic instead of duplicating it). `CartItem.variant` (nullable) lets cart items pin to a specific variant; `CartService.addToCart(userId, productId, variantId, quantity)` overload resolves variant-specific pricing (`ProductVariant.getEffectivePrice()`). Admin CRUD nested under `AdminProductController` at `/api/v1/admin/products/{id}/variants` (`ProductVariantService`/`ProductVariantServiceImpl`, `CreateProductVariantRequest`, `UpdateProductVariantRequest` — a separate DTO from create, since update never touches inventory). Also fixes a pre-existing broken changeset (`20260624-002-create-product-variants.xml`): its FK referenced the legacy singular `product` table instead of the real `products` table, and lacked the `tableExists` guard this repo's other `products`-touching changesets use — it was dead on arrival (PROD-01, #81)
- `ProductVariantServiceImplTest` (10 tests), `AdminProductVariantControllerIntegrationTest` (8 tests) — CRUD, inventory creation, duplicate-SKU rejection, ownership validation (#81)
- `ProductImage` entity (imageUrl/altText/displayOrder/isPrimary) for a per-product image gallery. `POST /api/v1/admin/products/{id}/images` — previously a single-image *replace* endpoint (setting `Product.imageUrl` directly) — now *appends* to the gallery instead; the first image uploaded becomes primary, and `Product.imageUrl` is kept in sync with whichever image is primary (updated on upload and on deleting the primary image), preserving backward compatibility for existing consumers of that field. New `PATCH /api/v1/admin/products/{id}/images/reorder` (accepts the full ordered list of the product's current image IDs — rejects any request that doesn't contain exactly that set) and `DELETE /api/v1/admin/products/{id}/images/{imageId}` (deleting the primary image promotes the next image in display order; deleting the last image clears `Product.imageUrl`). `ProductImageService`/`ProductImageServiceImpl` reuse the existing `StorageService` abstraction. Also fixes a pre-existing broken changeset (`20260624-003-create-product-images.xml`): same class of bug as #81's fix — FK referenced the legacy singular `product` table and lacked the `tableExists` guard (PROD-02, #82)
- `ProductImageServiceImplTest` (12 tests), `AdminProductImageControllerIntegrationTest` (7 tests) — upload/primary-assignment, reorder validation (exact-set matching), delete with primary-promotion and gallery-empty cases (#82)
- `eslint-plugin-react`'s `react/button-has-type` rule, set to `error` — prevents regressing #299 (an un-typed `<button>` inside a `<form>` defaults to `type="submit"` and triggers unintended form submission). **Tooling caveat**: this plugin's latest release (7.37.5) doesn't yet list `eslint@^10` (this project's version) in its peer range, so installing it requires `--legacy-peer-deps`; a plain `npm install`/`npm ci` in `frontend/` will fail with an ERESOLVE error until upstream publishes ESLint 10 support. Documented in a comment at the top of `eslint.config.js`. Confirmed with the user before proceeding, given the tradeoff (no CI risk today — no workflow installs/tests the frontend yet — but real friction for a future contributor's first `npm install`) (#299)
- `hooks/useAsync.ts` — generalizes the `data`/`loading`/`error` + cancellation-flag fetch-in-`useEffect` pattern that was hand-written across ~15 hooks and components. `deps` behaves like a `useEffect` dependency array; `reload()` re-runs the fetcher on demand (e.g. a Retry button); `setData()` is an escape hatch for optimistic local mutations (e.g. removing an item from a list after a delete, without a full re-fetch) (#298)
- `useAsync.test.ts` — 8 tests: loading → success, error message surfaced on rejection, generic fallback for a non-`Error` rejection, re-runs on a dependency change, does not re-run on an unrelated re-render, `reload()` re-runs on demand, `setData()` updates without re-fetching, a stale response after unmount does not throw (#298)
- `model/payload/ChangePasswordRequest.java` — validated request body (`oldPassword`, `newPassword`) for the password-change endpoint (#249)
- `PasswordResetControllerTest` +1 test: `changePasswordShouldDeriveUserIdFromAuthenticatedPrincipalNotClientInput` — regression guard proving the endpoint uses the authenticated principal's id, not a client-supplied one (#249)
- `frontend/src/api/user.test.ts` (new) — 5 tests: request sends credentials in a JSON body (not query params), URL contains no password text, resolves on success, surfaces the backend's error message on a wrong old password, generic fallback when the backend gives no message (#249)
- `SecurityTab.test.tsx` (new) — 6 tests: calls `changePassword` with only token + passwords (no `userId`), shows a success confirmation, shows the backend's error message on a wrong password, client-side new-password-length validation, password-mismatch validation, fields clear after a successful change (#249)
- `components/product/ProductCard.tsx`: quick "Add to Cart" button (quantity 1) — hidden (in favor of an "Out of stock" label) when `stockQuantity` is 0; on click, `preventDefault`/`stopPropagation` stop the card's wrapping `<Link>` from navigating; shows "Adding…" while in flight, "Added ✓" (green) for 2s on success, "Failed — retry" (red) for 2s on error, or "Sign in to add" for 2s when the user isn't authenticated (no navigation to `/login` — same inline-prompt convention as `ProductDetailPage`'s `handleAddToCart`). The issue as filed stated the button should call `POST /api/user/cart/items`; the real, already-implemented endpoint is `POST /api/user/cart/add?userId=` (`api/cart.ts`'s existing `addToCart`) — used that instead of introducing a second, non-existent endpoint (#248)
- `ProductCard.test.tsx` — 7 tests: button shown when in stock, hidden with out-of-stock label at zero stock, sign-in prompt (no API call) when unauthenticated, `addToCart` called with quantity 1 and success feedback shown when authenticated, error feedback on a rejected call, feedback reverts to idle after the 2s window, clicking the button does not navigate to the product detail page (#248)
- `components/common/ErrorBoundary.tsx` — top-level React error boundary (class component; error boundaries have no hook equivalent). Catches any unhandled render-time exception, logs it via `console.error` (the natural seam for wiring a real error-reporting service later), and renders a fallback UI with a "Reload page" button instead of unmounting the whole tree to a blank screen. Wrapped around `<Routes>` (not `<App />`) in `App.tsx`, so the `Navbar` stays mounted and navigation still works even if one page's content throws (#300)
- `ErrorBoundary.test.tsx` — 4 tests: renders children normally, renders the fallback instead of unmounting when a child throws, logs the caught error, reload button calls `window.location.reload()` (#300)
- Backend address-book CRUD (#247) — the issue as filed listed `GET`/`PUT`/`DELETE`/`PUT .../default` as if they already existed; `AddressController` was actually create-only (its own doc comment said so). Implemented the missing endpoints:
  - `AddressService`/`AddressServiceImpl`: `getAddresses` (default-first, then by id), `updateAddress` (ownership-checked, does not touch `isDefault`), `deleteAddress` (ownership-checked; if the deleted address was the default, promotes the lowest-id remaining address to default, mirroring the existing "first address is default" convention), `setDefaultAddress` (ownership-checked; clears the previous default, sets the new one)
  - `AddressController`: `GET /api/user/addresses`, `PUT /api/user/addresses/{id}`, `DELETE /api/user/addresses/{id}`, `PUT /api/user/addresses/{id}/default` — all `@PreAuthorize("hasRole('USER')")` (class-level, already present)
  - `UpdateAddressRequest` payload (same validated shape as `CreateAddressRequest`)
  - Ownership enforcement follows the existing `CartServiceImpl` convention: `ResourceNotFoundException` if the address doesn't exist, `AccessDeniedException` if it exists but belongs to a different user
  - No Liquibase changes needed — the `addresses` table already had every column required (`is_default`, `address_type`, etc.) from the original #78/#104 migration
  - `AddressServiceImplTest` +15 tests, `AddressControllerTest` +4 tests, covering all four new methods, ownership checks, and the default-address-reassignment-on-delete behavior
- `frontend/src/api/addresses.ts`: added `fetchAddresses`, `updateAddress`, `deleteAddress`, `setDefaultAddress` — all routed through the shared client (#295) (#247)
- `addresses.test.ts` — 10 tests covering success and non-2xx paths for all four new functions (#247)
- `components/account/AddressesTab.tsx` — replaced the "coming soon" placeholder with a working address book: fetches and lists saved addresses on mount (default shown first with a badge), an "Add address" form, per-address "Set as default" and "Delete" actions, loading skeleton, empty state, and an error state with retry (#247)
- `AddressesTab.test.tsx` — 6 tests: list render, empty state, error-then-retry, add-address-then-reload, delete-then-reload, set-default-then-reload (#247)
- `index.css`: `@theme` block defining a `primary-*` color scale (50–900) aliased to Tailwind's built-in indigo palette via `var(--color-indigo-*)`. This is the one place a future brand-color change would touch — every component now uses `primary-*` utilities instead of hardcoding `indigo-*` or `amber-*` directly (#245)
- `pages/ForgotPasswordPage.tsx` and `pages/ResetPasswordPage.tsx` — the login page's "Forgot password?" link previously pointed at a route that rendered a blank page; the backend (`PasswordResetController`) already fully supported the flow, only the frontend was missing. `ForgotPasswordPage` shows the same generic "if that email exists…" success message regardless of whether the account exists, matching the backend's own non-revealing behavior. `ResetPasswordPage` reads `?token=` from the URL and shows a clear "request a new link" fallback when it's missing. `api/auth.ts`'s `apiForgotPassword`/`apiResetPassword` send their params as a URL query string rather than a JSON body — `PasswordResetController.forgotPassword`/`resetPassword` read `@RequestParam`, not `@RequestBody`, unlike every other endpoint this API client talks to (#308)
- `pages/NotFoundPage.tsx` + a catch-all `<Route path="*">` in `App.tsx` — the app previously had no 404 page at all; any unmatched or mistyped route silently rendered blank (#308)
- `api/auth.test.ts` — 8 tests for `apiForgotPassword`/`apiResetPassword`: confirms query-string param encoding (not a JSON body), success paths, and backend error-message passthrough (#308)
- `AdminCategoryController` — full admin CRUD for product categories at `/api/v1/admin/categories` (list/get/create/update/delete), plus hierarchical parent/child category support: `Category.parentCategory` (self-referencing `@ManyToOne`) and `Category.subcategories`. `CategoryService`/`CategoryServiceImpl` already existed but had no controller exposing them and no hierarchy field — both were built from scratch. Deleting a category is blocked (400) while it still has products or subcategories referencing it; updating/creating a category rejects a non-existent parent, a category naming itself as its own parent, and any parent assignment that would create a hierarchy cycle. Also fixes a latent bug found during implementation: `Category.products` was mapped `cascade = CascadeType.ALL`, meaning deleting a category would have cascade-deleted every product in it — the opposite of the "prevent delete if products exist" requirement; cascade removed entirely (ADM-02, #68)
- `20260707-018-alter-categories-add-parent-id.xml` — nullable self-referencing `parent_id` column + FK on `categories`, following the same `tableExists` drift-guard pattern established for `products`/`cart_items` in #81/#82 (`categories` is a Hibernate-only table, not Liquibase-managed) (#68)
- `CategoryServiceImplTest` +9 tests, `CategoryTest` +3 tests, `AdminCategoryControllerIntegrationTest` (13 tests) — parent resolution, parent-not-found, self-parent rejection, cycle detection, delete blocked by existing products, delete blocked by existing subcategories, non-admin 403 (#68)

### Removed
- `prometheus.yml.template`'s `mysql`, `redis`, and `node` scrape jobs — none of `mysqld_exporter`, `redis_exporter`, or `node_exporter` are deployed anywhere in `docker-compose.yml`, so these targets were permanently "down" in Prometheus's own `/targets` page (real, current signal noise, distinct from the docs-vs-live-config discrepancies elsewhere in this project). Decided with the user (Option B of the issue's two options): remove rather than deploy the exporters, since this repo's observability maturity doesn't yet have anything (Grafana dashboards, #122) consuming that data. Verified empirically: active target list is now exactly `['buildnest', 'prometheus']`, no dangling entries (#325)

### Fixed
- `logstash.conf` hardcoded Elasticsearch credentials (`user => "elastic"`, `password => "changeme"`) instead of reading `docker-compose.yml`'s existing `ELASTICSEARCH_USERNAME`/`ELASTICSEARCH_PASSWORD` container env vars — rotating the real password in `.env` silently broke Logstash's connection with no error surfaced anywhere except its own container logs. Now uses Logstash's native `${VAR}` interpolation (fails loudly at startup on an undefined var, unlike the previous silent-failure mode). Also fixed `logstash.conf`'s and `prometheus.yml`'s hardcoded `environment: 'production'` label, which misrepresented any non-production deployment of this compose file. `prometheus.yml` is now a template (`prometheus.yml.template`) rendered by a new one-shot `prometheus-config-render` service before Prometheus starts, substituting the real `ENVIRONMENT` value (`.env`, defaults to `dev`) — native Prometheus env-var substitution for `external_labels` exists but only from v2.55+ (this repo pins v2.52.0), and bumping to the current 3.x LTS line to get it natively was judged out of scope given real breaking-change risk to `alerts.yml`/behavior not verified here. Also fixed, discovered in the same pass: `alerts.yml` (referenced by `prometheus.yml`'s own `rule_files`) was never actually mounted into the Prometheus container at all — a dangling reference now resolved via the same rendered volume. Verified empirically end-to-end: rendered config shows `environment: 'dev'` correctly resolved, Prometheus starts cleanly with zero config errors, and `/api/v1/rules` shows the alert rule groups genuinely loaded (previously impossible, since the file was never mounted) (#324)

### Removed
- `WebMvcConfig.java`'s `addCorsMappings` (`allowedOrigins("*")`, `allowCredentials(false)`) was dead code — `SecurityConfig.filterChain`'s explicit `corsConfigurationSource` is authoritative per Spring Security's documented precedence (an explicit `corsConfigurationSource` makes Spring MVC's own CORS config, the kind `addCorsMappings` produces, unreachable for any request through the security filter chain, which covers all paths here via `anyRequest().authenticated()`). Confirmed empirically, not just from docs: a live preflight request from an origin disallowed by `SecurityConfig`'s list got a flat 403 with no `Access-Control-Allow-Origin` header at all — the wildcard mapping never had a chance to grant it. Deleted the now-fully-dead `WebMvcConfig` class entirely (it had no other purpose) and documented the single-CORS-source rule in `spring-security.md` so it isn't reintroduced. Not itself an exploit — filed and investigated as a discovered-during-#350's-toolchain-audit cleanup item, not a confirmed vulnerability (#352)

### Changed
- CheckStyle's 8,305 CI violations had been silently ignored indefinitely (non-blocking wrapper, no numeric signal ever surfaced) since the tool was first wired up. Decided policy, after weighing 4 options (tune the ruleset, baseline+ratchet, surface-count-only, bulk-fix now) with the user: **baseline + ratchet**, mirroring the PIT mutation-score pattern (`pom.xml`'s `mutationThreshold`, 77%→83% through M5) already proven in this repo. CheckStyle is now a genuine **blocking** CI check via `maven-checkstyle-plugin`'s native `maxAllowedViolations` parameter, set to 8,305 — the real, freshly-verified violation count — meaning the build fails only if a PR introduces a *net-new* violation beyond that ceiling, not on the pre-existing debt itself. Does not require fixing the 8,305 now; stops the count from growing silently, which the prior non-blocking wrapper never did. One-way tightening: lowering the ceiling as violations get triaged is expected; raising it back up requires a documented reason, mirroring this repo's other quality-gate ratchets. `Run SpotBugs` (the step immediately after CheckStyle in `security.yml`'s `code-quality` job) gained `if: always()` so CheckStyle's new blocking behavior doesn't silently skip it. Job summary now shows the real violation count (`{count}/8305`) instead of a static message (#354)

### Fixed
- SpotBugs had never actually run in CI — `mvn spotbugs:check` failed with `No plugin found for prefix 'spotbugs'`, silently swallowed by the step's non-blocking wrapper, misleadingly logging "SpotBugs issues found" when the tool had never once resolved. Root cause: `com.github.spotbugs` isn't one of Maven's default-searched plugin groups (unlike CheckStyle's `org.apache.maven.plugins`), and the plugin was declared nowhere in `backend/pom.xml`. Fixed by adding a bare `spotbugs-maven-plugin` (4.10.2.0) declaration to `pom.xml` — deliberately no `<executions>` binding, matching the CheckStyle pattern where CI invokes the goal as an explicit standalone step rather than on every build phase. First genuine run found 103 real findings; kept non-blocking (documented decision, matching #354's CheckStyle precedent — an untriaged first-ever report shouldn't immediately block every future PR) but the job summary now shows the real finding count instead of a static, meaningless message, parsed from `target/spotbugsXml.xml` (`grep -o ... | wc -l`, not `grep -c` — the XML is emitted as a single unbroken line, so `-c`'s line-counting semantics would always report 0 or 1 regardless of the real count) (#353)
- SonarCloud analysis had never actually run in CI despite `SONAR_TOKEN` being configured since 2026-07-06 — the `code-quality` job's Maven command only invoked `clean test`, with no `sonar:sonar` goal anywhere on the command line, and `backend/pom.xml` had zero SonarQube plugin configuration; `-Psonar` referenced a Maven profile that doesn't exist, which Maven silently warns about and ignores rather than failing the build. Fixed by invoking `org.sonarsource.scanner.maven:sonar-maven-plugin:sonar` (pinned version, no pom.xml changes needed — Maven resolves it directly), switching `clean test` to `verify` so the JaCoCo XML report is guaranteed generated first, adding `sonar.organization`, switching the deprecated `sonar.login` to `sonar.token`, wiring `sonar.coverage.jacoco.xmlReportPaths`, adding the `GITHUB_TOKEN` env var and `fetch-depth: 0` checkout needed for PR analysis/decoration, and explicitly skipping `dependency-check-maven` (bound to the default `verify` phase with no skip gate in this pom, so plainly switching to `verify` would have triggered a redundant full OWASP/NVD scan duplicating the existing `dependency-check`/`security-scan` jobs). Also corrected the job summary's misleading "SonarQube: Optional (configure with token)" line and a wrong artifact-upload path (`sonarqube-report.xml`, which the plugin never produces — real output is `target/sonar/report-task.txt`) (#350)
- `NotificationService` (the generic multi-channel webhook/email/Slack alert dispatcher), `DomainEventListener` (order/payment/inventory webhook + alert dispatch), `InventoryMonitoringService`, and `InventoryMonitoringScheduler` (low-stock/out-of-stock monitoring, every 5 min) were all accidentally gated behind `@ConditionalOnProperty(elasticsearch.enabled)` despite none of them having any Elasticsearch dependency — found while implementing #63, filed as #345. Removing the conditional from only `DomainEventListener` (the issue's original suggested fix) would have broken app startup entirely in the default `elasticsearch.enabled=false` configuration, since it depends on the equally-mis-gated `NotificationService`; investigation found `InventoryMonitoringService`/`Scheduler` were independently affected by the identical copy-paste pattern. All 4 classes un-gated together. `CivilEcommerceApplicationTests` extended with a regression test asserting all 4 are registered Spring beans under the default (ES-disabled) test profile, so this can't silently regress again — previously no test caught this, since existing unit tests construct these classes directly, bypassing Spring's conditional bean loading entirely. Deliberately left the misleading `elasticsearch.alert.*` config property namespace unrenamed — a separate, broader naming-cleanup concern touching deployment config, not required for the functional fix (#345)
- Build-log warnings surfaced during unrelated OWASP CI investigation on #335/#337/#338: `ProductCreatedEvent`/`ProductUpdatedEvent` declared a non-`transient` `Product` field on an otherwise-`Serializable` `ApplicationEvent` (now `transient`, matching the existing `OrderPlacedEvent` pattern, since `Product` doesn't implement `Serializable`); `StorageException` was missing an explicit `serialVersionUID`; all 25 test files using the deprecated `@MockBean` (marked for removal in a future Spring Boot release) migrated to `@MockitoBean`. A repo-wide grep confirmed no other `Serializable`/exception classes had the same gaps — every other custom exception already extends `BuildNestException`, which already declares `serialVersionUID` (#340)
- The OWASP `Cache NVD database` step (`ci.yml`) keyed its cache on a `hashFiles('**/pom.xml')` hash — since the NVD CVE dataset is an external source unrelated to this project's own dependency versions, any `pom.xml` change (a routine version bump, a suppression-adjacent edit) invalidated the cache and forced a full multi-hour NVD re-sync from scratch instead of a fast incremental delta. Discovered when three separate PRs (#335, #337, #338) each independently hit ~1.5-2 hour full syncs during the #332 CVE remediation work. Rekeyed to a daily date-based key (`nvd-db-${{ runner.os }}-${{ date }}`) with an OS-scoped `restore-keys` prefix fallback, fully decoupled from `pom.xml` (#342)
- **master's `OWASP Dependency-Check` CI gate had been failing for at least 2 days** (`failBuildOnCVSS=7`, `pom.xml`) on genuine CVSS≥7.0 findings across ~20 dependencies, several at 9.0+ including one at 10.0 (`netty-transport`). Real fixes via version bumps: `spring-boot-starter-parent` 3.5.10→3.5.16, `spring-cloud-dependencies` 2024.0.2→2025.0.3 (the actual Boot-3.5-compatible release train — 2024.0.x targets Boot 3.4), `springdoc-openapi-starter-webmvc-ui` 2.0.4→2.8.17 (bundles a patched `swagger-ui`/DOMPurify), `elasticsearch-java`/`elasticsearch-rest-client` 8.18.8→8.19.18, `log4j2`/`tomcat`/`netty`/`micrometer` version-property overrides past what Boot 3.5.16 itself still pins. The `elasticsearch-java` bump introduced a new transitive dependency on Jackson **3.0.0** (`tools.jackson.core`, a brand-new major release with its own fresh CVEs) — overridden to 3.2.0. **5 confirmed false positives** documented in `owasp-suppressions.xml`, each independently verified against the actual NVD CVE detail page (not just CPE-pattern suspicion): `angus-activation` (the real CVE is in `org.eclipse.angus:smtp`, already fixed by this project's `jakarta.mail` 2.0.5), `kotlin-stdlib` (a Kotlin *build-cache/compiler* deserialization CVE — this project never runs Kotlin build tooling, only consumes the compiled stdlib jar at runtime), `micrometer-registry-prometheus` (the CVE is in the Prometheus *server's* `/api/v1/read` endpoint, not the client-side exposition library), `netty-transport`/`netty-transport-native-unix-common` (CVE only affects Netty 4.2.0–4.2.12, this project is on 4.1.136.Final), `httpcore`/`httpcore-nio` (CVE affects HttpComponents Core **5.x**, a different artifact lineage from the legacy 4.x branch this project uses, which has no HTTP/2 support at all). Along the way, fixed two pre-existing bugs of the fix's own making: the `owasp` Maven profile's `dependency-check-maven` instance (v10.0.4 — the one CI's `-Powasp` run actually invokes) had **no `<suppressionFiles>` wiring at all**, silently discarding every suppression; and the suppression XML itself violated the schema (`<until>` must be an attribute on `<suppress>`, not a child element), which silently voided every suppression until fixed (#332)
- `com.example.buildnest_ecommerce.model.entity` package sat at 63.28% instruction coverage, failing the `jacoco-check` gate, entirely because of `model.entity.CustomUserDetails`, an unused class with 0% coverage. The issue as filed (#323) asked for unit tests to close the gap; verification against the repo showed the class was a dead duplicate — `security.CustomUserDetails` (the one actually implementing `UserDetails`) is what `CustomUserDetailsService` and every controller test construct, and already has full test coverage. Removed the dead duplicate instead of testing unreachable code — the root-cause fix, not a workaround to hit a number (#323)
- **SEC-15**: access and refresh JWT tokens were stored in `localStorage`, readable by any XSS anywhere in the app for the full 30-day refresh-token lifetime — violated this project's own documented rule (`react/security.md`: "never store sessions in localStorage"). Tokens now travel as httpOnly, `Secure` (profile-conditional), `SameSite=Lax` cookies (`AuthCookieService`), set by `login`/`refresh` and cleared by `logout`; the JSON response body no longer carries raw tokens (`AuthUserResponse`, `userId`/`username` only). `JwtAuthenticationFilter` reads the cookie (falls back to the `Authorization` header for non-browser clients). CSRF protection re-enabled via `CookieCsrfTokenRepository` double-submit pattern (`SecurityConfig`/`TestSecurityConfig`) — only `/api/auth/login` and `/api/auth/register` are exempt, since every other endpoint is now a cookie-driven, state-changing action a forged cross-site request could abuse. New `GET /api/auth/csrf` bootstrap endpoint. Frontend: `client.ts` sends `credentials: 'include'` and attaches `X-XSRF-TOKEN` on mutating requests; `AuthContext` no longer touches `localStorage` or decodes JWTs client-side, rehydrating via a live profile call instead; the `token` parameter was mechanically removed from ~20 API functions and ~18 components (single TypeScript-enforced pass, not a compatibility shim) (#282)
- Enabling CSRF surfaced a confirmed Spring Security defect ([GH-12141](https://github.com/spring-projects/spring-security/issues/12141)): `CsrfAuthenticationStrategy` clears the `XSRF-TOKEN` cookie on every authentication event but fails to regenerate it within the same request/response cycle. Because `JwtAuthenticationFilter` re-authenticates on every single stateless request (no session), this fired on effectively every request and silently broke all mutating endpoints — invisible to sequential curl-based testing, only reproduced under the app's real concurrent request pattern in a browser. Fixed via `NonClearingCsrfTokenRepository`, a wrapper that ignores null-token (clear) saves; the CSRF token is refreshed only by the app's own `/api/auth/csrf` bootstrap and login/refresh responses. Documented in the class's own javadoc and in `spring-security.md` so a future refactor doesn't remove the wrapper without understanding why it exists (#282)
- `SecurityConfig` (the production security filter chain, including the SEC-14 CSP hardening from #237 that removed `unsafe-inline` from the main API chain) is `@Profile("!test")` and never loads during a test run — `TestSecurityConfig`, the hand-maintained test double used instead, had drifted: single filter chain with no Swagger isolation, and a CSP of just `"default-src 'self'"` missing `script-src`/`style-src`/`frame-ancestors`/`form-action` entirely, plus a missing `preload(true)` on HSTS. The #237 fix had zero regression coverage as a result. Extracted `SecurityHeaderPolicies` as the single source of truth for `MAIN_CSP`, `SWAGGER_CSP`, and `HSTS_MAX_AGE_SECONDS`, referenced by both `SecurityConfig` and `TestSecurityConfig` so the two configs can no longer drift apart without a compile error. New `SecurityHeadersTest` asserts the main-chain CSP has no `unsafe-inline` and matches the shared policy, plus frame-deny and HSTS (verified against a simulated-HTTPS request, since Spring Security only emits HSTS on requests it considers secure) (#312)
- The Admin Audit Log tab showed "An unexpected error occurred" / "0 entries" on almost every real-world visit despite real entries existing — `AuditLogService`'s `@Cacheable` methods (`getAllAuditLogs` and 4 sibling filter methods, all with the same defect) returned a raw Spring Data `Page<AuditLog>`, which serializes to Redis fine on a cache miss but cannot be deserialized back out on any subsequent cache hit (`PageImpl` has no default constructor or Jackson creator) — the first request to any given page "poisoned" that page's cache entry for the full 15-minute TTL. Introduced `AuditLogPageDTO`, a plain, Jackson-round-trippable class (`content`/`totalPages`/`totalElements`/`number`, matching the shape the frontend already reconstructs) and applied it to all 5 `@Cacheable` methods, not just the one endpoint currently wired to a controller. Deliberately non-generic rather than a generic `PageResult<T>` — a generic type has the same reified-type problem Jackson has with `PageImpl`, just one level down (a cached `List<T>` deserializes each element as a raw `LinkedHashMap` without explicit type info); a concrete `List<AuditLog>` field avoids that ambiguity entirely. New `AuditLogCacheSerializationServiceTest` reproduces the exact Redis serializer configuration from `CacheConfig` (no live Redis needed) and proves both the original bug (raw `PageImpl` throws on deserialize) and the fix (the DTO round-trips cleanly across two consecutive reads, matching the issue's exact reproduction steps) (#307)
- Admin Overview and Users tabs both failed with a generic error and no data — `!user.getIsDeleted()` auto-unboxed a `null` `Boolean` for at least one legacy user row, throwing an NPE that was caught by a bare `catch (Exception e)` with **no logging at all**, making the real failure invisible server-side. Fixed all 7 recurring call sites of this unguarded pattern (`AdminServiceImpl` ×3, `UserServiceImpl` ×2, `OrderServiceImpl` ×2) to `!Boolean.TRUE.equals(x.getIsDeleted())` — null-safe, and correctly treats a null flag as "not deleted" rather than throwing. Added `log.error(...)` to every previously-silent `catch (Exception e)` in `AdminReportController` (5 endpoints) and `AdminUserController` (4 endpoints), so a real failure is diagnosable going forward. Added Liquibase changeset `20260705-015-backfill-null-is-deleted-flags` to backfill existing `NULL` `is_deleted` rows on `users`/`orders` to `false` (guarded with `preConditions` — neither column is created by any Liquibase changeset in this repo, the same drift class as #305's `products`/`cart_items`; both only exist via Hibernate) (#306)
- `CartServiceImpl.addToCart` snapshotted `product.price` (full price) on every add-to-cart, never checking `product.discountPrice` — every discounted product on the storefront was silently overcharged relative to its advertised price, all the way through order confirmation (the same `price` snapshot flows into `OrderItem` at checkout). New cart items now snapshot `product.getDiscountPrice()` when set, falling back to `product.getPrice()` otherwise — matching the frontend's existing `discountPrice ?? price` display convention (`ProductCard.tsx`). Added Liquibase changeset `20260705-014-correct-cart-item-prices-for-discounted-products` to re-snapshot price on existing, not-yet-checked-out `cart_items` rows affected by the bug (guarded with `preConditions onFail="MARK_RAN"` on `products`/`cart_items` existing — neither table is created by any Liquibase changeset in this repo, the same drift class documented in #289's `20260704-012` changeset; both only exist via Hibernate). Already-placed, already-billed orders are deliberately **not** auto-corrected — `docs/reports/cart-price-correction-2026-07-05.md` gives a read-only audit query to identify potentially overcharged historical orders for manual finance/refund review, since retroactively rewriting a settled `order_items` price is a business decision, not a code fix (#305)
- `CartServiceImpl.getCartByUserId` and `WishlistServiceImpl.getWishlist`/`getWishlistProducts` threw a "not found" exception for a brand-new user who hasn't added anything yet — both Cart and Wishlist are lazily created on first *write* (`addToCart`/`addProduct`), but their read paths treated "no row yet" as an error rather than empty, surfacing a red error banner instead of the existing empty-state UI on `/cart` and the Wishlist account tab for the majority of first-session users. Both read paths now return an empty `CartResponseDTO` / empty `Wishlist` (`Set.of()` products) when no row exists yet, instead of throwing. Write-adjacent paths that act on an existing cart/wishlist (`clearCart`, `removeProduct`, `clearWishlist`) are unchanged — those legitimately have nothing to act on for a brand-new user (#303)
- `shipping_methods` had zero rows in a fresh environment (no seed data existed for this table), blocking every checkout at the shipping-options step — `ShippingServiceImpl.getShippingOptions` calls `findAllByIsActiveTrue()` with no fallback, so an empty table meant checkout could never complete on a fresh deploy or in a freshly-provisioned test/staging environment. Added Liquibase changeset `20260704-013-seed-default-shipping-method` inserting one active "Standard Delivery" flat-rate method. Verified the changeset applies correctly via a new standalone `ShippingMethodSeedMigrationTest` that runs Liquibase directly against a throwaway H2 connection (not `@SpringBootTest`) — the `test` profile's `ddl-auto=create-drop` runs Hibernate schema regeneration *after* Liquibase and drops/recreates every entity-mapped table including `shipping_methods`, discarding the seeded row before any `@SpringBootTest` body could observe it; production's `ddl-auto=validate` has no such conflict (#304)
- Added explicit `type="button"` to 31 non-submit `<button>` elements across 19 files that the new `react/button-has-type` lint rule flagged (re-enumerated fresh rather than trusting the audit's original count of 48 — the codebase had changed enough since the audit that the real count was different): `OrderDetailModal`, `OrdersTab` (account + admin), `WishlistTab`, `AuditLogTab`, `InventoryAdjustModal`, `InventoryTab`, `UsersTab`, `CartItemRow`, `ErrorMessage`, `Pagination`, `CategorySidebar`, `ImageGallery`, `QuantitySelector`, `ReviewsSection`, `AccountPage`, `AdminDashboardPage`, `CartPage`, `ProductDetailPage`. Every genuine submit button (login, register, checkout address step, address book save, security/password change, profile save) was left as `type="submit"` (#299)
- `PasswordResetController.changePassword`: **critical IDOR** — the endpoint accepted `userId` as a client-supplied `@RequestParam` with no check that it matched the authenticated caller. Any authenticated user could call `POST /api/password/change?userId=<victim>&oldPassword=<guess>&newPassword=<new>` and, given the victim's current password, take over their account. `userId` is now derived exclusively from `@AuthenticationPrincipal`, matching the pattern already used in `AddressController`. The issue as filed (#249) didn't mention this — it only flagged the credentials-in-URL problem below (#249)
- `PasswordResetController.changePassword`: credentials (`oldPassword`, `newPassword`) moved from URL query params to a JSON `@RequestBody` (new `ChangePasswordRequest`) — passwords no longer appear in server access logs, browser history, or `Referer` headers. **Correction to the issue as filed**: #249 stated the backend "expects a JSON body" and the existing query-param call would fail — that was incorrect. The endpoint used `@RequestParam` for all three fields and the existing frontend call worked exactly as the backend expected; switching only the frontend to send JSON without this backend change would have broken the endpoint (#249)
- `frontend/src/api/user.ts`: `changePassword` now sends `{ oldPassword, newPassword }` as a JSON body instead of URL query params, and no longer takes or sends `userId` at all (#249)
- `SecurityTab.tsx` / `AccountPage.tsx`: `SecurityTab` no longer takes a `userId` prop — nothing in the component needs it now that the backend derives the acting user from the auth token (#249)
- `SecurityTab.tsx`: password fields had no `id`/`htmlFor` association between `<label>` and `<input>` (same class of gap fixed elsewhere in #286) — added (#249)
- None of `ci.yml`, `ci-cd-pipeline.yml`, or `ci-cd.yml` filtered their `on:` triggers by changed path — a docs-only PR (#333, #334) still ran the full pipeline including the `OWASP Dependency-Check` job (30–50 minutes) despite touching no dependency-affecting files. Added `paths-ignore` (`docs/**`, `**/*.md`, `.github/ISSUE_TEMPLATE/**`) to the `push`/`pull_request` triggers of all three workflows. `paths-ignore` only skips a workflow when *every* changed file matches the ignore pattern, so a mixed docs+code PR still runs the full pipeline. Found in passing: `ci-cd.yml` targets `main`/`develop` branches, neither of which exists in this repo (only `master`) — it is currently unreachable regardless of this fix (#336)
- `PasswordResetServiceImpl.initiatePasswordReset` generated and stored a real reset token but never sent it anywhere — only logged that an email "would be sent in production", leaving no way for a real user to obtain their token outside direct DB/log access. The fix is a one-line wiring gap, not a missing feature: `INotificationService.sendPasswordResetEmail(email, token)` already existed fully implemented (real SMTP send via `JavaMailSender`, async, retry-on-transient-failure, Thymeleaf template) from #62 — `PasswordResetServiceImpl` just never called it. Also added a local dev-mode SMTP fallback per the issue's acceptance criteria: a `mailpit` service in `docker-compose.yml` (SMTP catcher + web UI at `localhost:8025`, no real credentials needed) and documented `MAIL_HOST`/`MAIL_PORT`/`MAIL_USERNAME`/`MAIL_PASSWORD`/`MAIL_FROM`/`MAIL_FROM_NAME` in `.env.example` (previously undocumented despite being consumed since #62) (#327)

### Changed
- Migrated 9 hand-rolled `data`/`loading`/`error` implementations to `useAsync` (#298), each behavior-preserving (verified via the existing test suite plus a manual browser check on the home and product-listing pages, since `useCategories`/`useFeaturedProducts`/`useProducts` are exercised directly by those pages):
  - Admin tabs (highest duplication count, migrated first per the acceptance criteria): `OverviewTab`, `InventoryTab` (kept its `reload` for the inventory-adjust-modal's post-save refresh), `UsersTab` (kept its optimistic `setData` update after disabling a user), `AuditLogTab`, `OrdersTab` (kept its optimistic `setData` update after a status change)
  - Account tabs: `ProfileTab` (fetch via `useAsync`; the profile→form sync moved to a `useEffect` keyed on the fetched data), `WishlistTab` (kept its optimistic `setData` updates after remove/move-to-cart), `OrdersTab` (kept its `reload` for the Retry button added in #246)
  - Hooks: `useProduct`, `useProducts`, `useCategories`, `useFeaturedProducts` — `useCategories`/`useFeaturedProducts` preserve their existing silent-fallback-to-`[]` behavior at the hook's return boundary (`data ?? []`), not exposing the new `error` capability to callers that didn't have it before
  - `useReviews.ts` intentionally left as-is — it was cited as one of the two cleanest existing examples the new hook generalizes *from*, and its combined `Promise.all` two-fetch shape doesn't fit `useAsync`'s single-fetcher contract without additional work not required by the acceptance criteria
- `components/checkout/AddressStep.tsx`: replaced the stale "Saved address book is coming in a future update" notice with a link to the now-working Address Book on the account page (#247)
- `pages/AccountPage.tsx`: `AddressesTab` now receives the `token` prop it needs to call the address API (#247)
- Chose indigo as the single primary brand color (was split: `indigo-600` on product listing/detail, `amber-500` on cart/checkout/admin/account, with no shared token). Converted every primary CTA button, focus ring, active/selected state, and brand-accent link across the app from hardcoded `indigo-*`/`amber-*` to the new `primary-*` token: `pages/{HomePage,ProductListingPage,ProductDetailPage,LoginPage,RegisterPage,CartPage,CheckoutPage,OrderConfirmationPage,AccountPage,AdminDashboardPage}.tsx`, `components/{checkout/*,common/{ErrorMessage,Pagination,RequireAuth,Navbar},filters/*,product/{ImageGallery,ProductCard},account/{OrdersTab,ProfileTab,SecurityTab,WishlistTab},admin/{OrdersTab,UsersTab,InventoryTab,InventoryAdjustModal,AuditLogTab}}.tsx` (#245)
- Deliberately left unconverted, as genuinely semantic (not brand) colors: star-rating gold (`amber-400` in `StarRating.tsx`/`ReviewsSection.tsx` — a conventional rating indicator, not a CTA), order-status badge maps (`indigo-100/700/800` for `SHIPPED`/`PROCESSING` alongside yellow/blue/green/red in `OrdersTab`/`OrderDetailModal`/`OrderConfirmationPage` — status semantics, not brand), notice/warning banners (`amber-50/200/700/800` — "coupon not yet supported," "address book coming soon," cart error banners), and the admin dashboard's categorical stat-card/chart-bar palette (`OverviewTab.tsx` — 4 distinct colors distinguishing 4 different metrics, not a brand inconsistency) (#245)
- `components/common/Navbar.tsx` — persistent global navigation bar mounted once in `App.tsx` (above `<Routes>`, inside `AuthProvider`), consuming `useAuth()` and `useCart()`: logo/home link, search bar (submits to `/products?search=`), cart icon with a live item-count badge (sum of item quantities; hidden when zero), account menu — authenticated users see their username with a dropdown (Account, Admin link only when `roles` includes `ADMIN`, Sign out), unauthenticated users see a "Sign in" link. Collapses to a hamburger menu below the `sm` breakpoint, closing on click-outside via a `mousedown` listener. This is the root-cause fix for auth (sign in/out) being invisible anywhere in the app — `AuthContext` existed and worked, but no shared layout consumed it (#244)
- `Navbar.test.tsx` — 9 tests: logo always renders, Sign in when unauthenticated, username + dropdown when authenticated, Admin link gated on role, logout invoked on Sign out click, cart badge shows the summed quantity, no badge when cart is empty/unauthenticated, search submit navigates to the filtered listing, mobile menu open/close toggle (#244)
- `components/account/OrdersTab.tsx`: error state now includes a "Retry" button that re-runs the fetch (`loadOrders` extracted to a `useCallback` reusable from both the initial load and the retry click), closing the last unmet acceptance criterion of #246 — every other criterion (fetch on mount, list with ID/date/status/total, click-through to detail, empty state, loading skeleton) was already satisfied before this session; the issue's stated premise ("no API call is made") was stale even before #297, since the tab already called the endpoint, just via an unchecked raw `fetch()` (#246)
- `OrdersTab.test.tsx` +1 test: clicking Retry after a failure re-invokes `fetchOrders` and renders the list on the second attempt (#246)
- `frontend/src/api/orders.ts`: added `fetchOrders(token)` — the API layer previously only exposed `fetchOrderById`, so `OrdersTab.tsx` called `fetch('/api/user/orders')` directly with no `res.ok` check; a 401 (e.g. an expired token) or 500 would still resolve as JSON, `body.data` would be `undefined`, and the list rendered as "You haven't placed any orders yet" — an auth/server failure silently presented as a false-negative empty state. `fetchOrders` goes through the shared client and throws on non-2xx like every other endpoint (#297)
- `orders.test.ts` +3 tests (list success, throws on non-2xx instead of returning `[]`, empty-array fallback when `data` is absent on an otherwise-successful response); `OrdersTab.test.tsx` (new) — 3 tests proving the fix directly: renders the list, shows the genuine empty state only on real success with zero orders, and surfaces an error message (not the empty state) when the fetch fails (#297)
- `frontend/src/api/client.ts`: 401 responses to a token-bearing request now trigger one silent refresh-and-retry via a registered `setUnauthorizedHandler()` callback — `apiRefresh()` (added in an earlier session, but never called from anywhere) is finally wired up. On a 401, `request()` calls the handler once; if it resolves a new access token, the original request is retried once with that token; if it resolves `null` (refresh itself failed), the original 401 error is thrown. A request with no `token` never triggers the handler, and a retried request is never retried again, so there is no loop (#296)
- `frontend/src/contexts/AuthContext.tsx`: registers the unauthorized handler on mount — reads `refresh_token` from storage, calls `apiRefresh()`, stores and returns the new access token on success; on failure (or no stored refresh token), clears storage and logs the user out (`setUser(null)`, `setToken(null)`) so a session that can no longer be refreshed doesn't linger in a half-authenticated state (#296)
- `client.test.ts` +5 tests: retry-with-new-token on 401, throws original error when the handler can't refresh, retries at most once, skips the handler entirely for tokenless requests, throws immediately when no handler is registered (#296)
- `AuthContext.test.tsx` +2 tests: a 401 on any request silently refreshes the token and updates context state (verified by driving the real `client.request()` against a mocked `fetch`, not by mocking the client); a failed refresh logs the user out (#296)
- `frontend/src/api/client.ts` — shared HTTP client: `request<T>()` sends a request and returns the raw parsed JSON body, throwing `ApiError` (carrying the HTTP status) on a non-2xx response, preferring the backend's own `ApiResponse.message` over a caller-supplied fallback; `requestData<T>()` additionally unwraps the standard `ApiResponse<T>` envelope and throws when `success` is `false` even on a 2xx response. Centralizes `Authorization` header injection and `Content-Type: application/json` (only added when a body is present) that were previously copy-pasted across 11 files (#295)
- `frontend/src/api/client.test.ts` — 13 tests: header injection, body serialization, 204 handling, backend-message-over-fallback precedence, string/function fallback messages, `ApiError.status`, malformed error-body tolerance, `requestData` envelope unwrapping and `success:false` handling (#295)
- `frontend/src/components/common/RequireAuth.tsx` — route-guard wrapper component consuming `useAuth()`: shows a loading spinner while auth state resolves, redirects unauthenticated users to `/login` (preserving the original path via `location.state.from`, matching the existing `LoginPage` redirect-back convention), and renders an "Access denied" message when an optional `role` prop is required but the user lacks it (#294)
- `frontend/src/components/common/RequireAuth.test.tsx` — 5 tests: loading state, unauthenticated redirect, authenticated with no role required, authenticated missing required role, authenticated with required role (#294)
- `frontend`: Vitest + React Testing Library test harness (`#293`) — `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`, `jsdom`, `@vitest/coverage-v8` installed; `test`/`test:watch`/`test:coverage` scripts added to `package.json`; Vitest configured inline in `vite.config.ts` (jsdom environment, `src/test/setup.ts`) rather than a separate config file
- `frontend/src/contexts/AuthContext.test.tsx` — session restore (no token, valid token, expired token clears storage without calling the profile API), login (stores tokens, loads roles from profile), logout (clears tokens, calls `apiLogout`) (#293)
- `frontend/src/hooks/useCart.test.ts` — no-op when userId/token missing, load, error surfacing on fetch failure, add-item-then-reload, remove-item-then-reload (#293)
- `frontend/src/api/cart.test.ts` — success and non-2xx paths for `fetchCart`, `addToCart`, `removeCartItem`, `clearCart` (#293)
- `docs/reports/frontend-anti-patterns-audit-2026-07-04.md` — exhaustive frontend anti-pattern audit (12 findings); source of issues #293–#302
- `docs/wiki/learned-lessons/github-issue-multi-domain-labeling.md`, `vitest-needs-triple-slash-reference-in-vite-config.md` — reusable lessons from this session's audit and test-harness work

### Changed
- `App.tsx`: mounts `<Navbar />` once, above `<Routes>`, inside `<AuthProvider>` (#244)
- Removed the duplicated per-page `<header>` from 8 pages now that `Navbar` provides it globally: `HomePage.tsx` (dropped its own search form — the page's other search-adjacent state was cleaned up too), `ProductListingPage.tsx` (dropped its own logo+search header; kept the local "Clear" affordance for the active keyword filter, moved next to the toolbar's `for "keyword"` text since the URL-sync effect already reacts to `Navbar`'s search), `ProductDetailPage.tsx` (dropped logo + "← Back" button; the existing breadcrumb already covers back-navigation context), `CartPage.tsx`, `OrderConfirmationPage.tsx` (both were logo-only), `CheckoutPage.tsx` (kept a slim "← Back to Cart" link in place of the full logo+breadcrumb bar), `AccountPage.tsx` and `AdminDashboardPage.tsx` (both dropped logo, cart link, username, and Sign out — all now covered by `Navbar`'s account dropdown; `AdminDashboardPage` keeps its "Admin" badge as a page-specific indicator with no `Navbar` equivalent) (#244)
- `components/account/OrdersTab.tsx`: replaced the inline `fetch('/api/user/orders', ...)` call (no status check, no shared client) with `fetchOrders(token)` from `api/orders.ts` (#297)
- `docs/SDLC-docs/software-testing/test-plan.md`: TR-10 marked resolved (frontend test suite now exists); Phase 2 entry-criteria row and the frontend-component-coverage metrics row updated to reflect the Vitest baseline rather than "not yet active" — coverage is not yet at the ≥80% gate, only `AuthContext`, `useCart`, and `api/cart` are covered so far (#293)
- `App.tsx`: `/account` and `/admin` routes now wrapped in `<RequireAuth>` (the latter with `role="ADMIN"`) instead of relying on each page to guard itself (#294)
- `AccountPage.tsx` / `AdminDashboardPage.tsx`: removed the duplicated inline loading/`isAuthenticated`/`isAdmin` guard blocks — now handled once by `RequireAuth` at the route level; both pages' existing null-safe rendering (`token &&`, `user?.username`) already tolerated the change with no further edits needed (#294)
- All 11 `api/*.ts` modules (`addresses`, `admin`, `auth`, `cart`, `categories`, `checkout`, `orders`, `products`, `reviews`, `user`, `wishlist`) migrated from hand-rolled `fetch()` + header + error-parsing boilerplate to the shared `api/client.ts`; public function signatures unchanged. `wishlist.ts`'s `addToWishlist`/`removeFromWishlist` previously did not check `res.ok` at all (any 4xx/5xx was silently swallowed as success) — migrating through the shared client now surfaces those failures; both call sites (`WishlistTab.tsx`) already wrap the calls in `try/catch`, so this is a pure correctness fix with no new unhandled-rejection risk (#295)
- `cart.test.ts` updated to match the shared client's header shape (no `Content-Type` on bodyless GET requests, `body: undefined` present in the `fetch` call args) — behavior improvement, not a regression (#295)

### Fixed
- `CartController` IDOR: `addToCart`, `getCart`, `clearCart`, `getCartTotal` now enforce `#userId == authentication.principal.id` via `@PreAuthorize`; `removeFromCart` (which took no `userId` at all and did a blind `deleteById`) now derives the caller's ID from `@AuthenticationPrincipal` and verifies cart-item ownership in `CartServiceImpl` before deleting — a valid token for one user previously could read/mutate another user's cart with no ownership check (#281)
- `GlobalExceptionHandler`: added a handler for `org.springframework.security.access.AccessDeniedException` (the supertype of `AuthorizationDeniedException`, thrown by `@PreAuthorize` denial) — previously uncaught and falling through to the generic 500 handler, so a blocked IDOR attempt returned "Internal Server Error" instead of 403 (found verifying #281)
- `AuthServiceImpl.login()` / `refreshAccessToken()`: `AuthResponse.userId` was never populated (always `null`) despite the field existing on the DTO — found verifying #280; the frontend's fresh-login path could not resolve a real user ID without a page reload first
- `AuthContext.tsx`: `user.id` and `user.roles` no longer decoded from the JWT payload, which deliberately carries only `sub`/`iat`/`exp` (see `spring-security.md`) — `tokenToUser()`/`tokenToRoles()` always produced `id: 0` and `roles: []`. `id` now sourced from the login response (`tokens.userId`) and `GET /api/user/profile`; `roles` now sourced from `GET /api/user/profile` on both the login path and the page-reload/restore-session path (#280, #292)
- `UserResponseDTO` / `UserServiceImpl.mapToResponseDTO`: added `roles: List<String>`, populated from `user.getRoles()` (EAGER-fetched, no N+1 risk) — no API surface exposed the authenticated user's roles at all, so `AdminDashboardPage`'s `user.roles.includes('ADMIN')` gate was permanently false and the admin dashboard was unreachable for every real admin account (#292)
- "Décor" category `name`/`description` corrected via Liquibase changeset `20260704-011` — row was double-encoded UTF-8 (`DÃ©cor` instead of `Décor`) at the database layer, not a frontend rendering bug; root-caused to a client/connection encoding mismatch at original insert time, unrelated to any versioned migration (#283)
- `backend/.env` / `.env.example`: `SPRING_DATASOURCE_URL` now includes `useUnicode=true&characterEncoding=UTF-8`, quoted (`.env` is loaded via `bash source`, and an unquoted `&` is parsed as a shell background operator, silently truncating the value) (#283)
- `backend/.env`: removed a duplicate `LIQUIBASE_ENABLED=false` line that silently overrode an earlier `=true`, which had disabled Liquibase entirely in this local environment for an unknown number of prior sessions (found while investigating #283)
- Liquibase changeset `20260630-010-alter-inventory-add-reservation-expires-at`: added a `preConditions`/`columnExists` guard — with Liquibase previously disabled, `ddl-auto=update` had already drift-created this column outside Liquibase's tracking, so re-enabling Liquibase failed on "duplicate column name" before reaching any later changeset (found while investigating #283)
- `ProductListingPage.tsx`: search keyword and category filters are now hydrated from the URL (`?search=`, `?category=`) on initial load, and re-synced via an effect when the URL changes externally (browser back/forward) — previously the search box and product grid stayed stale after back/forward even though the URL had already reverted (#284)
- `JacksonConfig.objectMapper()`: added `.disable(SerializationFeature.WRITE_DATES_AS_TIMESTAMPS)` — every date/time field in every API response was serializing as a JSON integer array (e.g. `[2026,6,29,15,46,13]`) instead of an ISO-8601 string (#285)
- `LoginPage.tsx` (username, password) and `ProfileTab.tsx` (firstName, lastName, email, phone, address): added `id`/`htmlFor` pairing and `name` attributes — form fields had no accessible label association (#286)
- `CheckoutPage.tsx`: removed the hardcoded `PLACEHOLDER_ADDRESS_ID = 1` — every order was silently placed against whatever address happened to be DB row 1, regardless of what the customer entered in the address step (#243)

### Removed
- `OrderProcessingComprehensiveTest` — duplicated `OrderServiceImplTest`'s Mockito-based unit coverage and `OrderServiceIntegrationTest`'s `@DataJpaTest` repository-level assertions; unique scenarios migrated into the two canonical classes before deletion (#259)
- 7 misleadingly named top-level `@DataJpaTest` classes — `RBACTest`, `AdminDashboardTest`, `AnalyticsReportingTest`, `ApiIntegrationTest`, `PaymentProcessingTest`, `InventoryManagementTest`, `CategoryManagementTest` — each tested only basic JPA persistence mechanics while its name implied coverage of RBAC enforcement, admin dashboards, analytics, API contracts, payment processing, inventory management, or category management respectively; `ApiIntegrationTest` asserted tautologies (e.g. `page >= 0`) and tested no actual API contract; every persistence scenario they covered is already tested more thoroughly by dedicated classes (`AuthenticationAuthorizationSecurityTest`, `AdminProductControllerIntegrationTest`, `SalesAnalyticsServiceImplTest`/`AdminAnalyticsServiceTest`, the payment test suite, `InventoryServiceImplTest`, `CategoryRepositoryTest`); no unique coverage lost (#255)

### Investigated
- Product listing filter/sort/pagination (#242): verified live — category filtering, sorting, and pagination all work correctly today via client-side filtering over the full fetched catalog (`useProducts.ts`); the originally filed "P0, silently broken" characterization did not match current behavior. Real underlying issue is architectural (fetches the entire catalog unfiltered/unpaginated, won't scale to a real catalog size) — downgraded from critical to low, no code changed
- Duplicate API requests per page load (#287): built and ran a production build (`npm run build && npm run preview`) — every endpoint fired exactly once, versus twice under `npm run dev`. Confirmed React 18/19 `StrictMode` (wraps `<App/>` in `main.tsx`) deliberately double-invoking mount effects in development; not a production defect, no code changed
- PIT mutation testing non-determinism (#276): ran `./mvnw verify -Dpitest.skip=false -Dsurefire.skip=false` three times on commit f4165cc and diffed all three `mutations.xml` outputs keyed on (mutatedClass, mutatedMethod, lineNumber, mutator) — identical aggregate stats across all 3 runs (1637 generated, 1272 killed, 78% mutation score, 68 no-coverage, 81% test strength) and **zero of 1530 distinct mutant identities changed status** between runs; confirmed absent, no fixes required per the issue's own closing criterion

### Changed
- `AdminInventoryControllerTest` converted from `@SpringBootTest` (full application context, including database/Redis/Elasticsearch config) to `@WebMvcTest(AdminInventoryController.class)` with `@MockBean InventoryService` — all 22 existing scenarios pass with a much cheaper test context; `AdminInventoryControllerIntegrationTest` (real H2) is unchanged (#259)
- PIT mutation score raised from 75% (1,237/1,658) to 77% (1,284/1,658), bumping the CI gate to ≥77% (#277); no-coverage mutations reduced from 72 to 68; test strength improved from 78% to 81%
  - `AdminServiceImplTest`: `ArgumentCaptor<User>` on `deleteUser` asserting `isDeleted=true` and `deletedAt≠null`; `ArgumentCaptor` on `updateUser`/`updateUserByAdmin` asserting `updatedAt≠null`; `ex.getMessage()` assertions on all not-found paths to kill lambda null-return mutations
  - `CartServiceImplTest`: full `CartItemResponseDTO` field assertions (cartItemId, productId, productName, quantity, price, itemTotal); `ArgumentCaptor<Cart>` on `save()` for new-cart path asserting user and items set; new test `testAddToCart_newItem_setsAllFieldsOnCartItem` with `ArgumentCaptor<CartItem>` asserting all fields; `testGetCartTotal` tightened from `assertTrue(total >= 0)` to `assertEquals(200.0, total)`
  - `UserServiceImplTest`: `ArgumentCaptor<User>` on `save()` in `updateUserProfile` asserting `updatedAt≠null`; field assertions on all three name/phone fields; `assertNotNull(existing.getDeletedAt())` in `testDeleteUser`
  - Three test files renamed via `git mv` so PIT `targetTests` pattern includes them: `RedisCheckoutSessionStoreTest` → `RedisCheckoutSessionStoreImplTest`, `EmailTemplateRenderingTest` → `EmailTemplateRenderingServiceTest`, `OrderProcessingComprehensiveTest` → `OrderProcessingComprehensiveServiceTest`
- PIT mutation score raised from 69% (1,122/1,637) to 75% (1,237/1,658), clearing the ≥75% CI gate (#277); no-coverage mutations reduced from 167 to 72; test strength improved from 76% to 78%
  - `NotificationServiceImplTest`: `ArgumentCaptor<Context>` assertions on all `ctx.setVariable()` calls; `MimeMessage` recipient/subject assertions to detect `setTo`/`setSubject` removal mutations
  - `InventoryServiceImplTest`: added `@Mock InventoryAuditLogRepository` (was null, blocking `adjustStock`); 22 new tests covering `reserveStock`, `releaseReservation`, `releaseExpiredReservations`, `adjustStock`, `getInventoryStatus`, `getAllInventorySummary`
  - `InventoryReportServiceTest`: replaced size-only assertions with specific map-field checks; added sorting-order tests for all three sorted result sets
  - `InventoryThresholdManagementServiceTest`: state assertions after `setProductThreshold`/`setCategoryThreshold`; new cache-repopulation test for `getProductThreshold` cache-miss path
  - `CheckoutServiceImplTest`: added 4 missing `@Mock` fields (was causing 57 no-coverage mutations); 12 new multi-step checkout tests; strengthened subtotal/order-number/status assertions
  - Scheduler tests renamed `*Test` → `*ImplTest` (`TokenCleanupScheduler`, `InventoryReservationCleanupJob`, `InventoryMonitoringScheduler`) so PIT `targetTests` pattern includes them
  - `OrderSpecificationTest` renamed to `OrderSpecificationImplTest` for same reason

### Added
- `frontend/src/pages/HomePage.tsx` at `/`: featured products (via new `GET /api/public/products/featured`), category quick-links, search bar, and a static promotional banner (explicitly commented as a placeholder pending #290's backend-driven version, deferred by decision — FR-FE-11, #288). `ProductListingPage` relocated from `/` to `/products` to remain reachable as the full catalog view; ~7 internal "Browse Products"/"Continue Shopping"/"Back to products" links across `CartPage`, `OrderConfirmationPage`, `WishlistTab`, and `ProductDetailPage` updated to point at `/products` instead of the old catalog root
- `Product.isFeatured` (Liquibase `20260704-012`, default `false`) + `ProductRepository.findByIsFeaturedTrueAndIsActiveTrue()` + `GET /api/public/products/featured` (`HomeController`) + admin toggle via `CreateProductRequest.isFeatured` (update only applies the flag when the request explicitly provides it, so existing admin calls that don't send the field can't silently clear it) (PROD-06, #289)
- `frontend/src/hooks/useFeaturedProducts.ts` + `fetchFeaturedProducts()` in `api/products.ts` (#288, #289)
- Backend address creation: `AddressController` (`POST /api/user/addresses`, create-only), `AddressService`/`AddressServiceImpl`, `CreateAddressRequest`, `AddressResponseDTO`; derives `userId` from the authenticated principal. First address for a user is automatically marked default. Listing/update/delete deliberately out of scope, left for FE-11 (#247) (#243)
- `frontend/src/api/addresses.ts` — `createAddress()` typed client; `AddressStep.tsx` now collects a `country` field (previously missing) and submits the full address instead of discarding it (#243)
- `docs/reports/dynamic-analysis-2026-07-04.md` — browser-driven runtime assessment of the live app; source of #279–#292
- `docs/wiki/learned-lessons/env-sourcing-and-cache-pitfalls-fixing-liquibase-data.md`, `allargs-constructor-positional-test-fragility.md`, `react-router-searchparams-not-resynced-on-navigation.md`, `jwt-excluded-claims-recur-across-fields.md` — reusable lessons from this session's investigation and fix work
- CI: `ci-cd-pipeline.yml` `integration-tests` job now parses `target/pit-reports/mutations.xml` and posts a PR comment with PIT mutation results (killed/survived/no-coverage counts, top 5 classes by surviving/uncovered mutants); pass/fail indicator is derived from **mutation score** (`killed / total generated`, including no-coverage mutants) — the metric `pom.xml`'s `mutationThreshold` actually gates on — with test strength (`killed / reached`) shown alongside for visibility only, since it is not gated; section omitted gracefully when `mutations.xml` is absent (e.g. PIT skipped). Note: issue #275 as filed named `ci.yml` as the target file and test strength as the gated metric — both were incorrect. `ci.yml` never runs `mvn verify`/PIT (only `clean test jacoco:report`), so `mutations.xml` never exists there; PIT only executes in `ci-cd-pipeline.yml`. Gating on test strength would also have produced a pass/fail indicator inconsistent with the actual `mutationThreshold` gate, which evaluates mutation score (see `pom.xml` pitest-maven plugin comment, added during #277) (#275)
- `PitNamingConventionTest` — ArchUnit rule in the `architecture` package enforcing that service-package test classes use `*ImplTest` or `*ServiceTest` suffixes; plain `*Test` classes are silently excluded from PIT's `targetTests` pattern and kill zero mutations while appearing in JaCoCo coverage; the rule fails the build at PR time with a rename suggestion (#278)
- React product detail page at `/products/:id`: image gallery (main + thumbnail strip), star rating (half-star aware, sm/md/lg sizes), quantity selector (clamped to stock), paginated reviews section (summary box with distribution bars + review list), related products grid (same-category, max 4); SEO meta (`document.title`, og:title/description/image); skeleton loader and 404 error state; add-to-cart placeholder (deferred to FE-06, #95) (FE-02, #93)
- `src/api/products.ts` — `fetchProductById(id)` (`GET /api/public/products/{id}`) typed API client (#93)
- `src/api/reviews.ts` — `fetchReviews(productId, page, size)` (`GET /api/products/{id}/reviews`) and `fetchReviewSummary(productId)` (`GET /api/products/{id}/reviews/summary`) typed API clients (#93)
- `src/hooks/useProduct.ts` — single-product data-fetching hook with cancellation-flag pattern (#93)
- `src/hooks/useReviews.ts` — parallel fetch of review list + summary with cancellation (#93)
- `src/components/product/StarRating.tsx` — half-star-aware rating display, amber-400 filled stars, three size variants (#93)
- `src/components/product/ImageGallery.tsx` — main image large display with thumbnail strip; graceful single-image degradation (#93)
- `src/components/product/QuantitySelector.tsx` — +/− buttons clamped to `[1, stockQuantity]` (#93)
- `src/components/product/ReviewsSection.tsx` — average rating + distribution bars summary box, paginated review list (#93)
- `src/components/product/RelatedProducts.tsx` — same-category product grid (max 4); uses `linkable={false}` on `ProductCard` to avoid anchor-in-anchor HTML nesting (#93)
- `src/pages/ProductDetailPage.tsx` — orchestrates gallery, rating, quantity, add-to-cart, reviews, and related products; uses `useParams<{ id: string }>()` for route param (#93)
- `ReviewUser`, `Review`, `ReviewSummary`, `PagedResponse<T>` TypeScript interfaces in `src/types/index.ts` (#93)
- React product listing page at `/`: TypeScript + Tailwind CSS v4 responsive grid (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4`); keyword search via form submit; client-side category multi-filter, sort (relevance / price asc-desc / newest), and pagination; loading skeleton (12 animated cards), per-card error and empty states; Vite dev-server proxy (`/api` → `:8080`) eliminates CORS in development (FE-01, #92)
- `src/types/index.ts` — shared TypeScript interfaces (`Product`, `Category`, `ApiResponse<T>`, `ProductFilters`, `SortOption`) for the React frontend (#92)
- `src/api/products.ts` — `fetchProducts()` (`GET /api/public/products`) and `searchProducts(keyword)` (`GET /api/public/products/search?keyword=`) typed API clients (#92)
- `src/api/categories.ts` — `fetchCategories()` (`GET /api/public/categories`) typed API client (#92)
- `src/hooks/useProducts.ts` — data-fetching hook with cancellation-flag pattern (async fetch in `useEffect`; returns `{ products, total, loading, error }`) (#92)
- `src/hooks/useCategories.ts` — category data-fetching hook with graceful empty-array fallback (#92)
- `src/components/product/ProductCard.tsx` — product card with category label, discount strikethrough price, out-of-stock badge, and placeholder emoji (#92)
- `src/components/product/ProductGrid.tsx` — responsive product grid with empty-state message (#92)
- `src/components/product/LoadingSkeleton.tsx` — 12-card animated skeleton loader (#92)
- `src/components/filters/CategorySidebar.tsx` — multi-select category filter checkboxes with "Clear filters" button (#92)
- `src/components/filters/SortDropdown.tsx` — sort order selector (relevance / price asc / price desc / newest) (#92)
- `src/components/common/Pagination.tsx` — prev/next + numbered page buttons with ellipsis via `buildPageRange` helper (#92)
- `src/components/common/ErrorMessage.tsx` — error display component with optional retry callback (#92)
- `src/pages/ProductListingPage.tsx` — orchestrates header (logo + search), toolbar (count + sort), sidebar (categories), grid, and pagination; manages filter state and page resets on keyword/category change (#92)
- Full-text product search at `GET /api/v2/products/search`: delegates to Elasticsearch when `elasticsearch.enabled=true` (multi-field match across name^3, description, categoryName with fuzziness; category/price/stock filters applied); falls back to JPA `advancedSearch` when ES is disabled (SRCH-01, #74)
- `ProductSearchService` interface + `ProductSearchServiceImpl` (`@ConditionalOnProperty("elasticsearch.enabled","true")`): `search`, `indexProduct`, `deleteFromIndex`, `reindexAll` with Resilience4j circuit-breaker protection and graceful degradation on `CallNotPermittedException` (SRCH-01/SRCH-02, #74/#75)
- `ProductDocument` Elasticsearch document (`@Document(indexName="products")`): name (Text, standard analyzer, boost×3), description (Text), categoryId/categoryName (Keyword), price/discountPrice (Double), inStock/isActive (Boolean), sku/imageUrl (Keyword), createdAt (Date) (#74)
- `ProductElasticsearchRepository`: `fullTextSearch` (multi_match native query), `findByCategoryIdAndIsActiveTrue`, `findByIsActiveTrue` (#74)
- `ProductCreatedEvent`, `ProductUpdatedEvent`, `ProductDeletedEvent` — Spring `ApplicationEvent` subclasses carrying the affected `Product` or `productId` (SRCH-02, #75)
- `ProductIndexEventListener` (`@ConditionalOnProperty("elasticsearch.enabled","true")`): `@Async @EventListener` handlers for all three product events; calls `ProductSearchService.indexProduct` / `deleteFromIndex` (#75)
- `POST /api/v1/admin/search/reindex` (admin only): triggers `ProductSearchService.reindexAll()`; returns 503 if Elasticsearch is disabled (SRCH-02, #75)
- `ProductSearchServiceTest` — 22 unit tests: full-text query routing, category routing, all-active routing, price-range post-filter, indexProduct mapping, deleteFromIndex, reindexAll; circuit breaker OPEN paths for all 4 methods, repository exception paths, blank-query fallback routing, inStock filter excluding out-of-stock documents, toDocument null-field branches (null category, null price/discountPrice, null/zero stockQuantity) (#74/#75, #264)
- `ProductIndexEventListenerTest` — 3 unit tests: created/updated/deleted event dispatch (#75)
- `AdminSearchControllerIntegrationTest` — 4 integration tests: admin 200, 403, 401, service-exception 500 (#75)
- `AdminSearchControllerTest` — 2 unit tests: `Optional.empty()` (Elasticsearch disabled) → 503, `Optional.of(mock)` → 200 with `reindexAll` invocation; the 503 path is structurally unreachable from the integration test because `TestElasticsearchConfig` always provides the bean (#75, #268)
- Payment refund endpoint `POST /api/v1/admin/orders/{id}/refund` (admin only): accepts `{ amount, reason }`, validates amount ≤ original payment, calls Razorpay refund API, sets Payment status to `REFUNDED` (full) or `PARTIALLY_REFUNDED` (partial), auditable (PAY-02, #61)
- `PaymentServiceImpl.processRefund`: find-by-order-id → SUCCESS-status guard → amount guard → gateway call → status + `refundedAmount` + `refundReason` + `refundInitiatedAt` update (#61)
- `RefundRequest` payload — `@NotNull @DecimalMin("0.01") Double amount`, optional `String reason` (#61)
- `PaymentRepository.findByOrderId` — for refund lookup by internal order ID (#61)
- `GlobalExceptionHandler` now handles `PaymentProcessingException` → HTTP 400 (#61)
- Liquibase changeset `20260629-009` — adds `refunded_amount`, `refund_reason`, `refund_initiated_at` columns to `payment` table and extends MySQL status ENUM with `PARTIALLY_REFUNDED` (PAY-02, #61)
- `PaymentRefundServiceTest` — 5 unit tests: full refund, partial refund, excess amount, non-SUCCESS status, payment not found, gateway failure (#61)
- `AdminPaymentRefundControllerIntegrationTest` — 8 integration tests: full/partial refund, excess amount (400), non-SUCCESS (400), missing field (400), no payment (404), 403, 401 (#61)
- Razorpay webhook endpoint at `POST /api/v1/webhooks/payment` (public; no JWT): validates `X-Razorpay-Signature` via HMAC-SHA256, handles `payment.captured` (Payment→SUCCESS, Order→PAID) and `payment.failed` (Payment→FAILED, Order→PAYMENT_FAILED) with idempotency protection; returns 200 on success, 401 on invalid signature (PAY-01, #60)
- `PaymentServiceImpl.processWebhookEvent`: signature validation → JSON parse → find-by-razorpay-order-id → idempotency check → status update + `OrderService.updateOrderStatus` + `DomainEventPublisher` (#60)
- `razorpay.webhook.secret` property (`${RAZORPAY_WEBHOOK_SECRET:test_webhook_secret}`) in `application.properties` (#60)
- `PaymentWebhookServiceTest` — 7 unit tests: captured event, failed event, invalid signature, idempotency skip, payment not found, unknown event, malformed JSON (#60)
- `PaymentWebhookControllerIntegrationTest` — 4 integration tests: valid webhook → 200, invalid signature → 401, missing header → 401, public access without JWT → 200 (#60)
- Shipping method and cost calculation service: `GET /api/v1/checkout/shipping-options?postalCode=` returns all active methods with `calculatedCost = baseCost + (costPerKg × totalWeightKg × zoneMultiplier)`; zone derived from postal code prefix hash into configurable `app.shipping.zone-multipliers` list (SHIP-01, #87)
- `ShippingConfig` (`@ConfigurationProperties("app.shipping")`): `defaultWeightPerItemKg` (default 0.5 kg/unit) and `zoneMultipliers` (default [1.0, 1.5, 2.0]) — fully configurable without code changes (#87)
- `AdminShippingController` at `GET/POST/PUT/DELETE /api/v1/admin/shipping-methods`: full CRUD with `@Auditable` on all mutating operations; DELETE is a soft-deactivate (`isActive=false`) (#87)
- `ShippingServiceImplTest` — 16 unit tests covering cost formula, zone resolver boundary cases (null/blank postal, short postal, hash consistency), empty-cart guard, and admin CRUD (#87)
- `AdminShippingControllerIntegrationTest` — 11 integration tests covering list, create (201 + isActive assertion), update, soft-delete (DB `isActive=false` verified), validation (400), role enforcement (403), 404, and GET /{id} (found → 200, not-found → 404) (#87, #268)
- Multi-step checkout flow at `POST /api/v1/checkout/{address,shipping,payment,confirm}`: address selection → shipping method → payment initiation (delegates to `PaymentService`) → order confirmation; session stored in Redis with 30-minute TTL; out-of-order step returns 409 Conflict (CHK-01, #76)
- `CheckoutSessionStore` interface + `RedisCheckoutSessionStore` implementation (`StringRedisTemplate` + Jackson; key `checkout:session:{userId}`; 30-min TTL) for Redis-backed checkout session management (#76)
- `CheckoutSession` / `CheckoutStep` — serializable session POJO and step enum (`PENDING_SHIPPING → PENDING_PAYMENT → PENDING_CONFIRM`) (#76)
- `ShippingMethod` JPA entity + `ShippingMethodRepository` mapping the `shipping_methods` table (created in #104/#87): name, baseCost, costPerKg, estimatedDaysMin/Max, isActive (#76)
- `AddressRepository` — for checkout address ownership validation (#76)
- `SetAddressRequest` + `SelectShippingRequest` — validated request payloads for checkout step 1 and step 2 (#76)
- `CheckoutSessionDTO` — response DTO exposing current session step, addressId, shippingMethodId, shippingCost, orderId, razorpayOrderId (#76)
- `CheckoutFlowIntegrationTest` — 13 integration tests covering happy paths, invalid step transitions (409), wrong-owner address (404), inactive shipping method (404), unauthenticated access (401), and GET /shipping-options (authenticated with active cart → 200, unauthenticated → 401); `@MockBean CheckoutSessionStore` and `@MockBean PaymentService` used to avoid Redis/Razorpay in tests (CHK-01, #76, #262)
- Inventory management admin endpoints: `GET /api/v1/admin/inventory` (list all with `InventoryDTO` — productId, productName, qty, reservedQty, availableQty, status) and `PATCH /api/v1/admin/inventory/{productId}` (delta-based adjustment with required reason; validates result ≥ 0; `@Auditable`) (ADM-06, #72)
- `InventoryAuditLog` JPA entity + `InventoryAuditLogRepository` mapping the `inventory_audit_log` table (created in #104); every `PATCH` adjustment writes a record with before/change/after quantities, reason, actor, and `referenceType=MANUAL` (#72)
- `InventoryDTO` — list-view DTO for admin inventory endpoint (#72)
- `AdjustInventoryRequest` — `{ @NotNull Integer delta, @NotBlank String reason }` request payload (#72)
- `AdminInventoryControllerIntegrationTest` — 9 integration tests covering list, positive/negative delta, below-zero guard (400), missing reason (400), 404, role enforcement (403), and audit log record verification (ADM-06, #72)
- Order management admin endpoints at `GET/PATCH /api/v1/admin/orders` — list with status/userId/dateFrom/dateTo filters + pagination, full detail with nested items, forward-only status transitions (PENDING→CONFIRMED→SHIPPED→DELIVERED; any→CANCELLED) enforced server-side; `PATCH` is `@Auditable`; customer notification dispatched on each transition (ADM-03, #69)
- `OrderSpecification` — composable `JpaSpecificationExecutor`-based filter for admin order listing (#69)
- `OrderSpecificationTest` — 13 unit tests: fetch-join guard (entity query adds join, count query skips it to avoid JPA exception), `isDeleted=false` predicate always applied, all 4 optional filters (status, userId, dateFrom, dateTo) present and absent, all filters combined; raw `@Mock Path isDeletedPath` used to satisfy `cb.isFalse(Expression<Boolean>)` type signature (#69, #265)
- `AdminOrderDetailDTO` + `OrderItemDTO` — full admin order view with nested item lines (#69)
- `UpdateOrderStatusRequest` — validated request payload for status transitions (`@NotBlank status`, optional `cancellationReason`) (#69)
- `NotificationServiceImpl` — stub implementation of `INotificationService`; logs all calls; real email delivery wired in #62 (#69)
- `NotificationServiceImplTest` — 14 unit tests covering all dispatch methods (`sendOrderConfirmation`, `sendShipmentNotification` both overloads, `sendPasswordResetEmail`, `sendVerificationEmail`), null-user/null-email early-exit guards, null optional fields, shipping address context branch, anonymous `MailException` subclass, and `MessagingException` → `MailException` wrapping in `sendEmail`; `@Value` fields injected via `ReflectionTestUtils`; real `MimeMessage` used for success paths, mocked for error paths (#69, #263)
- `AdminOrderControllerIntegrationTest` — 13 integration tests covering list, filter, detail, valid/invalid transitions, and role enforcement (ADM-03, #69)
- Product CRUD admin endpoints at `POST/GET/PUT/DELETE /api/v1/admin/products` — all `@PreAuthorize("hasRole('ADMIN')")`, all `@Auditable`; image upload via `POST /api/v1/admin/products/{id}/images` (multipart/form-data, 10 MB cap, JPEG/PNG/WebP/GIF only) (ADM-01, #67)
- `StorageService` interface + `LocalStorageService` implementation (UUID-keyed filenames, configurable `app.storage.location`, static serving via `/uploads/**`) (#67)
- `StorageConfig` (`WebMvcConfigurer`) serving uploaded files from the configured storage directory (#67)
- `Product.updateProductImage` service method for atomic image URL updates (#67)
- `AdminProductControllerIntegrationTest` — 11 integration tests covering create, read, update, soft-delete, image upload, and role-enforcement (403/401) (ADM-01, #67)
- `/api/v1/admin/**` URL-level `hasRole("ADMIN")` rules added to `SecurityConfig` and `TestSecurityConfig` (was missing; only `/api/admin/**` was covered) (#67)
- Liquibase XML master orchestrator (`db.changelog-master.xml`) replacing direct SQL master reference; enables per-entity XML changeset files and clean include-based composition (#104)

- M4 test coverage gap remediation — pushed all packages above the JaCoCo 85% instruction-coverage gate; issues #258, #261–#265, #267–#269:
  - `RateLimiterServiceTest` +7 tests: circuit breaker OPEN path (`isAllowed` returns `true`), null increment, null TTL, `resetRateLimit` on error and on OPEN breaker (#258)
  - `ProductReviewServiceImplTest` +12 tests: duplicate-review guard, user/product not-found paths for `createReview`/`updateReview`/`deleteReview`/`markAsHelpful`, empty rating distribution, purchase-false path, all delegation methods (`getReviews`, `getReviewSummary`, `getTopHelpfulReviews`, `getReviewsByUser`) (#258)
  - `HttpsEnforcementFilterTest` (new) — 6 tests: enforcement disabled passes through, secure connection passes, insecure without header → 403, `X-Forwarded-Proto: https` overrides socket → pass, `X-Forwarded-Proto: http` overrides secure socket → 403, case-insensitive header comparison (#261)
  - `RedisCheckoutSessionStoreTest` (new) — 6 tests: `save` serialises with correct key (`checkout:session:{userId}`) and 30-minute TTL, serialisation failure → `IllegalStateException`, `find` absent/valid/corrupted JSON (corrupted deletes the key), `delete` removes key (#262)
  - `ProductControllerV2Test` +1 test: `searchProducts` when `Optional<ProductSearchService>` is present delegates to ES service and never calls JPA fallback (#264)
  - `ApiSunsetInterceptorTest` expanded 6 → 12 tests: `replacedBy` non-empty adds `X-API-Replaced-By` header, `warningDays=1000000` triggers approaching-sunset warning permanently without date rot, class-level `@ApiSunset` annotation applies headers, expired sunset with `replacedBy` includes replacement in 410 body, writer exception does not propagate, `enforce=false` past-sunset logs warn and allows request (#267)
  - `AdminServiceImplTest` +6 tests: `getUserById` not-found, `updateUser(Long, User)` (method was 0% covered), `updateUser` not-found, `updateUserByAdmin` not-found, `deleteUser` not-found, `convertToDto` with null roles (#268)
  - `AdminProductControllerTest` +3 tests: `createProduct` exception → 400, `updateProduct` exception → 400, `uploadProductImage` general `Exception` (second catch block) → 500 (#268)
  - `InventoryThresholdManagementServiceTest` +13 tests: not-found paths for all 6 methods (`setProductThreshold`, `setCategoryThreshold`, `getProductThreshold`, `getCategoryThreshold`, `useProductCategoryThreshold`, `getEffectiveThreshold`), `getCategoryThreshold` cache-miss with null category threshold returns 0, `getEffectiveThreshold` with `useCategoryThreshold=true` but no category falls back to product threshold (#269)
  - `InventoryReservationCleanupJobTest` (new) — 2 tests: success delegation and exception-swallowing; `InventoryReservationIntegrationTest` tests `InventoryServiceImpl`, not the scheduler (#269)
  - `InventoryMonitoringScheduler` confirmed at practical ceiling (83.3%): only uncovered lines are the catch block of `generateDailyInventorySummary()` whose try block contains only `log.info()` — dead code by design, no artificial test written (#269)
- `RazorpayClientAdapterTest` +3 tests: `createOrder` exception → `RuntimeException`, `refundPayment` exception → `PaymentProcessingException`, `fetchPaymentDetails` exception → `RuntimeException`; `initClient()` failure path is at practical ceiling (requires PowerMock-style constructor interception not available in Mockito) (#266)
- `ConstraintValidatorsTest` (new — 34 tests, `validator` package) + `EmailPhoneValidatorTest` +3 tests (`validation` package): full coverage of all 7 package-private `ConstraintValidator` implementations — `EmailValidator`, `PasswordStrengthValidator`, `PhoneNumberValidator`, `PostalCodeValidator`, `PriceValidator`, `QuantityValidator`, `SKUValidator`; each covers null, valid, boundary, and invalid-format cases; both null-passthrough and length/range guard branches are now killed by PIT (#260)

### Fixed
- `start.sh`: `cleanup()` could fire multiple times on Ctrl+C — backgrounded subshells inherit the parent's trap table, so each ran its own copy on exit; added a `CLEANED_UP` guard and `trap - EXIT INT TERM` inside each subshell to clear the inherited traps
- `start.sh`: MySQL health-wait loop had no timeout and would spin forever with no error if MySQL never became healthy (e.g. an unfilled `.env` copied fresh from `.env.example`, the exact state the script itself creates on first run); added a 120s timeout with a `docker compose logs mysql` pointer
- `SecurityConfig` and `TestSecurityConfig`: add `POST /api/auth/refresh` to `permitAll()` — was returning 401 when access token expired, forcing clients into a broken loop
- `SecurityConfig` and `TestSecurityConfig`: split blanket `/api/password/**` permit into explicit `/forgot` and `/reset` (public) and `/change` (requires `USER` or `ADMIN`) — unauthenticated callers could previously invoke the password-change endpoint
- `CheckoutController.calculateTotal` (`GET /api/checkout/calculate-total/{cartId}`): add `@PreAuthorize("hasRole('USER')")` — the only method in the controller missing the annotation its siblings already carried
- `WebhookAdminController`: add class-level `@PreAuthorize("hasRole('ADMIN')")` — the only controller under `/api/admin/**` without an authorization annotation
- `CacheConfig.cacheManager` now injects the Spring-managed `ObjectMapper` bean into `GenericJackson2JsonRedisSerializer` (all 9 cache regions: products, categories, auditLogs, userPermissions, inventoryItems, rateLimitStats, orders, users, and default); previously each region created its own bare `ObjectMapper` instance that could not handle Hibernate lazy-proxy types, causing silent cache-write failures for JPA entities
- `application.properties`: add `spring.jpa.properties.hibernate.type.preferred_boolean_jdbc_type=TINYINT` so Hibernate 6.x schema validation correctly maps `Boolean` entity fields to MySQL `TINYINT(1)` (MySQL `BOOLEAN` synonym) instead of `BIT(1)`, eliminating column-type mismatch errors on startup
- `application.properties`: externalize `spring.jpa.hibernate.ddl-auto` via `${SPRING_JPA_DDL_AUTO:validate}` to allow environment-specific override without code changes

### Changed
- `App.tsx`: added `/products/:id` route wired to `ProductDetailPage` (FE-02, #93)
- `src/components/product/ProductCard.tsx`: added `linkable?: boolean` prop (default `true`); when `false` renders the card div without a wrapping `<Link>`, enabling parent-controlled navigation in `RelatedProducts` (#93)
- `SecurityConfig` and `TestSecurityConfig`: add `GET /api/products/*/reviews`, `/reviews/summary`, and `/reviews/top-helpful` to `permitAll()` (required for unauthenticated product detail page) (#93)
- `App.tsx` migrated from JSX stub to full TypeScript: wraps `ProductListingPage` in `BrowserRouter` + `Routes` (react-router-dom) (FE-01, #92)
- `main.tsx` migrated from `.jsx` to `.tsx`: null-checked root element, no `.jsx` import extension (#92)
- `vite.config.js` replaced by `vite.config.ts`: added `@tailwindcss/vite` plugin and `/api` proxy to `http://localhost:8080` (#92)
- `src/index.css` replaced with Tailwind v4 import (`@import "tailwindcss"`) (#92)
- `eslint.config.js`: added `typescript-eslint`; disabled `react-hooks/set-state-in-effect` rule (hooks v7 incorrectly flags valid async data-fetching patterns documented by the React team) (#92)
- `ProductControllerV2.searchProducts` now routes to `ProductSearchService` (ES) when the bean is present, falling back to `ProductService.advancedSearch` (JPA) when absent; uses `Optional<ProductSearchService>` injection (#74)
- `ProductServiceImpl.advancedSearch` replaced in-memory stream filter with proper JPA `ProductRepository.advancedSearch` query (fixes O(n) memory load and incorrect pagination); `findByCategory` and `getProductsByCategory` likewise replaced with repository-level queries (#74)
- `ProductServiceImpl` now publishes `ProductCreatedEvent`, `ProductUpdatedEvent`, `ProductDeletedEvent` from `createProduct`, `updateProduct`, `deleteProduct` via `DomainEventPublisher` (#75)
- `TestElasticsearchConfig` extended with `ProductSearchService` and `ProductElasticsearchRepository` mocks so integration tests remain ES-free (#74/#75)
- `Payment` entity extended with `refundedAmount`, `refundReason`, `refundInitiatedAt` fields; status comment updated to include `PARTIALLY_REFUNDED` (#61)
- `AdminOrderController` now injects `PaymentService` and exposes `POST /{id}/refund` (#61)
- `RazorpayClientAdapter.refundPayment` throws `PaymentProcessingException` instead of generic `RuntimeException` (#61)
- `Order.OrderStatus` enum extended with `PAID` and `PAYMENT_FAILED` to represent webhook-confirmed payment outcomes (#60)
- `OrderServiceImpl.VALID_TRANSITIONS` extended: `PAID→{SHIPPED, CANCELLED}`, `PAYMENT_FAILED→{CANCELLED}` (#60)
- `PaymentRepository` extended with `Optional<Payment> findByRazorpayOrderId(String)` for idempotent webhook lookup (#60)
- `PaymentServiceImpl` refactored to explicit constructor injection (`@Lazy OrderService` to avoid circular dependency); `processWebhookEvent` added; `RazorpayClientAdapter.refundPayment` fixed to call `razorpayClient.payments.refund()` (was a no-op stub) (#60)
- `/api/v1/webhooks/**` added to `permitAll()` in both `SecurityConfig` and `TestSecurityConfig` (#60)
- `MultiStepCheckoutController` extended with `GET /api/v1/checkout/shipping-options` delegating to `ShippingService` (#87)
- `CheckoutService` extended with 4 new methods (`setAddress`, `selectShipping`, `initiatePayment`, `confirmCheckout`); `CheckoutServiceImpl` extended with implementations + `AddressRepository`, `ShippingMethodRepository`, `PaymentService`, `CheckoutSessionStore` injected via constructor (#76)
- `AdminInventoryController` migrated from `/api/admin/inventory` to `/api/v1/admin/inventory`; `GET /` and `PATCH /{productId}` added; legacy sub-path endpoints retained for backward compatibility (ADM-06, #72)
- `InventoryRepository` extended with `@EntityGraph({"product"}) findAll()` to prevent N+1 queries on admin list (#72)
- `InventoryServiceImpl` extended with `getAllInventorySummary()` and `adjustStock()` (delta validation, audit write, status recalculation, `LowStockWarningEvent` if threshold crossed) (#72)
- `AdminInventoryControllerTest` URL references updated from `/api/admin/inventory` to `/api/v1/admin/inventory` (#72)
- `AdminOrderController` rewritten at `/api/v1/admin/orders` (was `/api/admin/orders`); removed try/catch anti-pattern; delegates all error handling to `GlobalExceptionHandler` (ADM-03, #69)
- `OrderRepository` extended with `JpaSpecificationExecutor<Order>` for specification-based admin queries (#69)
- `OrderServiceImpl` extended with `adminUpdateOrderStatus` (transition validation + notification) and `getAdminOrders` / `getAdminOrderDetail` admin methods (#69)
- `AdminProductController` base path corrected from `/api/admin/products` to `/api/v1/admin/products` (ADM-01, #67)
- `deleteProduct` changed from hard delete (`deleteById`) to soft delete (`isActive = false`) (ADM-01, #67)
- `AuditAspectIntegrationTest` and `InputValidationSecurityTest` URL references updated to `/api/v1/admin/products` (#67)
- Liquibase changeset `addresses` table: user address book with user FK, default-flag, address-type, and covering indexes (USR-01, #78, #104)
- Liquibase changeset `product_variants` table: size/colour variants per product with SKU uniqueness, price adjustment, and stock quantity (PROD-01, #81, #104)
- Liquibase changeset `product_images` table: multiple ordered images per product with primary-flag (PROD-02, #82, #104)
- Liquibase changeset `product_tags` + `product_tag_map` join table: many-to-many product tagging with slug uniqueness (PROD-03, #83, #104)
- Liquibase changeset `coupons` table: PERCENTAGE/FIXED_AMOUNT discount codes with usage limits, validity windows, and min-order threshold (CHK-02, #77, #104)
- Liquibase changeset `shipping_methods` table: base cost, per-kg cost, and estimated delivery day range (SHIP-01, #87, #104)
- Liquibase changeset `return_requests` table: order return/refund flow with status lifecycle (PENDING → APPROVED/REJECTED → REFUNDED) and admin notes (RET-01, #88, #104)
- Liquibase changeset `inventory_audit_log` table: full inventory change audit trail with change type, before/after quantities, reference type/ID, and actor FK (ADM-06/INV-01, #72, #73, #104)

---

## [0.4.0] — 2026-06-24 (M3: Technical Debt Reduction)

### Changed
- Upgrade Elasticsearch, Kibana, and Logstash Docker images from 8.10.2 to 8.17.6 (#236)
- Relocate git repository root from `backend/` to `BuildNest/` project root to bring frontend and CI/CD files under version control (#233)
- Harden Content Security Policy: remove `'unsafe-inline'` from `script-src` and `style-src` on all API paths; introduce dedicated `@Order(1)` `SecurityFilterChain` scoping `'unsafe-inline'` to Swagger UI documentation paths only; add `frame-ancestors 'none'` and `form-action 'self'` to main chain (SEC-14, #237)
- Implement explicit circuit breaker fallbacks: add `elasticsearchCircuitBreaker` bean; protect all ES repository calls in `ElasticsearchIngestionService` (async writes skip silently on CB OPEN; reads return empty list); distinguish `CallNotPermittedException` from transient failures in `RateLimiterService` — CB OPEN logged at DEBUG, not WARN (#238)
- Raise JaCoCo instruction coverage gate from 50% to 55% — actual coverage 94.57%, no package below 55% (#239)
- Restore PIT mutation score from 73% to 85%: add boundary-value tests for HTTP status code classification, CB-OPEN fallback tests for all ES read/write paths, and non-empty return assertions on all read methods to kill surviving "replaced return value with emptyList" mutations (#240)
- Audit and enforce `@Auditable` AOP coverage on all admin endpoints: verify all `@PostMapping`, `@PutMapping`, `@DeleteMapping` in `admin/` carry `@Auditable`; add `AuditAspectIntegrationTest` (TC-AUDIT-001 to TC-AUDIT-006) proving audit entries capture user ID, action, entity type, and IP address through the full HTTP → AOP → AuditLogService pipeline; fix timing-sensitive `UptimeMonitoringServiceTest.uptimeMetricsFailSlaWhenDowntimeHigh` by replacing `Thread.sleep` with deterministic clock back-dating via `ReflectionTestUtils` (SRS ADM-06, RTM AUDIT-01, #59)

### Fixed
- `ElasticsearchConfig.clientConfiguration()` now passes injected credentials via `.withBasicAuth()` — previously credentials were declared but never forwarded to the client builder, causing HTTP 401 against any secured cluster (#236)
- Add explicit `FetchType.LAZY` to `Category.products` and `Order.orderItems` associations to eliminate N+1 query risk (DC-08, #54)

---

## [0.3.0] — 2026-06-22 (M2: Quality Foundation)

### Added
- OWASP Dependency-Check Maven plugin (`owasp` profile, CVSS ≥ 7.0 fails build) (#53)
- Integration tests for rate-limiting behaviour using Bucket4j/Redis (#51)
- Comprehensive unit tests for `CartService` and `WishlistService` — 15 and 14 tests respectively (#50)
- Comprehensive unit tests for `OrderServiceImpl` including edge cases (#49)
- Comprehensive unit tests for `ProductServiceImpl` including pagination edge cases (#48)
- Edge-case unit tests for `AuthServiceImpl` (#47)
- All 62 required environment variables documented across 16 sections in `.env.example` (#52)

### Changed
- Raise JaCoCo instruction coverage gate from 40% to 50% (#46)

### Fixed
- `ProductServiceImpl.advancedSearch()` and `findByCategory()` pagination bug corrected as part of test expansion (#48)

---

## [0.2.0] — 2026-06-14 (M1: Stabilisation)

### Fixed
- Add missing `RoleRepository` mock in `AuthServiceImplTest` — test context failed to load (#38)
- Add `@Tag("e2e")` to `OrderApiTest` so it is correctly excluded from the unit-test profile (#40)
- Correct HTTP status assertions in `testRoleHierarchyEnforcement` — expected 403, was asserting 200 (#41)
- Correct XSS test assertion in `InputValidationSecurityTest`: 401 → 400 (#42)
- Correct file-upload test assertions in `InputValidationSecurityTest`: 401 → 415 (#43)

---

## [0.1.0] — 2026-06 (Pre-M1: Foundation)

### Added
- Spring Boot 3.5 / Java 21 backend: REST API, JWT auth (access + refresh tokens), Spring Security RBAC with `Permission` entities
- JPA entities: `User`, `Product`, `Category`, `Order`, `OrderItem`, `Cart`, `CartItem`, `Wishlist`, `Inventory`, `Payment`, `ProductReview`, `AuditLog`, `Role`, `Permission`, `RefreshToken`, `PasswordResetToken`, `WebhookSubscription`
- Liquibase-managed schema migrations (DDL auto = `validate`)
- Redis-backed rate limiting (Bucket4j) and caching (`@Cacheable`)
- Elasticsearch 8.x integration for audit log ingestion, metrics collection, and alerting
- `@Auditable` AOP aspect for declarative audit logging
- `ApiSunsetInterceptor` adding deprecation headers to v1 product endpoints
- Dual API versioning: `ProductControllerV1` and `ProductControllerV2`
- Docker Compose stack: MySQL 8.2, Redis 7, Elasticsearch, Kibana, Logstash, Prometheus
- GitHub Actions CI/CD: build, test, JaCoCo coverage, CodeQL, OWASP Dependency-Check
- Structured Logback/Logstash JSON logging pipeline to Elasticsearch
- Resilience4j circuit breaker configuration
- SDLC documentation suite: SRS (ISO/IEC/IEEE 29148), SDD (IEEE 1016), RTM, Test Plan (ISO 29119-3), SDP, V&V Report, BRD, ICD, CSD, metrics and quality reports
- React 19 / Vite frontend scaffold (stub; real UI not yet built)
